# 2026-04-24

## 1. Windows + WSL/Linux 双平台全量稳健性测试已完成，Python e2e 认证脚本已同步到当前单个 `key` 模型

截至 2026-04-24，已完成当前轮“Windows + WSL/Linux 双平台全量稳健性测试”的主体执行。首次进入 Python e2e 时，命中的不是产品运行态缺陷，而是测试脚本仍停留在旧认证模型：`proxy_groups` seed 仍写 `token_id/token_hash`，`frpc` 启动仍传 `--token`；而当前代码树已经明确改成 `client_id/client_secret_hash` 与 `--key`。

本次完成的测试脚本收口如下：

- `test/e2e_tcp_single.py`
  - `proxy_groups` seed 改为写入 `client_id`、`client_secret_hash`
  - `frpc` 启动参数改为 `--key`
- `test/e2e_tcp_range.py`
  - `proxy_groups` seed 改为写入 `client_id`、`client_secret_hash`
  - `frpc` 启动参数改为 `--key`
- `test/e2e_udp_single.py`
  - `proxy_groups` seed 改为写入 `client_id`、`client_secret_hash`
  - `frpc` 启动参数改为 `--key`
- `test/e2e_udp_range.py`
  - `proxy_groups` seed 改为写入 `client_id`、`client_secret_hash`
  - `frpc` 启动参数改为 `--key`
- `test/e2e_management_webui.py`
  - 管理面返回字段由 `token` 校验改为 `key`
  - 轮换接口由 `/token` 改为 `/key`
  - 数据库校验由 `token_id/token_hash` 改为 `client_id/client_secret_hash`
  - `build_or_resolve_webui_dist()` 改为优先复用仓库现成的 `frps/webui/dist`
  - Linux / WSL 下不再误选 `npm.cmd`

本次确认并收束的边界如下：

- 当前 Python e2e 失败的首个阻塞点是测试脚本与现行认证/schema 脱节，不是 `frps` / `frpc` 产品代码把登录或数据库打坏。
- 本次不改业务协议，不回退到旧 `token` 模型；测试入口直接跟随当前单个 `key` UX。
- 当前管理 e2e 在 Linux / WSL 下已经可以直接复用仓库现有 `dist` 目录，不再依赖 Windows `npm.cmd` 可执行文件。

本次执行的验证如下：

- `python -m py_compile test/e2e_tcp_single.py test/e2e_tcp_range.py test/e2e_udp_single.py test/e2e_udp_range.py test/e2e_management_webui.py`
- `python test/e2e_tcp_single.py --scenario happy_path`
- `python test/e2e_tcp_range.py`
- `python test/e2e_udp_single.py --scenario happy_path`
- `python test/e2e_udp_single.py --scenario idle_cleanup`
- `python test/e2e_udp_range.py`
- `python test/e2e_management_webui.py`
- `python test/e2e_tcp_single.py --scenario bad_token`
- `python test/e2e_tcp_single.py --scenario disabled_group`
- `python test/e2e_tcp_single.py --scenario disabled_tunnel`
- `python test/e2e_tcp_single.py --scenario local_unavailable`
- `python -m py_compile test/e2e_management_webui.py`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_single.py --scenario happy_path"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_range.py"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_udp_single.py --scenario happy_path"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_udp_single.py --scenario idle_cleanup"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_udp_range.py"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_management_webui.py"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_single.py --scenario bad_token"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_single.py --scenario disabled_group"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_single.py --scenario disabled_tunnel"`
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp && python3 test/e2e_tcp_single.py --scenario local_unavailable"`

## 2. WSL/Linux 稳定性矩阵暴露的是测试关闭语义过窄，不是控制面运行时真实缺陷

截至 2026-04-24，WSL/Linux 首次执行 `frps` 稳定性矩阵 `all` 时，在 `gomaxprocs` 档位命中过一次 `TestServerScenarioRejectsInitialInvalidEffectiveIPWithoutLeakingSessionState` 失败；表象是服务端已经正确回了 `config.apply_failed` 并关闭 startup session，但测试里的二次读收到的是 `io: read/write on closed pipe`，而不是原先只接受的 `io.EOF` / `net.ErrClosed`。

本次完成的收口如下：

- 对同一失败点做了 WSL 定向复验：
  - 单测定向 `-count=10` 通过
  - `control-core` 全套 `-count=5` 通过
- 对照现有测试发现：
  - `frps/internal/control/abnormal_input_test.go` 已有 `errorsIsEOFOrClosed()`，明确把 `closed pipe` 视为合法关闭变体
  - `effective_ip_scenario_test.go` 与 `server_test.go` 仍保留旧的窄判定
- 已把以下测试改成统一使用 `errorsIsEOFOrClosed()`：
  - `frps/internal/control/effective_ip_scenario_test.go`
  - `frps/internal/control/server_test.go`

本次确认并收束的边界如下：

- 当前没有发现 `frps/internal/control` 在该失败点上的业务状态错误；错误帧、runtime issue、session 清理和 group slot 释放都符合预期。
- 当前收掉的是跨平台 `net.Pipe` 关闭时机导致的测试断言脆弱点，不改控制面业务逻辑，不改恢复判定。
- 当前 Linux / WSL 下真正需要统一的是“startup rejection 后连接关闭的允许错误集合”，而不是额外改登录或恢复实现。

本次执行的验证如下：

- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile all"`（首次失败，命中 `control-core-cpu-matrix` 的关闭语义脆弱点）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=10 -tags testhooks -cpu 1,2,4 -run '^TestServerScenarioRejectsInitialInvalidEffectiveIPWithoutLeakingSessionState$' -timeout 240s ./internal/control"`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=5 -tags testhooks -cpu 1,2,4 -run '^(TestServerScenarioRejectsInitialInvalidEffectiveIPWithoutLeakingSessionState|TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioRecoversNonListeningTunnelAfterPollingSeesOccupancyGone|TestServerScenarioKeepsRuntimeIssueUntilSamePortFlappingReallyRecovers|TestServerScenarioDeduplicatesMatchingRefreshAndScanRecoveryPush|TestServerScenarioIgnoresStaleScanSnapshotAfterRefreshAdvancesVersion|TestServerScenarioUsesFreshSnapshotWhenRepositoryChangesDuringLoginHandshake|TestServerScenarioKeepsSessionAliveWhenPortBecomesOccupiedRightAfterStartupAck|TestServerScenarioShrinksToEmptyConfigWhenEffectiveIPBecomesInvalidRightAfterStartupAck|TestServerScenarioReconnectAfterEmptyConfigClearsOldRuntimeIssueAndListeners|TestServerScenarioReconnectAfterPendingRecoveryPushDropsOldPendingConfig|TestServerScenarioKeepsEffectiveIPRuntimeIssueAcrossHighFrequencyEmptyAndFullRecoveryFlaps|TestServerScenarioKeepsSessionAliveWhenTCPRecoveryBindFailsWithPermissionDenied|TestServerScenarioClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn|TestServerScenarioKeepsSessionAliveWhenUDPRecoveryBindFailsWithPermissionDenied|TestServerScenarioServesHeartbeatWhileEmptyAndFullRecoveryConfigsArePending|TestServerScenarioLateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession|TestServerScenarioRejectsOutOfOrderRecoveryAckWithoutLeavingPendingOrListeners|TestServerScenarioRejectsDuplicateRefreshAckWithoutLeavingListenersOrPending|TestServerScenarioDelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession|TestServerUnregisterOldSessionKeepsReplacementSlotAndSession|TestServerScenarioReleasingBlockedStartupBindAfterClientDisconnectLeavesNoListenerLeak|TestServerScenarioReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|TestServerScenarioReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak|TestServerScenarioSkipsOverlappingRuntimeScanRoundDuringRecoveryPush|TestServerScenarioScanReusesFreshNetworkSnapshotBeforeRecoveryPush|TestServerScenarioShutdownDuringInitialRuntimeScanDoesNotOpenControlListenerAfterRelease|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|TestServerScenarioShutdownWhileRefreshBindBlockedDoesNotAttachStaleListener)$' -timeout 240s ./internal/control"`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile all"`（修正后通过）
- `go test -tags testhooks ./internal/control`（目录：`frps/`，Windows，通过）

## 3. 双平台全量稳健性测试当前已重新收绿

截至 2026-04-24，当前代码树已经重新通过 Windows 与 WSL/Linux 双侧的稳定性矩阵、模块级 Go 回归和本轮要求的 Python 核心闭环 / 故障 e2e；本轮没有发现新的业务实现回退。

本次完成的双平台验证如下：

- `go run ./cmd/stabilitymatrix -profile all`（目录：`frps/`，Windows，通过）
- `go test ./...`（目录：`frps/`，Windows，通过）
- `go test -tags testhooks ./...`（目录：`frps/`，Windows，通过）
- `go test ./...`（目录：`frpc/`，Windows，通过）
- `go test -tags testhooks ./internal/control`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile all"`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./..."`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -tags testhooks ./..."`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frpc && go test ./..."`（通过）
- 检查 `.gitignore`，当前 `test/tmp/`、`__pycache__/` 等临时产物已被忽略；本轮无需新增忽略项

本次确认并收束的边界如下：

- 当前 Windows 与 WSL/Linux 下，`frps` 稳定性矩阵 `all` 已同时通过，`race`、多 `GOMAXPROCS`、配置抖动、soak、平台基线和资源压力 6 类入口本轮没有新增未收敛失败。
- 当前 Windows 与 WSL/Linux 下，`frps` 普通构建、`testhooks` 视角和 `frpc` 模块全量都通过；当前测试修正没有把服务端/客户端主线打坏。
- 当前 Windows 与 WSL/Linux 下，核心 Python e2e 和 4 个 TCP 故障场景都已通过；当前单个 `key` 登录模型、管理面 key 展示/轮换和 UDP idle cleanup 没有新增跨平台回退。
- 本轮优先排查代码缺陷后，实际收敛的两个阻塞点都属于测试层：一个是旧认证脚本未跟进当前 schema，一个是关闭语义断言过窄。
