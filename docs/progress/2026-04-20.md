# 2026-04-20 归档

## 1. `session.go` 已收口 session 错误辅助

截至 2026-04-20，第一轮“初步框架重构”当前子步骤又完成了一次连续收束：`frps/internal/control/server.go` 中剩余的 session 错误辅助已经全部迁到 `session.go`。

本次已完成的实现和收束如下：

- `(*Server).replyProtocolErrorWithSession` 已迁移到 `frps/internal/control/session.go`。
- `(*Server).replyErrorWithSession` 已迁移到 `frps/internal/control/session.go`。
- `(*Server).writeErrorWithSession` 已迁移到 `frps/internal/control/session.go`。
- `server.go` 本次只删除上述三个方法的原地实现；没有改任何调用点，没有改错误码、错误回写路径、session 写锁语义或日志路径。
- 至此，`session.go` 已集中承接 session request id、自身关闭信号、session 帧写入和 session 级错误回复辅助；`server.go` 继续只保留连接入口、登录入口、会话读循环以及尚未收出的配置相关逻辑。

本次执行的验证如下：

- `gofmt -w internal/control/server.go internal/control/session.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `frps/internal/control` 的 session 基础设施收束可视为当前小阶段已完成；下一条最小代码动作切到配置相关逻辑。
- 下一最小代码动作已压缩为单点：先只新建 `frps/internal/control/config.go` 并把 `(*Server).pushConfig` 迁移进去，暂不移动 `handleConfigAck` 和 `loadGroupRuntime`。

## 2. `config.go` 已收口配置下发、确认与 runtime 读取

截至 2026-04-20，第一轮“初步框架重构”当前子步骤又完成了一次连续四点收束：`frps/internal/control/server.go` 中的配置相关逻辑已经集中迁到新的 `config.go`。

本次已完成的实现和收束如下：

- 新建 `frps/internal/control/config.go`，集中承接配置快照下发、确认与 group runtime 读取逻辑。
- `(*Server).pushConfig` 已从 `frps/internal/control/server.go` 迁移到 `frps/internal/control/config.go`。
- `(*Server).handleConfigAck` 已从 `frps/internal/control/server.go` 迁移到 `frps/internal/control/config.go`。
- `(*Server).loadGroupRuntime` 已从 `frps/internal/control/server.go` 迁移到 `frps/internal/control/config.go`。
- `server.go` 本次只删除上述三个方法的原地实现并收紧 import；没有改任何调用点，没有改 `config.push` request id 分配、`config.ack` 校验路径、listener 启动时机、repository 读取路径或 timeout 边界。
- 至此，`config.go` 已集中承接配置快照加载、首次 `config.push` 和 `config.ack` 收口；`server.go` 继续保留连接入口、登录入口、session 读循环、基础传输读写和 challenge / 连接生命周期辅助。

本次执行的验证如下：

- `gofmt -w internal/control/server.go internal/control/config.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `frps/internal/control` 的配置相关逻辑收束可视为当前小阶段已完成；下一条最小代码动作切到 listener 展开与启动逻辑。
- 下一最小代码动作已压缩为单点：先只新建 `frps/internal/control/listeners.go`，并把 `ensureTunnelListeners` 与其直接依赖的 `closeStartedTunnelListeners` 迁移进去；暂不移动 `serveTunnelListener`、`serveUDPTunnelListener`、`handlePublicConnection` 和 `shutdownSession`。

## 3. `listeners.go` 已收口 listener 展开与失败回滚辅助

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 listener 收束：`frps/internal/control/data_plane.go` 中的 listener 展开与启动前失败回滚辅助已经迁到新的 `listeners.go`。

本次已完成的实现和收束如下：

- 新建 `frps/internal/control/listeners.go`，开始承接 tunnel listener 生命周期相关逻辑。
- `(*Server).ensureTunnelListeners` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。
- `closeStartedTunnelListeners` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。
- `data_plane.go` 本次只删除上述两个实现并收紧 import；没有改 tunnel 展开顺序、TCP/UDP listener 启动顺序、失败回滚路径、`listenersStarted` 防重入语义或 UDP idle cleanup 启动时机。
- 至此，`listeners.go` 已开始集中承接 listener 展开与启动前资源回滚；`data_plane.go` 继续保留具体 listener serve 循环、TCP stream 桥接、UDP 数据面和会话关闭收口。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/listeners.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `frps/internal/control` 的 listener 展开与启动前回滚辅助已完成第一段收束；下一条最小代码动作继续只处理 TCP listener accept 循环。
- 下一最小代码动作已压缩为单点：只把 `(*Server).serveTunnelListener` 迁移到 `frps/internal/control/listeners.go`，暂不移动 `serveUDPTunnelListener`、`handlePublicConnection`、`shutdownSession` 或 stream / udp 其他逻辑。

## 4. `listeners.go` 已收口 TCP listener accept 循环

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP listener 收束：`serveTunnelListener` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。

本次已完成的实现和收束如下：

- `(*Server).serveTunnelListener` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。
- `listeners.go` 现在同时承接 listener 展开、启动前失败回滚以及 TCP listener 的 accept 循环。
- `data_plane.go` 本次只删除 `serveTunnelListener` 的原地实现；没有改 accept 循环、`net.ErrClosed` 退出条件、TCP accept 失败日志路径或 `handlePublicConnection` 的启动时机。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/listeners.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `listeners.go` 的 TCP listener 侧边界已继续清晰化；下一条最小代码动作只处理 UDP listener 读循环。
- 下一最小代码动作已压缩为单点：只把 `(*Server).serveUDPTunnelListener` 迁移到 `frps/internal/control/listeners.go`，暂不移动 `handlePublicConnection`、`stream.*` 生命周期处理或 `shutdownSession`。

## 5. `listeners.go` 已收口 UDP listener 读循环

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 UDP listener 收束：`serveUDPTunnelListener` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。

本次已完成的实现和收束如下：

- `(*Server).serveUDPTunnelListener` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/listeners.go`。
- `listeners.go` 现在集中承接 tunnel listener 展开、失败回滚、TCP accept 循环和 UDP datagram 读循环。
- `data_plane.go` 本次只删除 `serveUDPTunnelListener` 的原地实现；没有改 UDP 读循环、payload 复制、`net.ErrClosed` 退出条件、`handlePublicUDPDatagram` 调用路径或日志路径。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/listeners.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `listeners.go` 已基本收住 listener 层的最小职责；下一条最小代码动作切到 TCP bridge 的连接打开流程。
- 下一最小代码动作已压缩为单点：新建 `frps/internal/control/tcp_bridge.go`，只把 `(*Server).handlePublicConnection` 迁移进去，暂不移动 `publicStream`、`stream.opened` / `stream.data` / `stream.close` 或 `shutdownSession`。

## 6. `tcp_bridge.go` 已收口 TCP 连接打开入口

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`handlePublicConnection` 已从 `frps/internal/control/data_plane.go` 迁移到新的 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- 新建 `frps/internal/control/tcp_bridge.go`，开始承接 TCP stream 生命周期相关逻辑。
- `(*Server).handlePublicConnection` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `data_plane.go` 本次只删除 `handlePublicConnection` 的原地实现并收紧 import；没有改 `stream.open` request id / stream id 分配、`stream.open` 帧内容、等待打开超时处理、`sendStreamClose` 调用路径或 `copyPublicToClient` 启动时机。
- 至此，`listeners.go` 负责 listener 展开与 serve 循环，`tcp_bridge.go` 开始负责 TCP stream 打开流程，`data_plane.go` 继续保留剩余 `stream.*` 生命周期处理、`publicStream` 结构、UDP 数据面和 session shutdown 收口。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 已完成第一刀收束；下一条最小代码动作只处理 `stream.opened` 的帧入口。
- 下一最小代码动作已压缩为单点：只把 `(*Server).handleStreamOpened` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `handleStreamData`、`handleStreamClose`、`sendStreamClose`、`copyPublicToClient`、`publicStream` 或 `shutdownSession`。

## 7. `tcp_bridge.go` 已收口 `stream.opened` 帧入口

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`handleStreamOpened` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*Server).handleStreamOpened` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在同时承接 TCP 连接打开入口和 `stream.opened` 帧处理。
- `data_plane.go` 本次只删除 `handleStreamOpened` 的原地实现；没有改 request id 校验、`stream.opened` 状态分支、`publicStream` 查找路径、`openRequestID` 校验或 ready 信号语义。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的打开确认入口已完成收束；下一条最小代码动作只处理 `stream.data` 的帧入口。
- 下一最小代码动作已压缩为单点：只把 `(*Server).handleStreamData` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `handleStreamClose`、`sendStreamClose`、`copyPublicToClient`、`publicStream` 或 `shutdownSession`。

## 8. `tcp_bridge.go` 已收口 `stream.data` 帧入口

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`handleStreamData` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*Server).handleStreamData` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在集中承接 TCP 连接打开入口、`stream.opened` 和 `stream.data` 帧处理。
- `data_plane.go` 本次只删除 `handleStreamData` 的原地实现；没有改 `stream.data` 的 request id / stream id 校验、`publicStream` 查找路径、public conn 写入失败后的 `closePublicStream` 判定或 `sendStreamClose` 调用条件。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的数据帧入口已完成收束；下一条最小代码动作只处理 `stream.close` 的被动关闭帧入口。
- 下一最小代码动作已压缩为单点：只把 `(*Server).handleStreamClose` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `sendStreamClose`、`copyPublicToClient`、`publicStream` 或 `shutdownSession`。

## 9. `tcp_bridge.go` 已收口 `stream.close` 被动关闭帧入口

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`handleStreamClose` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*Server).handleStreamClose` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在集中承接 TCP 连接打开、打开确认、数据帧和被动关闭帧处理。
- `data_plane.go` 本次只删除 `handleStreamClose` 的原地实现并收紧 import；没有改 `stream.close` 的 request id / stream id 校验、body 反序列化校验或 `closePublicStream` 调用路径。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的被动关闭入口已完成收束；下一条最小代码动作只处理主动发送 `stream.close` 的辅助。
- 下一最小代码动作已压缩为单点：只把 `(*Server).sendStreamClose` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `copyPublicToClient`、`publicStream` 或 `shutdownSession`。

## 10. `tcp_bridge.go` 已收口主动发送 `stream.close` 的辅助

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`sendStreamClose` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*Server).sendStreamClose` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在集中承接 TCP 连接打开、`stream.opened`、`stream.data`、`stream.close` 和主动发送关闭帧的辅助。
- `data_plane.go` 本次只删除 `sendStreamClose` 的原地实现；没有改 `stream.close` reason code、initiator、message 编码、session 写锁路径或任何调用点。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的主要 `stream.*` 入口已经基本集中；下一条最小代码动作只处理 public 侧到 client 侧的数据拷贝循环。
- 下一最小代码动作已压缩为单点：只把 `(*Server).copyPublicToClient` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `publicStream`、`shutdownSession` 或任何 `udp.*` 逻辑。

## 11. `tcp_bridge.go` 已收口 public 到 client 的数据拷贝循环

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge 收束：`copyPublicToClient` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*Server).copyPublicToClient` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在集中承接 TCP 连接打开、`stream.opened`、`stream.data`、`stream.close`、主动发送关闭帧以及 public 到 client 的数据拷贝循环。
- `data_plane.go` 本次只删除 `copyPublicToClient` 的原地实现并收紧 import；没有改 public conn 读循环、`stream.data` 写帧路径、读错误到 close reason 的映射或 `session.closePublicStream` 的调用时机。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的主要行为路径已经全部集中，下一条最小代码动作切到 stream runtime 结构本身的 ownership 收束。
- 下一最小代码动作已压缩为单点：只把 `publicStream` 结构体迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动其方法、session stream 辅助、`shutdownSession` 或任何 `udp.*` 逻辑。

## 12. `tcp_bridge.go` 已收口 `publicStream` 结构体定义

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge ownership 收束：`publicStream` 结构体已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `publicStream` 结构体定义已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在同时承接 TCP stream 生命周期入口和 stream runtime 的核心数据结构定义。
- `data_plane.go` 本次只删除 `publicStream` 结构体原地定义并收紧 import；没有改字段、初始化方式、ready / closeOnce 语义或任何引用点。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- TCP bridge 的 runtime 数据定义已开始回收到位；下一条最小代码动作只处理 `publicStream` 的 ready 信号辅助。
- 下一最小代码动作已压缩为单点：只把 `(*publicStream).signalReady` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `close`、session stream 辅助、`shutdownSession` 或任何 `udp.*` 逻辑。

## 13. `tcp_bridge.go` 已收口 `publicStream` 的 ready 信号辅助

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge runtime 收束：`(*publicStream).signalReady` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*publicStream).signalReady` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- `tcp_bridge.go` 现在同时承接 stream runtime 结构体和 ready 信号辅助，TCP stream 的打开确认语义开始在同一文件内闭合。
- `data_plane.go` 本次只删除 `signalReady` 的原地实现；没有改 `sync.Once` 保护、ready channel 写入次数或任何调用点。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `publicStream` 的 ready 行为已经随 runtime 结构一并收口；下一条最小代码动作只处理它的关闭辅助。
- 下一最小代码动作已压缩为单点：只把 `(*publicStream).close` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 session stream 辅助、`shutdownSession` 或任何 `udp.*` 逻辑。

## 14. `tcp_bridge.go` 已收口 `publicStream` 的关闭辅助

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小 TCP bridge runtime 收束：`(*publicStream).close` 已从 `frps/internal/control/data_plane.go` 迁移到 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `(*publicStream).close` 已从 `frps/internal/control/data_plane.go` 迁移到 `frps/internal/control/tcp_bridge.go`。
- 至此，`publicStream` 的结构体定义、ready 信号和连接关闭行为已在 `tcp_bridge.go` 内闭合；`data_plane.go` 不再承接 stream runtime 自身的方法实现。
- `data_plane.go` 本次只删除 `close` 的原地实现；没有改 `sync.Once` 保护、底层 public conn 关闭时机或任何调用点。

本次执行的验证如下：

- `gofmt -w internal/control/data_plane.go internal/control/tcp_bridge.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- `publicStream` 自身的结构和行为已经集中到 TCP bridge；下一条最小代码动作切到 session 侧的 stream id 分配辅助。
- 下一最小代码动作已压缩为单点：只把 `(*sessionState).nextTunnelStreamID` 迁移到 `frps/internal/control/tcp_bridge.go`，暂不移动 `addPublicStream`、`publicStream`、`closePublicStream`、`shutdownSession` 或任何 `udp.*` 逻辑。

## 15. `frps/internal/control` 已完成本轮三类联动收束

截至 2026-04-20，第一轮“初步框架重构”当前子步骤完成了一次三类联动收束：`data_plane.go` 已彻底退出，`server.go` 的鉴权 / group slot / connection ownership 已外提，`frps/internal/control` 的第一轮服务端骨架达到稳定提交边界。

本次已完成的实现和收束如下：

- 删除 `frps/internal/control/data_plane.go`；其中残留的 TCP/session runtime 辅助不再保留在单独“杂项数据面文件”里。
- `(*sessionState).nextTunnelStreamID`、`addPublicStream`、`publicStream`、`closePublicStream` 与 `(*Server).shutdownSession` 已收口到 `frps/internal/control/session.go`。
- `writeConnFull` 已收口到 `frps/internal/control/tcp_bridge.go`，其 ownership 与 TCP stream 写入路径保持一致。
- `tcpTunnelListener` 和 `udpTunnelListener` 已收口到 `frps/internal/control/listeners.go`。
- 新建 `frps/internal/control/auth.go`，集中承接 `(*Server).authenticate`、challenge 签发/消费、客户端来源 IP 判定、group slot 占用与释放。
- 新建 `frps/internal/control/connections.go`，集中承接控制连接注册/反注册，以及连接关闭原因与日志辅助。
- 新建 `frps/internal/control/sockaddr.go`，承接 TCP/UDP 共用的 `net.Addr -> protocol.SockAddr` 转换辅助。
- `frps/internal/control/server.go` 本次删除了登录握手、challenge、group slot、connection helper 等原地实现；当前只保留 accept/shutdown、连接入口、session read loop、心跳和底层帧读写辅助。
- `docs/frps/technical-design.md` 已同步当前 `internal/control` 的稳定文件 ownership。

本次执行的验证如下：

- `gofmt -w frps/internal/control/auth.go frps/internal/control/connections.go frps/internal/control/sockaddr.go frps/internal/control/session.go frps/internal/control/listeners.go frps/internal/control/tcp_bridge.go frps/internal/control/server.go frps/internal/control/udp.go`
- `go test ./internal/control`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- `frps/internal/control` 的第一轮服务端骨架可视为当前小阶段已完成，后续不再回到 `data_plane.go` 风格的职责集中实现。
- 下一条最小代码动作切换到 `frpc/internal/client`，先从登录握手 ownership 开始收束，让 `client.go` 不再同时承担登录、长会话、配置和桥接入口。

## 16. `frpc/internal/client` 已完成登录与 session 骨架四点收束

截至 2026-04-20，第一轮“初步框架重构”当前子步骤完成了一次连续四点收束：`frpc/internal/client/client.go` 中的登录握手和 session 基础设施已经拆到新的 `login.go` 与 `session.go`，客户端骨架开始形成稳定边界。

本次已完成的实现和收束如下：

- 新建 `frpc/internal/client/login.go`，集中承接 `(*Client).login`、`authResponse` 以及登录阶段直接使用的帧读取与头部校验辅助。
- 新建 `frpc/internal/client/session.go`，集中承接 `sessionState`、心跳间隔和读超时计算、request id 分配、配置快照读写。
- `(*Client).readLoop` 已从 `frpc/internal/client/client.go` 迁移到 `frpc/internal/client/session.go`。
- `(*Client).heartbeatLoop` 已从 `frpc/internal/client/client.go` 迁移到 `frpc/internal/client/session.go`。
- `frpc/internal/client/client.go` 本次删除了登录握手、session state、读循环和心跳循环的原地实现；当前只保留 `Run`、`runOnce`、`runSession`、`applyConfigPush`、通用帧读写和远端错误解码。
- 本次没有改 challenge 登录顺序、首次 `config.push` / `config.ack` 闭环、心跳 request id 语义、配置快照替换路径、TCP/UDP bridge 逻辑或 UDP 生命周期边界。

本次执行的验证如下：

- `gofmt -w internal/client/client.go internal/client/login.go internal/client/session.go`（`frpc/` 模块）
- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- `frpc/internal/client` 的登录与 session 基础设施已完成第一轮收束，`client.go` 不再继续回到“大文件混合登录与长会话细节”的实现方式。
- 下一条最小代码动作切到 target 解析 ownership：先只新建 `frpc/internal/client/targets.go`，把 `tunnelByID`、`localTarget`、`localUDPTarget` 以及它们直接依赖的 range 端口换算辅助迁移过去；本步暂不改 TCP/UDP open/data/close 主流程。

## 17. `targets.go` 已收口 frpc 的 target 解析 ownership

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小客户端收束：`frpc/internal/client` 中散落在 `streams.go` 和 `udp.go` 的 target 解析与 range 端口换算逻辑已经迁到新的 `targets.go`。

本次已完成的实现和收束如下：

- 新建 `frpc/internal/client/targets.go`，集中承接 `tunnelByID`、`localTarget`、`localUDPTarget` 和 TCP/UDP 共用的本地端口换算辅助。
- `streams.go` 本次只删除 tunnel 查找、本地 target 解析和 range 端口换算细节；`stream.open` / `stream.data` / `stream.close` 主流程未改。
- `udp.go` 本次只删除 tunnel 查找、本地 UDP target 解析和 range 端口换算细节；`udp.open` / `udp.data` / `udp.close` 主流程未改。
- `client_test.go` 新增了 TCP target 单端口与 range 映射测试，和现有 UDP target 测试一起固定当前解析边界。
- 本次没有改 wire protocol、配置快照结构、range 公式、TCP/UDP bridge 行为或 UDP 生命周期边界。

本次执行的验证如下：

- `gofmt -w internal/client/targets.go internal/client/streams.go internal/client/udp.go internal/client/client_test.go`（`frpc/` 模块）
- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- `targets.go` 现在是 `frpc/internal/client` 中唯一的 tunnel 查找和 local target 解析入口，后续不再把 range 端口换算散落回 TCP/UDP bridge 文件。
- 下一条最小代码动作切到文件 ownership 名称收束：先只把 `streams.go` 更名为 `tcp_bridge.go`，不改任何实现和调用关系。

## 18. `tcp_bridge.go` 已收口 TCP bridge 文件名

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小客户端收束：`frpc/internal/client/streams.go` 已更名为 `tcp_bridge.go`。

本次已完成的实现和收束如下：

- `frpc/internal/client/streams.go` 已更名为 `frpc/internal/client/tcp_bridge.go`。
- 本次只做 TCP bridge 文件名收束，没有改 `handleStreamOpen`、`handleStreamData`、`handleStreamClose`、`copyLocalToServer`、stream runtime 结构或任何调用关系。
- `internal/client` 当前文件名边界开始向蓝图中的 `tcp_bridge.go` / `udp_bridge.go` 形态对齐。

本次执行的验证如下：

- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- TCP bridge 的文件名已固定到 `tcp_bridge.go`，后续不再回到 `streams.go` 这种泛化命名。
- 下一条最小代码动作继续只处理 UDP bridge 文件名：把 `udp.go` 更名为 `udp_bridge.go`，不改任何实现和调用关系。

## 19. `udp_bridge.go` 已收口 UDP bridge 文件名

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小客户端收束：`frpc/internal/client/udp.go` 已更名为 `udp_bridge.go`。

本次已完成的实现和收束如下：

- `frpc/internal/client/udp.go` 已更名为 `frpc/internal/client/udp_bridge.go`。
- 本次只做 UDP bridge 文件名收束，没有改 `handleUDPOpen`、`handleUDPData`、`handleUDPClose`、`copyLocalUDPToServer`、UDP session runtime 或任何调用关系。
- `internal/client` 现在已同时具备 `tcp_bridge.go` 和 `udp_bridge.go` 两个明确的桥接 ownership 文件。

本次执行的验证如下：

- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- UDP bridge 的文件名已固定到 `udp_bridge.go`，后续不再回到 `udp.go` 这种泛化命名。
- 下一条最小代码动作继续只处理 runtime 文件名：把 `runtime.go` 更名为 `runtime_info.go`，不改任何实现和调用关系。

## 20. `runtime_info.go` 已收口 runtime 探测文件名，frpc 客户端骨架阶段完成

截至 2026-04-20，第一轮“初步框架重构”当前子步骤继续完成了一次最小客户端收束：`frpc/internal/client/runtime.go` 已更名为 `runtime_info.go`，且 `frpc/internal/client` 当前骨架已与蓝图中的稳定文件边界对齐。

本次已完成的实现和收束如下：

- `frpc/internal/client/runtime.go` 已更名为 `frpc/internal/client/runtime_info.go`。
- 本次只做 runtime 探测文件名收束，没有改 hostname、OS、架构探测逻辑或任何调用关系。
- 至此，`frpc/internal/client` 当前稳定文件布局已收口为 `client.go`、`login.go`、`session.go`、`targets.go`、`tcp_bridge.go`、`udp_bridge.go`、`runtime_info.go`。
- `frpc` 客户端骨架这一子阶段可以视为当前轮已完成；后续不再回到“桥接、target 解析和 runtime 信息混在泛化文件名里”的状态。

本次执行的验证如下：

- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- `frpc/internal/client` 的第一轮文件 ownership 已基本与蓝图一致，下一阶段不再优先继续拆客户端骨架，而是转入“最小共享规则与回归入口”的整理。
- 下一条最小代码动作切到仓库级回归入口说明：先只新建一个统一文档，收口当前 `go test` 与 Python e2e 的核心回归矩阵，不新增脚本、不改测试实现。

## 21. MySQL 地址简写兼容已删除

截至 2026-04-20，当前代码已删除 `frps` 对 MySQL 地址简写 DSN 的兼容重写，数据库启动链路现在只透传配置中的标准 MySQL DSN。

本次已完成的实现和收束如下：

- `frps/internal/app/database.go` 删除 `normalizeMySQLDSN`，`resolveDatabaseTarget` 不再把 `user:pass@host:3306/db` 之类地址简写改写为 `@tcp(...)`。
- `frps/internal/app/database.go` 新增最小 `resolveMySQLDataSource`，当前只负责去空白并透传标准 DSN。
- `frps/internal/app/database_test.go` 删除“简写 DSN 规范化”测试，改为验证标准 MySQL DSN 原样透传。
- `frps/README.md`、`docs/frps/project-overview.md`、`docs/frps/testing-debugging.md` 已同步去掉“简写兼容 / DSN 规范化”表述。

本次执行的验证如下：

- `gofmt -w internal/app/database.go internal/app/database_test.go`（`frps/` 模块）
- `go test ./internal/app`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- MySQL 数据源地址格式不再由 `frps` 做兼容修正；后续配置文档和测试都以驱动标准 DSN 为准。
- 当前轮的唯一下一步回到仓库级回归入口文档整理：先只新建统一回归命令矩阵文档，不新增脚本，不改产品代码。

## 22. 仓库级统一回归入口文档已收口

截至 2026-04-20，第一轮“初步框架重构”当前子步骤又完成了一次文档收束：仓库级最小回归入口已经集中到新的正式文档中，本轮后续先不新增自动化回归脚本。

本次已完成的实现和收束如下：

- 新建 `docs/regression-entry.md`，统一收口当前真实有效的 Go 定向测试、模块级 `go test ./...` 和 Python e2e 命令矩阵。
- `docs/regression-entry.md` 已固定当前核心闭环：`frps/internal/app`、`frps/internal/control`、`frpc/internal/client` 的定向 Go 回归，以及 TCP/UDP single、TCP/UDP range、UDP idle cleanup 和管理面最小闭环的 Python e2e。
- `docs/regression-entry.md` 同时把 TCP 单端口负向场景和 `test/e2e_tcp_perf.py` 收到扩展回归入口，明确它们不是每次最小步骤都必须阻塞执行的日常门槛。
- `docs/README.md` 已加入“仓库级回归入口”索引，并固定该文档只负责仓库级最小回归入口，不替代子项目测试文档的细节说明。
- 本次没有新增任何自动化脚本，也没有改现有测试实现或产品代码；当前判断是命令矩阵已经足够支撑这一轮日常回归。

本次执行的验证如下：

- 交叉核对 `docs/frps/testing-debugging.md` 与 `docs/frpc/testing-debugging.md` 中现有回归命令
- 交叉核对 `test/e2e_tcp_single.py`、`test/e2e_tcp_range.py`、`test/e2e_udp_single.py`、`test/e2e_udp_range.py`、`test/e2e_management_webui.py`、`test/e2e_tcp_perf.py` 的实际参数入口
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- 当前仓库级回归入口先以 `docs/regression-entry.md` 为统一入口，不在这一轮引入额外脚本层。
- 下一条最小动作切到“最小共享规则”判断：先只回看 `frps` / `frpc` 内是否存在第二处同时满足“明确重复”和“已经稳定”的纯规则；只有同时满足这两个条件，才考虑抽成极小共享 helper。

## 23. 初步框架重构已完成总收束

截至 2026-04-20，第一轮“初步框架重构”已经完成总收束：剩余的最小共享规则判断、核心回归、正式文档同步和当前轮清尾都已结束，本轮不再继续追加未完成项。

本次已完成的实现和收束如下：

- 回看 `frps` / `frpc` 当前剩余纯规则候选后，最终确认只有 challenge 响应摘要同时满足“明确重复、已经稳定、抽完更简单”三条条件。
- `frps/pkg/protocol/protocol.go` 新增 `ChallengeResponse`，统一承接 `sha256(token_hash + challenge_nonce)` 这条跨端共享纯规则。
- `frps/internal/control/auth.go` 与 `frpc/internal/client/login.go` 已改为复用 `protocol.ChallengeResponse`；没有改 wire protocol、登录时序、token 语义或常量时间比较路径。
- `frps/pkg/protocol/protocol_test.go` 新增固定向量测试；`frps/internal/control/server_test.go` 与 `frpc/internal/client/client_test.go` 已切到共享 helper。
- `docs/initial-architecture-refactor-blueprint.md`、`docs/frps-frpc-current-architecture.md`、`docs/frps/technical-design.md`、`docs/frpc/technical-design.md` 已同步当前稳定边界和最终共享规则口径。
- `docs/tmp/todo.md` 已清空，等待下一轮重新定义总目标、子步骤和唯一下一步。

本次执行的验证如下：

- `gofmt -w frps/pkg/protocol/protocol.go frps/pkg/protocol/protocol_test.go frps/internal/control/auth.go frps/internal/control/server_test.go frpc/internal/client/login.go frpc/internal/client/client_test.go`
- `go test ./...`（`frps/` 模块）
- `go test ./...`（`frpc/` 模块）
- `python test/e2e_tcp_single.py --scenario happy_path`
- `python test/e2e_tcp_range.py`
- `python test/e2e_udp_single.py --scenario happy_path`
- `python test/e2e_udp_single.py --scenario idle_cleanup`
- `python test/e2e_udp_range.py`
- `python test/e2e_management_webui.py`
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- 第一轮“初步框架重构”已完成；后续工作必须开启新一轮总目标，不再把未完成项继续堆回本轮 `todo`。
- 除 `protocol.ChallengeResponse` 外，本轮不再新增其他共享 helper；其余纯规则继续保持服务端 / 客户端局部实现。
- 当前仓库级最小回归入口继续以 `docs/regression-entry.md` 为准；后续新轮次先在新 `todo` 中重新定义唯一下一步，再恢复执行。

## 24. 正式文档已按当前实现口径同步

截至 2026-04-20，当前仓库的正式文档已经按实际代码实现重新同步，不再把未来规划写成当前已实现状态。

本次已完成的文档收束如下：

- 重写 `docs/project-overview.md`，把仓库级能力收束到当前真实范围：管理认证、分组/隧道最小管理、TCP/UDP 单端口与连续范围正向代理、`frps` 侧 UDP idle cleanup、SQLite/MySQL 双支持。
- 重写 `docs/frps/project-overview.md`、`docs/frps/technical-design.md`、`docs/frps/functional-spec.md`，把 `frps` 的当前职责、模块边界、API、schema 消费范围、控制面和数据面语义统一到当前代码口径。
- 重写 `docs/frps/development-guide.md`、`docs/frps/testing-debugging.md`，把开发入口、运行方式、当前可执行测试和常见排查路径收束到当前真实命令矩阵，不再混入反向代理、WebSocket、连接注册表、限速或抓包的现状描述。
- 重写 `docs/frpc/project-overview.md`、`docs/frpc/technical-design.md`、`docs/frpc/functional-spec.md`，把 `frpc` 的极简边界、登录/会话/桥接实现、范围映射和 UDP 生命周期约定同步到当前代码。
- 重写 `docs/frpc/development-guide.md`、`docs/frpc/testing-debugging.md`，把客户端的开发入口、调试重点和当前自动化验证范围收束到现有实现。
- 这次文档同步明确固定了几条当前边界：
  - 反向代理仍未实现
  - 在线热更新推送仍未实现
  - 隧道入口 ACL 仍未执行
  - WebSocket、连接注册表、实时速率、限速、抓包仍未进入真实运行态
  - `group_tunnel_ip_rules`、`rate_limit`、`capture_enabled` 不能再在正式文档里被表述为“已支持”

本次执行的验证如下：

- 逐项对照当前代码实现核对 `frps` / `frpc` 正式文档口径
- `git diff --check`
- 再次检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- 后续正式文档继续以“当前真实实现”和“明确未实现边界”两套口径书写，不再把长期规划段落混写成现状。
- `docs/tmp/todo.md` 继续保持为空，等待下一轮总目标重新定义后再写入新的未完成项。

## 25. 前端重构这一轮的总目标与子步骤已建立

截至 2026-04-20，前端重构这一轮的执行基线已经建立完成：`docs/tmp/todo.md` 已重新写入面向 `frps/webui` 的当前总目标、子步骤、当前轮边界和唯一下一步。

本次已固定下来的执行约束如下：

- 本轮性质已明确为“前端信息架构与管理面板重构”，不是后端功能轮；当前后端 API、认证语义、路由路径和现有 CRUD 能力都不在本轮改动范围内。
- 本轮总目标已固定为：在保持真实能力边界不变的前提下，把 `frps/webui` 重构为更现代化、更清晰的管理面板布局，并统一视觉系统与桌面/移动端体验。
- `todo` 已把当前子步骤拆成七段：信息架构与版式基线、视觉系统与全局样式、概览页重构、分组管理页重构、隧道管理页重构、登录与初始化页重构、收束与验收。
- 当前正在推进的子步骤已收紧到“信息架构与版式基线”，并继续拆成页面盘点、全局 shell、页面骨架和布局组件边界四个细分点。
- 当前唯一下一步已固定为单点：先完成新版管理面板的全局 shell 与页面骨架方案，明确侧边导航、顶部状态区、主内容区和通用操作区的结构约定。
- 本次只建立计划，不改动任何前端代码实现。

本次执行的检查如下：

- 重读 `docs/workflow.md`，确认当前轮 `todo` 仍需包含总目标、子步骤、当前轮边界、当前细分拆分和唯一下一步。
- 盘点当前 `frps/webui` 的现状：当前技术栈为 Vue 3 + Pinia + Vue Router + Element Plus，现有页面为 Dashboard / Proxy Groups / Tunnels / Init / Login。
- 检查 `.gitignore`，本次无需新增忽略项。

本次固定下来的后续边界如下：

- 下一条最小动作直接进入新版管理面板的全局 shell 与页面骨架方案，不在这一步提前改视觉细节或具体页面代码。
- 后续每次前端子步骤完成后，继续按当前工作流立即归档、复查 `.gitignore` 并提交稳定状态。

## 26. Todo 工作流已取消“子步骤细分拆分”的强制要求

截至 2026-04-20，当前仓库的 `todo` 工作流已经按最新约束收紧调整：后续 `docs/tmp/todo.md` 仍然必须保留总目标、子步骤、当前轮边界和唯一下一步，但不再强制维护“当前正在推进子步骤的更细子任务拆分”。

本次已完成的规则调整如下：

- `docs/workflow.md` 已删除“`todo` 必须包含更细子任务拆分”的强制要求。
- `docs/workflow.md` 已把当前唯一下一步的要求调整为“保持单一明确，不并列堆叠多个动作”，不再要求先继续拆成更细子任务。
- `docs/workflow.md` 的固定执行步骤已同步改写：后续只要求把当前唯一下一步收紧为一个清晰动作，不再要求额外写细分层。
- `docs/tmp/todo.md` 已同步移除“当前正在推进的子步骤”这一段，只保留当前总目标、子步骤、当前轮边界和当前唯一下一步。
- 当前前端重构这一轮的 `todo` 已按新规则收束，后续继续直接围绕顶层子步骤推进。

本次执行的检查如下：

- 复查 `docs/workflow.md` 与 `docs/tmp/todo.md`，确认两处口径一致。
- 检查 `.gitignore`，本次无需新增忽略项。

本次固定下来的后续边界如下：

- 后续新轮和当前轮都不再恢复“细分 1.1 / 1.2 / 1.3”这类强制模板，除非未来再次显式调整工作流。
- `todo` 仍然必须保持“唯一下一步”这一约束，避免把多个并列动作堆进同一条待办。

## 27. 前端管理面板的全局 shell 与页面骨架方案已收口

截至 2026-04-20，前端重构这一轮当前唯一下一步已经完成第一项正式产出：新版管理面板的全局 shell、导航结构、顶部状态区、主内容区和通用页面骨架方案已经固定到正式文档。

本次已完成的实现和收束如下：

- 新建 `docs/frps/webui-redesign-plan.md`，集中定义当前这一轮 `frps/webui` 重构的壳层信息架构。
- 文档已固定新版管理壳层的核心结构：`shell-sidebar`、`shell-topbar`、`shell-alert-slot`、`shell-content` 和统一 `page-shell`。
- 文档已固定当前真实导航范围只包含概览、分组管理、隧道管理三类管理态页面，不新增任何伪入口或未来功能导航。
- 文档已固定通用页面骨架：`page-header`、`page-actions`、`page-feedback`、`page-metrics`、`page-body`，并分别说明了 Dashboard / Proxy Groups / Tunnels 三张页面的承载重点。
- 文档已固定认证页继续独立于管理壳层、但复用同一套视觉语言的边界。
- `docs/README.md` 已加入该方案文档索引，并顺手修正了其中仍残留的旧 `todo` 细分规则表述，使其与当前 `docs/workflow.md` 一致。

本次执行的检查如下：

- 对照当前 `frps/webui` 路由、`AppShellView.vue`、`DashboardView.vue`、`ProxyGroupsView.vue`、`TunnelsView.vue` 和现有全局样式，确认方案没有越过当前真实能力边界。
- 复查 `docs/workflow.md`、`docs/README.md` 和 `docs/tmp/todo.md` 的 `todo` 口径，确保“总目标 / 子步骤 / 当前轮边界 / 唯一下一步”一致。
- 检查 `.gitignore`，本次无需新增忽略项。

本次固定下来的后续边界如下：

- 后续实现阶段先只把新版管理壳层骨架落到 `AppShellView.vue` 和路由元数据，不在这一条动作里提前重写三张业务页内部结构。
- 当前页面可见能力继续严格限定在概览、分组和隧道管理；视觉改造不能扩张成功能承诺。

## 28. 前端管理面板新版壳层骨架已落地

截至 2026-04-20，前端重构这一轮当前唯一下一步已经完成第一段实现收束：新版管理面板的管理态壳层已经正式落到 `frps/webui` 代码中。

本次已完成的实现和收束如下：

- `frps/webui/src/views/AppShellView.vue` 已重构为新版管理壳层，当前结构固定为侧边导航、顶部状态区、全局告警槽和主内容容器。
- 侧边导航不再在模板里手写散落链接，当前已改为从管理态路由元数据动态读取导航项，并使用当前路由高亮。
- 顶部状态区已统一承接页面眉标、标题、说明、认证状态、会话到期信息以及“刷新状态 / 退出登录”全局动作。
- `frps/webui/src/router/index.ts` 已为三张管理页补齐 `eyebrow`、`description`、`navLabel`、`navOrder`、`managementNav` 元数据，壳层页面身份信息现在统一由路由驱动。
- `frps/webui/src/styles/main.css` 已补齐新版壳层所需的基础 design tokens、响应式侧边栏、顶部状态卡片和内容容器样式，并保留现有三张业务页内部结构继续运行在新壳层中。
- 当前移动端已具备基础侧边栏抽屉形态；桌面端为固定侧边栏 + 主内容区布局。
- 本次没有改后端 API、认证语义、管理路由路径，也没有提前重写 `DashboardView.vue`、`ProxyGroupsView.vue`、`TunnelsView.vue` 内部 CRUD 结构。

本次执行的验证如下：

- `npm run build`（`frps/webui/`）
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- 新版管理壳层已经成为后续页面重构的稳定外框，后续页面改造不再回到旧的顶栏胶囊导航结构。
- 下一条最小动作切到首页内容重构：先只改 `DashboardView.vue`，让它对齐新的通用页面骨架和文案口径，暂不动分组页、隧道页和认证页。

## 29. 管理概览页已改造成真实仪表盘

截至 2026-04-20，前端重构这一轮当前唯一下一步已经继续完成一段实现收束：`DashboardView.vue` 已按新版管理壳层重构为真实的管理仪表盘，不再保留旧的阶段说明和“下一步工作”叙事。

本次已完成的实现和收束如下：

- `frps/webui/src/views/DashboardView.vue` 已接入现有认证状态、分组列表和隧道列表的真实读取结果，首页现在会展示管理密钥状态、管理会话状态、分组数量和隧道数量。
- 首页结构已按通用页面骨架改造为 `page-header`、`page-actions`、`page-feedback`、`page-metrics` 和 `page-body`，并把内容收口为“当前能力概览”“当前配置画像”“快捷入口”“风险与边界”四块。
- 旧首页中的 `Phase` 文案、过时阶段描述和“下一步工作”面板已全部删除；首页现在只表达当前真实已接通能力与运行边界。
- `frps/webui/src/styles/main.css` 已补齐首页与后续页面可复用的骨架样式，包括页面头部、统计卡片、主区/侧栏布局、概览列表和快捷入口卡片样式，并补齐对应响应式规则。
- 本次仍然没有改后端 API、认证语义、管理路由路径、分组 CRUD、隧道 CRUD 或认证页结构；这一步只完成首页仪表盘重构。

本次执行的验证如下：

- `npm run build`（`frps/webui/`）
- `git diff --check`
- 检查 `.gitignore`，本次无需新增忽略项

本次固定下来的后续边界如下：

- 首页现在已经具备稳定的新版页面骨架，后续分组页、隧道页和认证页重构都应复用这套骨架语言，不再回到旧 hero + phase 叙事结构。
- 下一条最小动作切到第 2 步：先只重构 `ProxyGroupsView.vue`，对齐新的页面骨架、统计区、操作区和 token 展示流程，暂不改 `TunnelsView.vue`、`InitSecretView.vue` 和 `LoginView.vue`。
