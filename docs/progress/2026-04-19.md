# 2026-04-19 归档

## 1. WebUI 重构与管理认证闭环

WebUI 重构这一轮已经完成收口。

当前已确认落地的能力：

- `frps` 管理面已切换为独立的 `Vue 3 + Vite + TypeScript + Element Plus` 前端工程。
- 管理认证已固定为本地 `auth.json` 单一管理密钥方案，不再引入数据库 `admins` 表或管理员账号密码体系。
- 浏览器初始化和登录已固定为：
  - 初始化时提交 `sha256(secret)`
  - 登录时先申请一次性 challenge
  - 再提交 `sha256(key_hash + salt)`
- 管理 API 已全部置于真实管理会话门禁后。
- 分组管理已支持列表、新增、编辑、删除、token 重置。
- 隧道管理已支持列表、新增、编辑、删除。
- `frps` 已固定从当前工作目录下的 `data/config.json` 读取配置，并从配置中的 `webui.dist_dir` 直接托管构建产物。
- 直接启动 `frps.exe` 已不再依赖启动参数。

这一轮新增了真实联调脚本：

- `test/e2e_management_webui.py`

脚本固定验证以下闭环：

- 在隔离工作目录中写入固定 `data/config.json`
- 直接启动真实 `frps.exe`
- 托管真实 `webui/dist`
- 初始化管理密钥
- challenge 登录与会话恢复
- `proxy_groups` CRUD
- token 重置
- `tunnels` CRUD
- 登出与受保护接口拒绝

本轮已实际执行：

```powershell
python test/e2e_management_webui.py
```

当前结果是：

- 构建 `frps.exe` 和 `frps/webui/dist` 成功
- 直接启动 `frps.exe` 成功读取固定 `data/config.json`
- 管理认证与管理 CRUD 真实闭环通过
- 结果产物会落到 `test/tmp/management-e2e-<timestamp>/`

## 2. 管理密钥模型收束

这一轮后续又把管理密钥模型收束为更窄的边界：

- 现阶段不再提供管理密钥在线轮换接口。
- 管理密钥只在未初始化态设置一次。
- 重置路径固定为删除本地 `auth.json`。
- 服务端会监听 `auth.json` 删除并自动清空内存中的管理认证状态。
- 删除后旧 challenge 和旧管理会话立即失效。
- 前端说明统一改为“删除 `auth.json` 重置”。
- `test/e2e_management_webui.py` 已扩展覆盖删除 `auth.json` 后自动重置与重新初始化。

## 3. UDP 最小闭环归档

截至 2026-04-19，UDP 最小闭环这一轮已经完成并收口。

本轮归档结论：

- 管理面 `proxy_groups` / `tunnels` CRUD 已完成，隧道模型已支持 `protocol = tcp | udp`
- 协议文档已定义 UDP 会话时序、`sessionId` 语义和 `idleTimeoutMs` 字段
- UDP 生命周期边界已固定：`frps` 统一做空闲 `30s` 清理，`frpc` 不做本地 idle timeout 判断，只执行来自 `frps` 的 `udp.close`
- `frps/pkg/protocol` 已补齐 `udp.open` / `udp.close` 消息体与编解码，并补了 round-trip 单测
- `frps` / `frpc` 控制连接读循环已接入 `udp.*` 分支，UDP session 状态骨架已预留
- UDP 控制面未知会话处理已从默认 `unexpected message type` 收敛为会话级 `udp.close`
- 已补最小 UDP 控制面回归测试，覆盖 `frps/internal/control` 与 `frpc/internal/client` 的基础分支
- `frps` 已在 `config.ack` 后为启用的单端口 UDP tunnel 启动真实公网 UDP listener，并在控制会话结束时统一回收 listener
- `frps` 已按 `tunnelId + 公网客户端地址` 维护 UDP session；首个公网 datagram 会分配 `sessionId` 并顺序下发 `udp.open` / `udp.data`
- 同一公网客户端后续 datagram 会复用既有 `sessionId`；`frps` 也已支持把来自 `frpc` 的 `udp.data` 按 `sessionId` 回写给对应公网客户端
- `frps` 已在公网收包和 `frpc` 回包路径刷新 UDP session 活跃时间，并固定向 `frpc` 下发 `30s` idle timeout 元数据
- 已新增 `frps/internal/control/server_test.go` 的 UDP listener/session 回归测试，覆盖首包建会话、同客户端 session 复用和公网回包写出；`go test ./...` 已在 `frps/` 模块通过
- `frpc` 已按 `sessionId` 建立真实本地 `UDPConn`，把来自 `frps` 的 `udp.data` 写入本地 UDP 服务，并把本地 UDP 回包按原 `sessionId` 回发给 `frps`
- `frpc` 当前只在收到 `udp.close`、本地不可恢复错误或控制会话结束时释放本地 UDP session，不做本地 idle timer；已新增 `frpc/internal/client/client_test.go` 的 UDP happy path 回归测试，`go test ./...` 已在 `frpc/` 模块通过
- `frps` 已补上 UDP session 级 `30s` idle cleanup：在 UDP tunnel listener 启动后后台定期扫描空闲会话，超时后删除本地 session，并向 `frpc` 发送 `udp.close`
- 已新增 `frps/internal/control/server_test.go` 的 UDP idle cleanup 回归测试，覆盖真实 UDP listener 建会话、空闲超时清理和向 `frpc` 下发 `udp.close`；`go test ./internal/control` 与 `go test ./...` 已在 `frps/` 模块通过
- 已新增 `test/e2e_udp_single.py`，在隔离工作目录中直启真实 `frps`、seed 单端口 UDP tunnel，并分别用 Python UDP 客户端和 Python UDP 服务器跑通最小 happy path；`python test/e2e_udp_single.py` 已于 2026-04-19 本地通过
- `test/e2e_udp_single.py` 已扩展 `idle_cleanup` 场景：同一公网 UDP 客户端在一次 datagram round-trip 后空闲约 `30s`，`frps` 会记录 idle cleanup 并向 `frpc` 发送 `udp.close`，`frpc` 会记录 `udp session closed`，随后同一公网客户端再次发包可重建新 UDP session 并重新打通闭环；`python test/e2e_udp_single.py --scenario idle_cleanup` 已于 2026-04-19 本地通过

至此，单端口 UDP 隧道这一轮的真实公网入口、本地 UDP 转发、回包、`frps` `30s` idle cleanup、`frpc` 收到清理通知后的收口，以及最小 Python e2e 验证已全部闭环。

## 4. TCP/UDP 范围隧道执行约定收束

截至 2026-04-19，TCP/UDP 范围隧道这一轮的第一个子步骤已经完成；本次先收束长期有效的执行约定，还没有进入全部代码实现。

本次固定下来的边界如下：

- 这一轮只做连续、跨度一致的一一对应范围映射，不做稀疏映射。
- TCP 和 UDP 共用同一套端口偏移公式：

```text
offset = remotePort - remoteStart
localPort = localStart + offset
```

- `frps` 在运行态可以把一个 range tunnel 展开成逐端口公网 listener，但协议仍只保留同一个 wire `tunnelId`。
- 因此，`stream.open.remotePort` 和 `udp.open.remotePort` 的语义已固定为“本次实际命中的公网端口”，不能在 range 模式下退化为固定写 `remoteStart`。
- `frpc` 目标端口计算只依据当前配置快照和消息中的 `remotePort`，不依赖 listener 创建顺序、配置顺序或额外的子 tunnel 标识。
- UDP range 的 session 键已固定为 `tunnelId + remotePort + public client addr`；单端口 UDP 只是 `remotePort` 固定不变的特例。
- UDP 范围模式下仍保持当前生命周期模型不变：`frps` 是唯一 session 裁决方，活动发生后立即刷新最后活跃时间，空闲约 `30s` 后清理，`frpc` 只响应 `udp.close`，不做本地 idle timeout。
- 这些约定已同步到正式文档：
  - `docs/protocol.md`
  - `docs/project-overview.md`
  - `docs/frps/technical-design.md`
  - `docs/frpc/technical-design.md`

## 5. TCP range 的 frps 入口已打通

截至 2026-04-19，TCP/UDP 端口范围映射这一轮又完成了一个已归档子步骤：`frps` 侧 TCP range 入口已经进入真实执行链路。

本次已完成的实现和收束如下：

- `frps/internal/control` 的运行时 listener 容器已改为支持“一个 tunnel 挂多个 listener”，不再假设一个 tunnel 只能对应一个公网 listener。
- `frps` 在收到 `config.ack` 后，已经会为启用状态的 TCP range tunnel 按 `remoteStart..remoteEnd` 逐端口展开真实公网 TCP listener。
- TCP range listener 命中后，`frps` 下发的 `stream.open.remotePort` 已改为实际命中的公网端口，不再固定写 `remoteStart`。
- 当前 TCP range 仍然只使用原有同一个 wire `tunnelId`；`frpc` 继续按 `remotePort` 偏移计算本地目标端口。
- UDP range 运行态本次没有放开，当前仍保持“只支持 UDP 单端口 listener，UDP range 留到下一子步骤实现”的边界。

本次新增和更新的验证如下：

- `frps/internal/control/server_test.go` 已新增/更新覆盖：
  - 启用状态的 TCP range tunnel 会真正启动非 `remoteStart` 端口上的 listener
  - 命中 range 内实际公网端口后，`stream.open.remotePort` 会回填该实际端口
  - 既有单端口 TCP 监听行为仍保持可用
- 已执行：
  - `go test ./internal/control`
  - `go test ./...`（`frps/` 模块）

本次还同步了当前实现状态文档，避免继续按“只有单端口 TCP 数据面”理解代码现状：

- `docs/frps-frpc-current-architecture.md`
- `docs/frps/technical-design.md`

## 6. TCP range 最小 Python e2e 已归档

截至 2026-04-19，TCP/UDP 端口范围映射这一轮又完成了一个已归档子步骤：TCP range 的最小 Python e2e 已经收口。

本次已完成的实现和收束如下：

- 新增 `test/e2e_tcp_range.py`，在隔离工作目录中启动真实 `frps` / `frpc`，分别模拟公网客户端和内网 TCP 服务。
- `test/e2e_tcp_range.py` 已固定按当前产品边界启动 `frps`：使用 `workspace/data/config.json`、隔离 `data/auth.json` / SQLite，并复制 `webui/dist`，不再依赖旧的 `--config` 启动方式。
- 脚本会 seed 一个启用状态的 TCP single tunnel 和一个启用状态的 TCP range tunnel。
- 脚本会分别启动 1 个 single 本地 TCP 服务和 2 个 range 本地 TCP 服务；3 个本地服务使用不同响应前缀回包，因此可以直接验证实际命中的 `localPort`。
- 已验证 single remote port 仍会命中 single local port，确认 TCP 单端口最小链路没有因 range 改动回退。
- 已验证同一个 TCP range tunnel 命中两个不同的 `remotePort` 时，`frpc` 会按 `offset = remotePort - remoteStart` 转发到对应的两个 `localPort`。
- `test/e2e_tcp_single.py` 也已同步到当前 `frps` 直接从 `data/config.json` 启动的模型，并重新验证 `happy_path`，避免旧单端口脚本继续停留在失效启动方式上。

本次执行的验证如下：

- `python test/e2e_tcp_range.py`
- `python test/e2e_tcp_single.py --scenario happy_path`

本子步骤完成后，TCP range 当前已完成的闭环为：

- `frps` 在 `config.ack` 后按范围展开真实公网 TCP listener
- `stream.open.remotePort` 回填实际命中的公网端口
- `frpc` 按 `remotePort` 偏移计算目标 `localPort`
- 最小 Python e2e 已验证 range 映射和 single regression

当前仍未归档为“本轮总目标完成”的内容：

- UDP range 执行链路
- UDP range 的最小单测与 Python e2e
- TCP/UDP range 的总回归与本轮清尾

## 7. UDP range 的 frps 入口已打通

截至 2026-04-19，TCP/UDP 端口范围映射这一轮又完成了一个已归档子步骤：`frps` 侧 UDP range 入口已经进入真实执行链路。

本次已完成的实现和收束如下：

- `frps/internal/control` 已不再把 UDP range tunnel 排除在公网 listener 启动逻辑之外；收到 `config.ack` 后，会按 `remoteStart..remoteEnd` 为启用状态的 UDP range tunnel 逐端口展开真实公网 UDP listener。
- 单端口 UDP 不再走单独分支；当前 UDP 单端口 / range 共用同一套 listener 展开路径，单端口只是范围长度为 `1` 的特例。
- `frps` 的 UDP session key 已落实为 `tunnelId + remotePort + public client addr`，不再把同一公网客户端命中同一 range tunnel 的不同 remote 端口错误复用到一个 session。
- 首个公网 datagram 命中某个 UDP range listener 后，`udp.open.remotePort` 已回填本次实际命中的公网端口，不再固定写 `remoteStart`。
- 当前这一子步骤只收口 `frps` 入口和控制层最小验证，不包含 `frpc` UDP range 本地端口映射与 Python e2e；因此本轮总目标仍未完成。

本次新增和更新的验证如下：

- `frps/internal/control/server_test.go` 已新增/更新覆盖：
  - 启用状态的 UDP range tunnel 会真正启动非 `remoteStart` 端口上的 listener
  - `udp.open.remotePort` 会回填实际命中的公网端口
  - 同一公网客户端命中同一 UDP range tunnel 的不同 `remotePort` 时，会分配两个不同 `sessionId`
  - 既有单端口 UDP 监听行为仍保持可用
- 已执行：
  - `go test ./internal/control`（`frps/` 模块）

本次还同步了当前实现状态文档，避免继续按“UDP range 仍未进入真实运行态”理解代码现状：

- `docs/frps-frpc-current-architecture.md`
- `docs/project-overview.md`

## 8. UDP range 的 frpc 映射与最小单测已打通

截至 2026-04-19，TCP/UDP 端口范围映射这一轮又完成了一个已归档子步骤：`frpc` 侧 UDP range 本地端口映射和最小单测已经收口。

本次已完成的实现和收束如下：

- `frpc/internal/client/udp.go` 已不再拒绝带 `TunnelFlagRange` 的 UDP tunnel；`frpc` 现在会像 TCP 一样，按 `offset = remotePort - remoteStart` 计算 `localPort = localStart + offset`。
- `frpc` 对 UDP range 仍然沿用当前既有生命周期边界：只依据 `udp.open.remotePort` 和当前配置快照决定本地目标，不自行做本地 idle timeout，仍只在收到 `udp.close`、发生本地不可恢复错误，或控制连接结束时释放 session。
- `frpc/internal/client/client_test.go` 已新增最小单测，直接覆盖 UDP 单端口和 range 的目标地址换算，确认单端口映射没有回退。
- `frpc/internal/client/client_test.go` 已新增 range 转发回归，覆盖 range 模式下 `udp.open.remotePort -> localPort` 的实际映射，以及 `udp.close` 收口继续成立。

本次执行的验证如下：

- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）

本子步骤完成后，本轮仍未归档为“总目标完成”的内容是：

- UDP range 的最小 Python e2e
- TCP/UDP range 的总回归与本轮清尾

## 9. UDP range 最小 Python e2e 已归档

截至 2026-04-19，TCP/UDP 端口范围映射这一轮又完成了一个已归档子步骤：UDP range 的最小 Python e2e 已经收口。

本次已完成的实现和收束如下：

- 新增 `test/e2e_udp_range.py`，在隔离工作目录中启动真实 `frps` / `frpc`，分别模拟公网 UDP 客户端和内网 UDP 服务。
- 脚本会 seed 一个启用状态的 UDP single tunnel 和一个启用状态的 UDP range tunnel。
- 脚本会分别启动 1 个 single 本地 UDP 服务和 2 个 range 本地 UDP 服务；3 个本地服务使用不同响应前缀回包，因此可以直接验证实际命中的 `localPort`。
- 已验证 single remote port 仍会命中 single local port，确认 UDP 单端口最小链路没有因 range 改动回退。
- 已验证同一个 UDP range tunnel 命中两个不同的 `remotePort` 时，`frpc` 会按 `offset = remotePort - remoteStart` 转发到对应的两个 `localPort`。
- 脚本同时校验了当前稳定日志语义：`frps` 会记录实际启动的 `remote_port`，`frpc` 会记录实际命中的本地 `target`，避免回退为“range 监听存在但目标端口换算错误”。

本次执行的验证如下：

- `python test/e2e_udp_range.py`

本子步骤完成后，本轮仍未归档为“总目标完成”的内容只剩：

- TCP/UDP 单端口与 range 的总回归
- 本轮清尾、归档收口与清空 `todo`

## 10. TCP/UDP 范围映射这一轮总回归与收口已归档

截至 2026-04-19，TCP/UDP 端口范围映射这一轮的总回归与清尾已经完成，本轮总目标正式收口。

本次最终总回归实际执行如下：

- `python test/e2e_tcp_single.py --scenario happy_path`
- `python test/e2e_tcp_range.py`
- `python test/e2e_udp_single.py --scenario happy_path`
- `python test/e2e_udp_single.py --scenario idle_cleanup`
- `python test/e2e_udp_range.py`

本次总回归确认结果如下：

- TCP single 最小链路继续可用，没有因 TCP/UDP range 改动回退。
- TCP range 继续满足连续区间一一映射：`remotePort -> localPort` 仍按固定偏移公式换算。
- UDP single happy path 继续可用。
- UDP idle cleanup 继续满足当前稳定边界：一旦有成功收发，最后活跃时间立即刷新；随后从最后一次活动结束开始计时，空闲约 `30s` 后由 `frps` 清理 session 并通知 `frpc` 执行 `udp.close` 收口，而不是固定 `30s` 轮询一次才决定是否关闭。
- UDP range 继续满足连续区间一一映射，且没有破坏 UDP single 的现有行为。
- 本轮总回归没有发现阻塞性缺陷，因此本次收口不再追加功能性代码修改。

本次清尾结论如下：

- `.gitignore` 已复查；本轮回归产生的 `test/tmp/`、日志、构建产物和本地工作目录文件继续被现有规则覆盖，不需要新增忽略项。
- 按当前工作流，本轮完成后已归档总回归结果，并清空当前 `docs/tmp/todo.md`，等待下一轮重新制定新的总目标、子步骤、边界和唯一下一步。

## 11. 初步框架重构蓝图已冻结

截至 2026-04-19，第一轮“初步框架重构”的第一个子步骤已经完成：当前轮的正式蓝图已经冻结。

本次已完成的收束如下：

- 新增平台级正式文档 `docs/initial-architecture-refactor-blueprint.md`，作为本轮“等价迁移”执行基线。
- 文档已固定本轮性质是“框架收束”，不是功能轮；当前外部协议、鉴权、管理密钥模型、TCP/UDP single/range 行为和 UDP idle cleanup 语义都不得改变。
- 文档已固定 `frps` 的目标边界：`internal/app` 只保留装配，`internal/api` 只保留管理面 HTTP 边界，`internal/control` 在同一 package 下按登录、session、config、listener、TCP bridge、UDP bridge 做文件级责任拆分。
- 文档已固定 `frpc` 的目标边界：继续保持单一 `internal/client` package，但按连接生命周期、登录、session dispatch、target 解析、TCP bridge、UDP bridge 做文件级责任拆分。
- 文档已固定本轮不引入的内容：插件系统、事件总线、DI 容器、统一 runtime 框架、为未来预留的大一统共享抽象层。
- 文档已固定迁移顺序：先 `frps`，再 `frpc`，随后只提炼最小共享规则和回归入口，最后做总回归收束。
- 文档已固定第一条实现动作：先收束 `frps/internal/control` 的 session 基础设施和顶层 dispatch，让 `server.go` 明显缩责。
- 复查 `.gitignore` 时，已把 `docs/tmp/todo.md` 从通用 `**/tmp/` 忽略规则中显式放出；当前固定约定为：`docs/tmp/` 可作为草稿箱，但只有 `todo.md` 进入版本管理，其余草稿继续忽略。

本子步骤完成后，当前轮剩余未完成内容为：

- 收束 `frps` 服务端骨架
- 收束 `frpc` 客户端骨架
- 提炼最小共享规则与回归入口
- 完成总回归与收束

## 12. Todo 轮换规则已进一步收紧

截至 2026-04-19，当前仓库的执行工作流又完成了一次归档收束：`todo` 轮换规则已经进一步收紧，后续每一轮都必须把“当前唯一下一步”压缩到一个最小可独立完成点。

本次固定下来的规则如下：

- `docs/tmp/todo.md` 除了当前总目标、子步骤和当前轮边界外，还必须额外写明“当前正在推进子步骤的更细子任务拆分”。
- `todo` 中的当前唯一下一步如果仍然包含多件事，必须先继续拆分，只允许保留其中第一个最小可独立完成点。
- `todo` 仍然只记录未完成内容；任何已完成内容都只能进入 `docs/progress/`，不能回填到当前轮 `todo`。
- `docs/workflow.md` 已同步这些规则，作为后续每一轮执行和轮换的正式基线。

## 13. `frps/internal/control` 的 session 基础设施迁移边界已盘点

截至 2026-04-19，第一轮“初步框架重构”当前子步骤又完成了一个已归档小步：`frps/internal/control/server.go` 中属于 session 基础设施的最小迁移边界已经盘点完成。

本次盘点确认的最小边界如下：

- 下一次新建 `session.go` 时，第一批只迁移：
  - `initialServerRequestID`
  - `sessionState`
  - `(*sessionState).nextRequestID`
  - `(*sessionState).doneCh`
  - `(*sessionState).closeDone`
- `(*Server).writeFrameWithSession`、`writeFramesWithSession`、`replyProtocolErrorWithSession`、`replyErrorWithSession`、`writeErrorWithSession` 已确认属于 session 相关写入/错误辅助，但为了继续保持“每次只做一小点”，固定留到后续单独子步骤再迁移。
- 当前仍明确保留在 `server.go` 的内容是连接入口、登录入口和顶层会话读循环相关内容，包括 `handleConnection`、`authenticate`、`runSession`、`handleConfigAck`、`handleHeartbeatPing`、`pushConfig`，以及底层 frame 读写和无 session 的错误回复辅助。
- 当前已经散落在 `data_plane.go` / `udp.go` 中的 public stream / UDP session 运行态访问器，暂不在这一步强行并回；本轮优先先把 `server.go` 里的 session 壳收出去，再继续收窄写辅助和桥接边界。
- 本次盘点不改任何 Go 代码，也不改变 wire protocol、challenge 登录流程、管理密钥模型、TCP/UDP single/range 行为或 UDP idle cleanup 语义。

本子步骤完成后的执行结论如下：

- 当前下一条代码动作已经被压缩为更小粒度：只先新建 `session.go` 并迁移 `initialServerRequestID` 与 `sessionState` 结构体定义，不移动任何方法。
- `.gitignore` 已复查；本子步骤只有文档更新，不需要新增忽略规则。

## 14. `frps/internal/control` 的 session 壳定义已拆到 `session.go`

截至 2026-04-19，第一轮“初步框架重构”当前子步骤又完成了一个已归档小步：`frps/internal/control` 的 session 壳定义已经从 `server.go` 抽到独立的 `session.go`。

本次已完成的实现和收束如下：

- 新增 `frps/internal/control/session.go`。
- `initialServerRequestID` 已从 `server.go` 迁移到 `session.go`。
- `sessionState` 结构体定义已从 `server.go` 迁移到 `session.go`。
- 本次没有迁移任何方法；`nextRequestID`、`doneCh`、`closeDone` 以及所有 session 写辅助、错误回复辅助仍保留在 `server.go`。
- 本次不改任何调用点，不改行为，不改包边界，只先把 session 基础定义从连接入口和登录主流程中剥离出去。

本次执行的验证如下：

- `gofmt -w internal/control/server.go internal/control/session.go`
- `go test ./internal/control`（`frps/` 模块）

本次固定下来的后续边界如下：

- 下一最小代码动作是把 `(*sessionState).nextRequestID`、`doneCh`、`closeDone` 迁移到 `session.go`，继续保持只做 session 自身方法，不触碰 `Server` 方法。
- `.gitignore` 已复查；本子步骤没有产生新的临时文件类型，不需要新增忽略规则。
