# 2026-04-21 归档

## 1. 分组生效 IP 的第一步文档契约已收口

截至 2026-04-21，当前轮第 1 个子步骤已经以文档方式完成收口：分组级“生效 IP”的持久化字段、管理 API CRUD 模型和 WebUI 表单语义已经固定，但当前仍不动代码实现。

本次已完成的文档收束如下：

- `docs/frps/technical-design.md` 已新增“已确认待落地的分组生效 IP 设计”章节，固定 `proxy_groups.effective_ip` 的字段名、允许值范围、CRUD 返回模型和“不自动回退 / 不自动改库”的运行态边界。
- `docs/frps/functional-spec.md` 已新增“已确认待实现的分组生效 IP 规则”章节，明确 `effective_ip` 是分组级可管理字段，创建/编辑需要提交，后续页面状态统一按单一“启用 / 禁用 / 异常”字段派生。
- `docs/webui/proxy-groups.md` 已新增“已确认待落地的生效 IP 扩展”章节，固定 UI 标签“生效 IP”、下拉来源、本机地址限制和异常值回显边界。
- `docs/tmp/todo.md` 已重写为只保留剩余步骤，当前唯一下一步已切到“本机网卡/IP 发现与变化轮询”。
- `docs/progress/README.md` 已补充 2026-04-21 归档索引。

本次固定下来的边界如下：

- 数据库/API 字段名固定为 `effective_ip`，UI 语义固定为“生效 IP”。
- `effective_ip` 只允许使用服务端当前本机 IPv4、服务端当前本机 IPv6，以及特殊值 `0.0.0.0`、`::`。
- `effective_ip` 不允许写入 hostname、CIDR、端口、逗号列表或空字符串。
- `effective_ip` 是分组级持久化配置，不新增 tunnel 级绑定 IP。
- “分组异常”继续定义为运行态派生状态，不新增持久化异常字段；配置 IP 消失后不自动改写数据库，也不静默切换到其他 IP。
- 运行态地址可用性校验依赖后续本机地址快照能力，这一步先只收口字段面和 CRUD 模型。

本次执行的验证如下：

- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 2. 服务端本机网卡/IP 快照层已落地

截至 2026-04-21，当前轮第 2 个子步骤已经完成最小代码落地：`frps` 启动时会先抓取本机全部网卡及 IPv4/IPv6 地址，并在后台持续轮询维护一份当前可用地址快照；Windows / Linux 的收口入口已经按平台拆开，但这一轮还没有把该快照接入 `effective_ip` 绑定、管理 API 或 WebUI。

本次已完成的实现收束如下：

- 新建 `frps/internal/system/`，新增 `NetworkSnapshotService`，负责启动时首轮采集、后台定时轮询、线程安全快照读取和关闭收口。
- 新增 `Snapshot` / `NetworkInterface` / `IPAddress` 结构，当前快照同时保留“按网卡分组的 IP 列表”和“去重后的当前可用 IP 集合”，IPv4 / IPv6 都会进入集合；特殊值 `0.0.0.0`、`::` 仍保留给后续 `effective_ip` 绑定规则处理，不混入物理地址快照。
- 新增 `network_collect_windows.go`、`network_collect_linux.go` 和 `network_collect_other.go`，按 build tag 区分平台收口；当前 Windows / Linux 都统一走 `net.Interfaces()` 地址发现主路径，但平台分流点已经固定，后续如需补平台特化逻辑可直接在各自文件扩展。
- 新增 `frps/internal/app/network.go`，并把本机地址快照服务接入 `App.Run()` / `shutdown()`：服务启动阶段会先完成首轮采集，关闭阶段会显式停止轮询协程。
- 当前这一步只新增“地址发现与轮询”基础层，还没有修改数据库 schema、`GroupRuntime`、listener 绑定地址、异常态派生或 WebUI 表单。

本次固定下来的边界如下：

- 本机地址快照属于服务端运行时基础设施，由 `frps` 启动时自动拉起，后续步骤统一复用这份快照，不再各处重复自行枚举本机地址。
- 快照采集失败会阻止本轮服务启动；轮询刷新失败只记录日志并保留最后一次成功快照，不清空现有可用地址集。
- 快照内容包含所有已发现网卡，以及每张网卡上的全部 IPv4 / IPv6 地址；同一地址在网卡维度内会去重，在全局可用地址集合内也会去重。
- 当前实现还不负责提供管理 API，也不负责判定“分组异常”；这些都留给后续步骤基于同一份快照继续接入。

本次执行的验证如下：

- `gofmt -w internal/app/app.go internal/app/network.go internal/system/network_snapshot.go internal/system/network_collect.go internal/system/network_collect_linux.go internal/system/network_collect_windows.go internal/system/network_collect_other.go internal/system/network_snapshot_test.go`（`frps/` 模块）
- `go test ./internal/system ./internal/app`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 3. 分组生效 IP 的 schema / API / 控制面绑定已落地

截至 2026-04-21，当前轮第 3 个子步骤已经完成后端主链路落地：`proxy_groups.effective_ip` 已进入数据库 schema、管理 API CRUD、`GroupRuntime` 和公网 listener 绑定路径；`frps` 会复用本机地址快照校验该值，只允许当前本机 IPv4/IPv6 或特殊值 `0.0.0.0`、`::`，不再静默回退到任意地址。

本次已完成的实现收束如下：

- `frps/internal/system/` 新增共享 `NormalizeListenIP` / 特殊监听地址 helper，统一收口 `effective_ip` 的 IP 字面量规范化和特殊值判断。
- `frps/internal/app/schema.go` 为 SQLite / MySQL 的 `proxy_groups` 新增 `effective_ip` 非空列，并把严格 schema 校验同步更新；继续沿用当前开发阶段约束，不做自动 migration。
- `frps/internal/api/management.go` 已把 `effective_ip` 加入分组创建、更新、列表、按 ID 读取和重置 token 返回模型；创建/更新会复用本机地址快照校验，只接受当前本机地址或 `0.0.0.0` / `::`。
- `frps/internal/control/repository.go` 已把 `effective_ip` 装入 `GroupRuntime`；`frps/internal/control/listeners.go` 在创建分组下 TCP/UDP listener 时统一按该地址绑定，并在快照存在但地址已失效时直接拒绝，不再回退到空 host 监听。
- `frps/internal/app/app.go` 已把同一份本机地址快照 reader 注入管理 API 和控制面；后续异常态接口和 WebUI 下拉都继续复用这条依赖链。
- 回归已补到 schema / API / control / system：新增了 `effective_ip` 规范化测试、API 对本机地址校验和 CRUD 回归、控制面按指定 IP 绑定及地址缺失拒绝回归。

本次固定下来的边界如下：

- `effective_ip` 现在是分组持久化必填字段；管理 API 创建和编辑都要求显式提交，不再接受空字符串。
- 管理 API 当前就会拒绝非本机 IP；特殊值 `0.0.0.0` 与 `::` 始终允许。
- 控制面当前只负责“按配置绑定”或“因地址无效而直接失败”，还没有把这类失败派生成分组异常状态返回给 WebUI。
- 本轮仍不改 WebUI；因此页面侧的“生效 IP”下拉、单一状态字段对应的“启用 / 禁用 / 异常”标签和失效值回显继续留给下一步。

本次执行的验证如下：

- `gofmt -w internal/system/effective_ip.go internal/system/effective_ip_test.go internal/app/schema.go internal/api/server.go internal/api/management.go internal/app/app.go internal/control/server.go internal/control/repository.go internal/control/listeners.go internal/app/database_test.go internal/api/server_test.go internal/control/repository_test.go internal/control/server_test.go`（`frps/` 模块）
- `go test ./internal/system ./internal/api ./internal/control ./internal/app`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 4. 分组运行态状态字段已接入管理 API

截至 2026-04-21，当前轮第 4 个子步骤已经完成后端收口：管理接口现在会为分组返回单一运行态状态字段 `status` 和可选 `status_reason`；状态规则固定为 `enabled=false -> 禁用`、`enabled=true && effective_ip 在当前可用 IP 列表中 -> 启用`、`enabled=true && effective_ip 不在当前可用 IP 列表中 -> 异常`。本次按当前用户要求只改后端，不改 WebUI。

本次已完成的实现收束如下：

- `frps/internal/api/management.go` 已为分组返回模型补充 `status` / `status_reason`，并在列表、创建、更新、按 ID 读取和重置 token 返回路径统一派生该状态。
- 状态判定统一复用现有 `enabled`、`effective_ip` 和本机地址快照；特殊值 `0.0.0.0`、`::` 继续视为始终可用，不要求出现在物理网卡地址快照中。
- 当前异常原因已固定两类最小文案：本机地址快照不可用时返回“本机 IP 列表暂不可用”，已配置地址已不在当前列表时返回“已配置 IP 当前不存在于本机”。
- `frps/internal/api/server_test.go` 已补充回归，覆盖创建返回启用、更新后禁用、token 重置延续禁用，以及地址快照变化后状态从启用切到异常。
- `docs/frps/functional-spec.md`、`docs/frps/technical-design.md` 和 `docs/webui/proxy-groups.md` 已同步成“后端状态字段已落地、WebUI 仍待消费”的口径。

本次固定下来的边界如下：

- 分组状态当前只返回一个字段 `status`，不再额外拆出第二个“IP 可用性状态”字段。
- `status_reason` 只作为异常态附加说明，不参与独立状态判定。
- `enabled=false` 永远优先派生成“禁用”，即使当前 `effective_ip` 已经不在本机地址列表中，也不会改成“异常”。
- 本次不改 WebUI；页面对 `status` / `status_reason` 的展示、编辑弹窗提示和状态标签适配留给下一步。

本次执行的验证如下：

- `gofmt -w internal/api/management.go internal/api/server_test.go`（`frps/` 模块）
- `go test ./internal/api`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）

## 5. 分组状态字段的 WebUI 展示已落地

截至 2026-04-21，当前轮第 5 个子步骤已经完成 WebUI 接入：分组列表、操作栏和编辑弹窗现在都会展示后端返回的 `status` / `status_reason`，用户可以直接看到"启用 / 禁用 / 异常"三态，并在异常时获得具体原因提示。

本次已完成的实现收束如下：

- `frps/webui/src/api/index.ts` 已为 `ProxyGroup` 接口补充 `status` / `status_reason` 字段。
- `frps/webui/src/views/GroupConfigView.vue` 分组列表状态列已改为三态展示：启用显示绿色 success 标签、禁用显示灰色 info 标签、异常显示红色 danger 标签；异常时会用 tooltip 展示 `status_reason`。
- 操作栏已补充选中分组的状态展示，同样支持异常原因 tooltip。
- 编辑弹窗已新增异常状态提示：编辑异常分组时，表单上方会显示 `el-alert` 提示当前异常原因。
- 新增 refs `editingGroupStatus` / `editingGroupStatusReason` 用于在编辑态保留并展示当前分组状态。

本次固定下来的边界如下：

- 状态标签统一使用后端返回的 `status` 字段派生，不再只看 `enabled` 布尔值。
- 异常原因 `status_reason` 只在 `status === '异常'` 时展示，正常状态不显示额外文案。
- 编辑弹窗的异常提示只做信息展示，不提供快捷修复入口。

本次执行的验证如下：

- `npm run build`（`frps/webui/` 模块）
- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 6. 生效 IP 下拉与异常值回显已落地

截至 2026-04-21，当前轮第 6 个子步骤已经完成：WebUI 分组表单增加了"生效 IP"下拉列表，选项来自后端 `/api/v1/local-ips` 接口返回的本机可用 IPv4/IPv6 地址与特殊值 `0.0.0.0`、`::`；编辑分组时若数据库中的 `effective_ip` 已不在当前可用列表中，下拉框会以"已失效"标记回显该值。

本次已完成的实现收束如下：

- `frps/internal/api/management.go` 新增 `handleLocalIPs` handler 和 `listLocalIPs` 服务方法，返回本机可用 IP 列表，特殊值 `0.0.0.0` 和 `::` 固定排在最前面。
- `frps/internal/api/server.go` 已注册 `/api/v1/local-ips` 路由。
- `frps/webui/src/api/index.ts` 新增 `LocalIP` 接口和 `localIPsApi.list()` 方法，并为 `ProxyGroup` 接口补充 `effective_ip` 字段。
- `frps/webui/src/views/GroupConfigView.vue` 分组表单新增"生效 IP"下拉框，选项来自 `localIPsApi.list()` 返回值；创建时默认选中 `0.0.0.0`，编辑时保留当前配置值；操作栏新增"生效 IP"显示。
- `effectiveIPOptions()` 函数处理异常值回显：当编辑分组且当前 `effective_ip` 不在可用列表中时，会额外显示带"已失效"标记的选项。
- `frps/internal/api/server_test.go` 新增 `TestLocalIPsEndpoint` 和 `TestLocalIPsEndpointReturnsUnavailableWithoutSnapshot` 测试用例。

本次固定下来的边界如下：

- `/api/v1/local-ips` 固定返回 `0.0.0.0`、`::` 以及本机所有可用 IPv4/IPv6 地址；特殊值固定排在列表最前。
- 分组表单创建时默认选中 `0.0.0.0`，若 `0.0.0.0` 不在列表则选择第一个可用地址。
- 编辑分组时，下拉框会显示当前配置的 `effective_ip`，即使该值已不在当前本机可用列表中；此类异常值会标记"已失效"以提示用户。
- 后端 API 和控制面已有完整的 effective_ip 校验逻辑，WebUI 只负责展示和交互，不做额外校验。

本次执行的验证如下：

- `go test ./internal/api ./internal/system -v`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `npm run build`（`frps/webui/` 模块）
- `gofmt -w internal/api/management.go internal/api/server.go internal/api/server_test.go`（`frps/` 模块）
- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 7. 分组生效 IP 全链路回归已完成

截至 2026-04-21，当前轮第 7 个子步骤已经完成：围绕分组生效 IP 的 system / API / control / WebUI 全链路回归已收口，当前总目标已完成。本次以“补齐自动化验证 + 修正回归中暴露的小问题”为主，没有再引入新的功能范围。

本次已完成的回归与修正如下：

- `frps/internal/system/` 新增平台 build tag 回归：补充 `network_collect_linux_test.go` 与 `network_collect_windows_test.go`，固定 `newPlatformCollector()` 在 Linux / Windows 下分别返回对应平台标识，锁定当前平台分流入口不被回退。
- `frps/internal/api/server_test.go` 新增特殊 IPv6 回归，确认在本机地址快照不可用时，分组仍可使用特殊监听地址 `::` 创建且保持 `启用` 状态，不会被误判为“必须来自当前物理网卡地址”。
- `frps/internal/control/server_test.go` 新增控制面回归，确认 `resolveGroupEffectiveIP()` 对特殊监听地址 `::` 直接放行，不会被本机地址快照缺失错误拦截。
- `frps/webui/src/api/index.ts` 将 `status_reason` 类型改为可选字段，和后端 `omitempty` 返回模型保持一致。
- `frps/webui/src/views/GroupConfigView.vue` 修正分组页加载逻辑：`/api/v1/local-ips` 失败时不再静默吞错，而是进入页面错误态；同时把本机 IP 加载状态与页面加载阶段对齐，避免回归时出现空下拉但无提示的情况。

本次固定下来的边界如下：

- 当前仓库已经覆盖：Windows / Linux 平台分流入口、本机地址轮询后生效 IP 消失导致的异常态、特殊地址 `0.0.0.0` / `::`、IPv4 / IPv6 地址列表返回，以及 WebUI 对状态字段和生效 IP 下拉的编译级回归。
- 本轮仍未引入专门的前端单元测试框架；WebUI 回归继续以类型检查 + `vite` 生产构建通过为准。
- 当前总目标“分组生效 IP”已完成，本轮 `todo` 将按工作流清空，等待下一轮重新写入。

本次执行的验证如下：

- `gofmt -w internal/api/server_test.go internal/control/server_test.go internal/system/network_collect_linux_test.go internal/system/network_collect_windows_test.go`（`frps/` 模块）
- `go test ./internal/system ./internal/api ./internal/control`（`frps/` 模块）
- `GOOS=linux GOARCH=amd64 go test -c -o %TEMP%\\frps-internal-system-linux.test ./internal/system`（`frps/` 模块，编译验证 Linux build tag 路径）
- `go test ./...`（`frps/` 模块）
- `npm run build`（`frps/webui/` 模块）
- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 8. 在线 frpc 会话已支持随管理侧配置变更刷新

截至 2026-04-21，本轮针对代码巡检发现的第 1 个明显缺陷已经完成修复：管理 API 修改分组或隧道配置后，`frps` 现在会定位该分组的在线 `frpc` 会话并立即触发刷新，不再只能等客户端手工断开后才生效。

本次已完成的实现收束如下：

- `frps/internal/control/` 新增按 `group_id` 重载运行态的能力：控制面现在会登记在线分组会话，支持按分组 ID 重新读取 `GroupRuntime` 并向在线客户端重新下发 `config.push`。
- 刷新过程会同步重置该分组当前的公网 listener / UDP 会话；对正常配置变更走热刷新，对 token 被重置或前一版配置仍未 ack 的场景，主动断开控制连接，交给 `frpc` 现有自动重连逻辑重新登录。
- `frps/internal/control/repository.go` 补充了按分组 ID 读取运行态的方法，避免刷新路径只能依赖 token 查询。
- `frps/internal/api/management.go` 已把刷新动作接入分组更新、分组删除、token 重置、隧道创建、隧道更新、隧道删除；其中隧道跨分组移动会同时刷新旧分组和新分组。
- `frps/internal/app/app.go` 已把 control server 注入管理 API，收口为后端内部联动，不引入新的 HTTP 接口。
- 新增了控制面与 API 层回归：覆盖在线会话收到二次 `config.push`、token 重置后主动断线，以及管理侧各类变更触发对应分组刷新。

本次固定下来的边界如下：

- 当前实现优先保证“管理侧修改后自动生效”，不引入跨请求的配置刷新队列；如果在线会话仍有上一版 `config.push` 未确认，服务端会直接断开该会话而不是排队叠加多版配置。
- token 重置和分组删除不会尝试在原连接上继续维持会话，而是明确断线，确保旧鉴权信息不再继续复用。
- 空配置或禁用分组刷新后不会再尝试绑定监听 IP，只会保持控制连接存活并等待后续再次启用。
- 本次只处理后端在线会话刷新，不涉及 WebUI 变更。

本次执行的验证如下：

- `gofmt -w internal/control/repository.go internal/control/session.go internal/control/config.go internal/control/listeners.go internal/control/server.go internal/control/refresh.go internal/control/server_test.go internal/api/server.go internal/api/management.go internal/api/server_test.go internal/app/app.go`（`frps/` 模块）
- `go test ./internal/control ./internal/api`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `go vet ./...`（`frps/` 模块）
- 检查 `.gitignore`，本次无需新增忽略项

## 9. 缺失 WebUI 构建产物时默认启动已改为降级占位页

截至 2026-04-21，本轮针对代码巡检发现的第 2 个明显缺陷已经完成修复：`frps` 在默认 `webui.dist_dir` 指向的 `webui/dist` 不存在，或目录下缺少 `index.html` 时，不再因为 WebUI 初始化失败而直接退出；服务现在会回退到内置占位页，继续启动管理 API 和控制监听。

本次已完成的实现收束如下：

- `frps/internal/api/webui.go` 已把 WebUI 初始化改为“缺失构建产物则降级”：`webui.dist_dir` 目录不存在或 `index.html` 缺失时，返回内置占位页 handler，不再向上传递启动错误。
- 同一路径下仍保留对明显配置错误的硬失败：如果 `webui.dist_dir` 指向的不是目录，或 `index.html` 实际上是目录，服务依旧会直接报错退出，避免静默吞掉错误配置。
- `frps/internal/api/server.go` 已在降级场景记录 warning 日志，明确当前 WebUI 资源不可用且已切回占位页。
- `frps/internal/api/server_test.go` 已补充回归：覆盖“缺失 dist 自动回退”“缺失 index 自动回退”以及“dist 路径实际是文件仍然失败”三类路径。
- `frps/README.md` 与 `docs/frps/development-guide.md` 已同步当前长期规则：未构建完整 `webui/dist` 时不会阻塞管理 API 启动。

本次固定下来的边界如下：

- 当前降级只兜底“构建产物缺失”场景，不扩展 WebUI 功能本身；根路径会返回内置占位页，管理 API 继续可用。
- 显式错误配置仍保持硬失败，避免把“路径填错”误判成“尚未构建前端”。
- 本次不处理管理 API 其它调用契约，也不顺带修改前端页面或路由行为。

本次执行的验证如下：

- `gofmt -w internal/api/webui.go internal/api/server.go internal/api/server_test.go`（`frps/` 模块）
- `go test ./internal/api`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `git diff --check`
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项
- 尝试执行 `python test/e2e_management_webui.py`（仓库根目录）时暴露既有问题：脚本创建分组仍未提交 `effective_ip`，当前会在 `POST /api/v1/proxy-groups` 返回 `400`；该问题不属于本子步骤，留待后续单独收口

## 10. 分组 PATCH 契约与管理面 e2e 回归脚本已收口

截至 2026-04-21，本轮 `todo` 中剩余的两个收尾项已经一起完成：`PATCH /api/v1/proxy-groups/{id}` 现已明确为局部更新语义，配套的 `test/e2e_management_webui.py` 也已按当前 `effective_ip` 契约修正并重新跑通。

本次已完成的实现收束如下：

- `frps/internal/api/management.go` 已拆分分组创建请求与分组 PATCH 请求模型，避免把“字段缺失”和“显式传空”混成同一语义。
- `PATCH /api/v1/proxy-groups/{id}` 现在会先读取当前分组配置，再把请求体中的已提交字段覆盖到现值上；未提交字段保持数据库原值，不再强制要求更新时重复提交完整 `name + effective_ip + enabled`。
- PATCH 当前已固定两个边界：空对象会返回 `400`；只有显式提交的 `effective_ip` 才重新执行“本机当前 IP / 特殊值”校验。
- 因此，数据库里已经进入“异常”态的分组，现在仍可以先通过 PATCH 把 `enabled` 改成 `false`，不会因为旧 `effective_ip` 已失效而连禁用都失败。
- `frps/internal/api/server_test.go` 已补充回归，覆盖空 PATCH 拒绝、局部 PATCH 保留 `effective_ip` / `enabled` 现值，以及“配置 IP 已失效时仍可禁用分组”。
- `test/e2e_management_webui.py` 已在创建分组时显式提交 `effective_ip=0.0.0.0`，并在分组更新阶段验证 partial PATCH 后该值会被保留，脚本现已重新通过。
- `docs/frps/technical-design.md` 与 `docs/frps/functional-spec.md` 已同步当前长期规则：分组 PATCH 是局部更新语义，空对象无效。

本次固定下来的边界如下：

- `POST /api/v1/proxy-groups` 仍要求显式提交 `effective_ip`，这一点没有放宽。
- `PATCH /api/v1/proxy-groups/{id}` 只对请求体中实际出现的字段做写入和重新校验；未出现的字段保持现状。
- 已存在但当前失效的 `effective_ip` 允许暂时继续留在数据库中，以便运行态维持“异常”状态并允许用户后续禁用或修复；只有当客户端显式提交新的 `effective_ip` 时，才必须再次满足“当前本机 IP / 特殊值”约束。
- 当前收尾只涉及后端契约、自动化回归和文档，不新增 WebUI 代码变更。

本次执行的验证如下：

- `gofmt -w internal/api/management.go internal/api/server_test.go`（`frps/` 模块）
- `go test ./internal/api`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `python test/e2e_management_webui.py`（仓库根目录，通过）
- `git diff --check`
- 检查 `.gitignore`，本次无需新增忽略项

当前 `todo` 总目标已完成，后续按工作流清空 `docs/tmp/todo.md`，等待下一轮重新写入。

## 11. 在线整组配置热重载方案文档已收口

截至 2026-04-21，下一轮目标的第 1 个子步骤已经先以文档方式收口：在线分组配置热重载的目标方案已写入正式设计文档，并把端口范围在热重载场景下的判定边界一起固定。当前这一步只改文档，不动代码。

本次已完成的文档收束如下：

- `docs/frps/technical-design.md` 已新增“已确认待实现的在线整组配置热重载方案”章节，固定在线分组走“整组冻结 + 整组全量重建”、离线分组只写数据库、名称修改不触发热重载、失败时直接断开控制连接等边界。
- `docs/frps/functional-spec.md` 已新增“已确认待实现的在线整组配置热重载规则”章节，从产品行为层面固定 `frps` 在下发前先冻结整组 listener 和活动 TCP/UDP 连接，`frpc ack ok` 后才重新开放 listener 的语义。
- `docs/frpc/technical-design.md` 已新增对应章节，固定 `frpc` 后续仍只消费完整快照，在本地对比旧快照和新快照后完成资源清理和运行态替换，再返回 `config.ack(status=ok)`。
- `docs/protocol.md` 已补充在线整组热重载约束，明确后续仍复用现有 `config.push / config.ack`，不新增单 tunnel 增量协议事件。
- 上述文档已同步固定端口范围的热重载判定：range 只要边界或映射基准变化，就按整 tunnel 替换处理，不因区间有重叠而做端口复用优化。

本次固定下来的关键边界如下：

- 分组离线时，配置变更只写数据库。
- 分组在线且运行态配置变更时，首版按“整组冻结 + 整组全量重建”收口，不承诺保留未受影响 tunnel 的现有连接。
- 分组名和隧道名修改不进入运行态热重载。
- 同一控制连接同一时刻只允许一个未确认的 `config.push`。
- token 重置和分组删除不走热重载，继续直接断开控制连接。
- 端口范围从 `1000-2000` 改为 `1500-2500` 这类场景，虽然数值区间有重叠，但因为 `offset = remotePort - remoteStart` 的基准变化，映射关系已整体变化，必须按完整替换处理。

本次执行的验证如下：

- `git diff --check`
- 检查 `.gitignore`，本次无需新增忽略项

## 12. `frps` 侧在线整组热重载首步已落地

截至 2026-04-21，当前轮 `todo` 的第 1 个子步骤已经完成：`frps` 侧整组冻结、在线 `config.push / config.ack` 重载状态管理和 listener 重建已进入真实代码链路；当前总目标仍未完成，下一步切到 `frpc` 侧在线 `config.push` 处理、本地资源清理和快照替换。

本次已完成的实现收束如下：

- `frps/internal/control/session.go` 已把会话状态拆成“已生效快照”和“等待 `config.ack` 的 pending 快照”；刷新时不会再把新 `GroupRuntime / ConfigSnapshot` 提前覆盖成已生效状态。
- `frps/internal/control/refresh.go` 现在会在下发新的整组 `config.push` 前先冻结整组运行态：停止该分组全部公网 listener，清理该分组当前活动 TCP `stream`，并清理该分组当前活动 UDP session。
- `frps/internal/control/config.go` 已改成只有在收到匹配的 `config.ack(status=ok)` 后，才解除冻结并按新快照重新启动 listener。
- `frps/internal/control/listeners.go`、`tcp_bridge.go` 和 `udp.go` 已补快照代际门闩：旧 listener 在热重载窗口内即使还有并发 accept / read，也不能继续打开新的 `stream.open` / `udp.open`。
- `frps/internal/control/server_test.go` 已补回归：覆盖“刷新时已有未确认 `config.push` 直接断线”以及“冻结现存 TCP / UDP 运行态、`ack` 前 listener 不开放、`ack` 后按新快照重建”的核心路径。
- `docs/frps/technical-design.md` 与 `docs/frps/functional-spec.md` 已同步为“`frps` 侧已落地、`frpc` 侧仍待完成”的当前口径；`docs/tmp/todo.md` 已切到下一唯一动作。

本次固定下来的边界如下：

- 分组在线且发生运行态配置变更时，`frps` 当前会先冻结整组运行态，再下发新的整组完整快照。
- 服务端当前不做“第二版热重载排队”；如果刷新时前一版 `config.push` 仍未确认，继续直接关闭该分组控制连接。
- `frps` 只有在对应 `config.ack(status=ok)` 成功后，才会重新启动 listener；在此之前不会继续接受该分组新的公网连接。
- 当前这一步只完成 `frps` 侧；`frpc` 仍未在 `ack` 前完成本地 TCP / UDP 资源清理和运行态替换，因此当前 `config.ack(status=ok)` 还不能视为客户端运行态已经完整切换完成。

本次执行的验证如下：

- `gofmt -w internal/control/session.go internal/control/config.go internal/control/refresh.go internal/control/listeners.go internal/control/tcp_bridge.go internal/control/udp.go internal/control/server_test.go`（`frps/` 模块）
- `go test ./internal/control`（`frps/` 模块）
- `go test ./...`（`frps/` 模块）
- `git diff --check`（仅出现现有 CRLF 提示，无 diff 格式错误）
- `git status --short --ignored`
- 检查 `.gitignore`，本次无需新增忽略项

## 13. `frpc` 侧在线整组热重载已落地

截至 2026-04-21，当前轮 `todo` 的第 2 个子步骤已经完成首个实现收口：`frpc` 侧在线 `config.push` 现在会在返回 `config.ack(status=ok)` 前完成本地 TCP/UDP 运行态清理和快照替换；当前总目标仍未完成，下一步切到单端口 / 端口范围在线热重载回归。

本次已完成的实现收束如下：

- `frpc/internal/client/client.go` 已把 `config.push` 应用顺序改成“先清理、再替换、后 ack”：收到新的整组快照后，会先完成本地 reload 流程，再返回 `config.ack(status=ok)` 并更新 `lastAckedConfigVersion`。
- `frpc/internal/client/session.go` 新增快照热重载收口逻辑：会对比旧快照和新快照，按 `tunnelId` 归类新增 / 删除 / 替换 / 未变化 tunnel，并把统计结果进入日志。
- `frpc/internal/client/tcp_bridge.go` 与 `udp_bridge.go` 的批量收口现在会返回已关闭的本地运行态数量，供 `config.push` 热重载路径记录实际清理结果。
- `frpc/internal/client/reload_test.go` 已新增回归：覆盖“旧本地 TCP/UDP 运行态在 `config.ack` 前已经被关闭”以及“热重载后新的 `stream.open` 已命中新快照目标”的核心路径。
- `docs/frpc/technical-design.md`、`docs/frpc/functional-spec.md`、`docs/frps/technical-design.md`、`docs/frps/functional-spec.md` 与开发指南已同步为“`frpc` 侧热重载已进入真实链路”的当前口径。
- `docs/tmp/todo.md` 已重写为只保留剩余子步骤，当前唯一下一步切到在线热重载回归。

本次固定下来的边界如下：

- `frpc` 继续只消费整组完整快照，不新增单 tunnel 增量协议事件。
- 首版热重载仍按“整组冻结 + 整组全量重建”收口；`frpc` 会在本地直接关闭当前全部活动 TCP stream 和 UDP session，不承诺保留未受影响 tunnel 的现有连接。
- `config.ack(status=ok)` 当前已经收紧为“本地资源清理和快照替换都已完成”的明确信号；`frps` 只有在收到该信号后才重新开放 listener。
- tunnel 名称不在运行态快照内；单纯改名称不会触发 `frpc` 本地运行态变更。
- 端口范围在 `frpc` 侧的热重载判定继续固定为“按 tunnel 整体替换”，只要边界或映射基准变化，就按完整替换处理。

本次执行的验证如下：

- `gofmt -w internal/client/client.go internal/client/session.go internal/client/tcp_bridge.go internal/client/udp_bridge.go internal/client/reload_test.go`（`frpc/` 模块）
- `go test ./internal/client`（`frpc/` 模块）
- `go test ./...`（`frpc/` 模块）
- `python test/e2e_management_webui.py`（仓库根目录，通过）
- `python test/e2e_tcp_single.py --scenario happy_path`、`python test/e2e_tcp_range.py`、`python test/e2e_udp_single.py --scenario happy_path`、`python test/e2e_udp_single.py --scenario idle_cleanup`、`python test/e2e_udp_range.py`（仓库根目录，当前都在 seed 阶段失败；根因是脚本仍按旧 schema 直接写 `proxy_groups`，未提交 `effective_ip`，不属于本次 `frpc` 代码改动引入）
- `git diff --check`（仅出现现有 CRLF 提示，无 diff 格式错误）
- 检查 `.gitignore`，本次无需新增忽略项
