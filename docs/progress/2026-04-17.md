# 2026-04-17 归档

以下内容根据 `git log` 近似补档，用于承接工作流建立前的早期实现背景。

## 1. 基础协议与鉴权起步

这一阶段先固定了 `frps` / `frpc` 的最小协议边界和分组鉴权方向：

- 协议层明确为二进制业务帧和长度前缀分包。
- 分组 token 固定采用 `token_id + token_secret` 拼接结构。
- 登录校验方向固定为 challenge/response，而不是直接上传可复用密钥材料。
- 文档层已经开始区分平台概述、协议、子项目设计和执行流转。

## 2. 存储与配置基线

这一阶段同步落下了最小存储起点：

- 存储范围先固定为 SQLite / MySQL 双支持。
- `proxy_groups` / `tunnels` 成为最早进入实现主线的核心运行表。
- 启动期 schema bootstrap、基础校验和最小仓储抽象已经开始成型。

## 3. 早期长期边界

按当时的提交记录，以下边界在这一阶段已经被提前收束：

- 服务端托管配置，`frpc` 保持轻量。
- 限速、抓包、连接观测属于后续扩展面，不进入最早一轮主线。
- 平台长期目标仍包含 TCP/UDP 正向代理、反向代理、管理面和观测面，但实现顺序先压缩到最小闭环。
