# 2026-04-23

## 1. `RefreshGroup()` / 轮询扫描并发窗口已补齐并修正重复 pending 误关会话缺陷

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“竞争窗口补齐”第 1 个子步骤已经完成：已把 `RefreshGroup()` 与 `scanNonListeningTunnelRuntimeIssues()` 并发下的重复 `config.push` / 陈旧快照回退窗口落成正式测试，并顺手修掉了一处真实实现缺陷。

本次已完成的实现与测试如下：

- `frps/internal/control/runtime_recovery_scenario_test.go`
  - 新增 `TestServerScenarioDeduplicatesMatchingRefreshAndScanRecoveryPush`
    - 直接把 active session 固定在“空配置保活”状态；
    - 让轮询扫描先命中恢复路径并在 `control.config_push.before_write` 卡住；
    - 再并发触发 `RefreshGroup()`，验证不会重复下发第二个 `config.push`，也不会因为“已有 pending config”误关当前 session。
  - 新增 `TestServerScenarioIgnoresStaleScanSnapshotAfterRefreshAdvancesVersion`
    - 让轮询扫描先抓到旧快照 `v2`，再由 `RefreshGroup()` 推进到新快照 `v3`；
    - 验证旧扫描结果后续不会再补推 `v2`，不会把已生效的 `v3` listener / snapshot 回退成旧版本。
- `frps/internal/control/session.go`
  - 新增 `refreshPendingConfig()`：
    - 当已有 pending config 且其“已下发目标快照”与本次刷新目标一致时，不再把它视为冲突；
    - 同时会把最新 `GroupRuntime` 元数据覆写到 pending 上，保证后续 ack 生效时使用的是最新分组元信息。
- `frps/internal/control/refresh.go`
  - `RefreshGroup()` 现在会先计算“本次真正想推进的目标快照”（完整快照或空配置快照），再决定如何处理已有 pending config。
  - 命中“同一目标快照已在飞行中”时，改为去重返回，不再关闭 session。
  - 只有 pending config 与当前刷新目标确实不同，才继续沿用原来的保守策略关闭 session。
  - 新增 `samePushedConfigSnapshot()`，用 `config_version + generated_at + tunnel 集合` 精确判断“是否同一份已下发配置”，避免把新版本误并到旧版本上。

本次收束后的边界如下：

- 当前只修复“同一目标快照并发到来时的重复 pending 误关会话”；不同目标快照并发时，仍保持保守关闭策略，后续由剩余竞争窗口步骤继续覆盖。
- 当前已正式覆盖两类关键窗口：
  - 同目标恢复去重：不双推、不误断会话
  - 旧扫描结果晚到：不把已推进的新快照回退成旧快照

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerScenario(DeduplicatesMatchingRefreshAndScanRecoveryPush|IgnoresStaleScanSnapshotAfterRefreshAdvancesVersion)" -count=1`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenario(DeduplicatesMatchingRefreshAndScanRecoveryPush|IgnoresStaleScanSnapshotAfterRefreshAdvancesVersion)" -count=5`（目录：`frps/`，确认竞争窗口回归可稳定复现）
- `go test ./...`（目录：`frps/`）
- `go test -tags testhooks ./...`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 2. 空配置保活后的断线重连恢复路径已补齐，确认旧 listener / runtime issue / pending 不泄漏到新 session

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“恢复路径与故障补齐”第 1 个子步骤已经完成：已把“空配置保活后的客户端断线重连”补成正式回归，覆盖旧 session 退出后新 session 接管时的 listener、runtime issue 和 pending config 清理边界。

本次新增测试如下：

- `frps/internal/control/effective_ip_scenario_test.go`
  - 新增 `TestServerScenarioReconnectAfterEmptyConfigClearsOldRuntimeIssueAndListeners`
    - 先让旧 session 因 `effective_ip` 不在本机而缩成空配置保活；
    - 再在本机地址恢复后断开旧 session，并由新 session 重新登录；
    - 验证旧 listener 已全部清空，旧 runtime issue 不会遗留到新 session，最终只保留新 session 的单份 listener 和 group slot。
  - 新增 `TestServerScenarioReconnectAfterPendingRecoveryPushDropsOldPendingConfig`
    - 让旧 session 先进入空配置态，再由轮询扫描触发“完整配置恢复 push”，故意停在未 ack 的 pending full-config 状态；
    - 随后断开旧 session 并让新 session 重新登录；
    - 验证旧 pending config 不会阻塞或污染新 session，新 session 能重新完成初始下发、ack 和 listener 恢复。

本次收束后的边界如下：

- 旧 session 已处于 `empty_config` 或 `pending_full_config` 时，只要连接结束，后续新 session 登录不会继承旧 pending 状态。
- 旧 session 退出后，group slot 会被正确释放并重新分配给新 session；不会出现“无 active session 但 slot 仍占用”的残留。
- 新 session 在健康快照下完成首次 `config.ack` 后，会重新建立 listener 并清掉旧 runtime issue；当前这条恢复路径下未发现新的实现缺陷。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerScenarioReconnectAfter(EmptyConfigClearsOldRuntimeIssueAndListeners|PendingRecoveryPushDropsOldPendingConfig)" -count=1`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenarioReconnectAfter(EmptyConfigClearsOldRuntimeIssueAndListeners|PendingRecoveryPushDropsOldPendingConfig)" -count=5`（目录：`frps/`，确认恢复路径回归可稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 3. 外部端口高频抖动场景已补齐，并修正“probe 成功后过早清理 runtime issue”的真实缺陷

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“恢复路径与故障补齐”中“外部占用高频抖动同一端口”这一子步骤已经完成：已把“probe 成功后、正式重绑前再次被外部占用”的窗口补成确定性回归，并修正了一处真实实现缺陷。

本次新增的测试与修复如下：

- `frps/internal/control/runtime_recovery_scenario_test.go`
  - 新增 `TestServerScenarioKeepsRuntimeIssueUntilSamePortFlappingReallyRecovers`
    - 先让 tunnel 在启动阶段因外部占用进入 runtime `端口冲突`；
    - 再在轮询恢复里卡住 `runtime_probe -> runtime_start` 之间的窗口，制造“probe 成功后立刻复占”；
    - 验证旧 `runtime issue` 在 listener 真正恢复成功前不会被提前清空，不会错误显示为已恢复；
    - 再补一轮持续占用扫描和一轮真实恢复扫描，确认多次扫描交错下不会误恢复、不会遗留重复 listener，且最终能正常恢复。
- `frps/internal/control/runtime_scan.go`
  - 新增 `preserveScannedHealthyRuntimeIssuesUntilRecovery()`
    - 当轮询命中“active session + 同一 runtime snapshot + 无 pending config”的 listener 恢复路径时，如果扫描结果显示 runtime 前置条件已恢复，不再立即清空旧 `runtime issue`；
    - 只有等 `ensureTunnelListeners()` 里的正式 `runtime_start` 真正成功 attach listener 后，才由现有启动路径清掉旧异常。
  - `applyScannedTunnelRuntimeIssues()` 新增“保留旧异常直到真实恢复完成”的分支，避免把 `probe` 的瞬时成功误当成 tunnel 已恢复。

本次确认并收束的边界如下：

- 对于“同一快照下的未监听 tunnel 自动补 listener”路径，`runtime issue` 的清理时机现在收紧到“真实 listener 已恢复成功”之后。
- 若端口在 `probe` 成功后、`runtime_start` 前再次被外部占用，管理态仍会持续看到原有 `runtime issue`，不会出现一次扫描内的假恢复。
- 若连续多轮扫描都遇到同一端口反复占用，runtime `异常` 会持续保留，直到某一轮真实启动成功才清掉；当前这一路径下未发现新的 session 污染、重复 listener 或会话误关闭问题。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run TestServerScenarioKeepsRuntimeIssueUntilSamePortFlappingReallyRecovers -count=1 -v -timeout 60s`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenario(RecoversNonListeningTunnelAfterPollingSeesOccupancyGone|KeepsRuntimeIssueUntilSamePortFlappingReallyRecovers)" -count=5`（目录：`frps/`，确认抖动恢复路径稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- `go test ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 4. `effective_ip` 高频抖动场景已补齐，并修正“恢复未完成就提前清空 runtime issue”的真实缺陷

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“恢复路径与故障补齐”中“`effective_ip` 在本机地址集合内外高频抖动”这一子步骤已经完成：已把“empty config / full config 连续交错、健康与失效来回抖动”的窗口补成确定性回归，并修正了一处真实实现缺陷。

本次新增的测试与修复如下：

- `frps/internal/control/effective_ip_scenario_test.go`
  - 新增 `TestServerScenarioKeepsEffectiveIPRuntimeIssueAcrossHighFrequencyEmptyAndFullRecoveryFlaps`
    - 先让 session 从正常监听进入 `effective_ip` 不在本机导致的 `empty config` 待 ack 状态；
    - 再在 `empty config` 未 ack、`full config` 未 ack 这两个窗口里，反复把 `effective_ip` 从“本机不存在”切回“重新可用”；
    - 验证轮询扫描不会在恢复尚未真正生效前提前清空 `runtime issue`，也不会重复补推配置；
    - 同时验证旧 IP / 新 IP 不会残留脏 listener，最终仍满足“完整配置 -> ack -> listener 恢复”的顺序。
  - 收紧 `TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind`
    - 原测试此前默认 `config.ack` 后立即可观测到 `empty_config`；本次改为等待稳定观测态后再断言，避免把瞬时 `pending_empty_config` 误当成实现回归。
- `frps/internal/control/runtime_scan.go`
  - `preserveScannedHealthyRuntimeIssuesUntilRecovery()` 现在不仅覆盖“同一快照下自动补 listener”的恢复路径，也覆盖：
    - `pending empty config`
    - `empty config` 已生效、等待轮询补推 `full config`
    - `pending full config`
  - 对这些窗口，只要扫描结果只是“前置条件暂时恢复”，就继续保留旧 `runtime issue`，直到真实恢复完成后再由现有 listener 启动路径清掉异常。

本次确认并收束的边界如下：

- `effective_ip` 从“无效/不在本机”抖回健康时，只要 `empty config` 或 `full config` 还在飞行中，管理态仍保持 `runtime issue` 可见，不会先误报为“启用”。
- `empty config` 待 ack 时，轮询扫描不会重复补推 `full config`；`full config` 待 ack 时，轮询扫描也不会重复补推第二份恢复配置。
- 从旧 IP 解绑到新 IP 恢复的整个过程中，不会遗留旧 IP listener、重复 listener、脏 group slot 或脏 pending 状态。
- 当前这一路径下，`runtime issue` 的清理时机已经收紧到“完整配置真正 ack 且 listener 恢复成功”之后。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run TestServerScenarioKeepsEffectiveIPRuntimeIssueAcrossHighFrequencyEmptyAndFullRecoveryFlaps -count=1 -v -timeout 60s`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run TestServerScenarioKeepsEffectiveIPRuntimeIssueAcrossHighFrequencyEmptyAndFullRecoveryFlaps -count=5 -timeout 60s`（目录：`frps/`，确认高频抖动窗口稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- `go test ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 5. 恢复路径资源层故障已补齐，覆盖权限不足、文件句柄紧张、高 churn listener 回滚，以及 pending recovery config 期间 heartbeat

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“恢复路径与故障补齐”中“资源层故障系统性覆盖”这一子步骤已经完成：已把恢复路径上的权限不足、文件句柄紧张、短时间大量 listener 启停回滚，以及空/满恢复配置 pending 期间 heartbeat 保活补成正式回归。

本次新增的测试如下：

- `frps/internal/control/recovery_resource_fault_scenario_test.go`
  - 新增 `TestServerScenarioKeepsSessionAliveWhenTCPRecoveryBindFailsWithPermissionDenied`
    - 先让 TCP tunnel 因外部占用进入 runtime `端口冲突`；
    - 再在同一快照的轮询恢复里注入 `runtime_start -> EACCES`；
    - 验证 session 不会被误关闭，runtime issue 会从“端口冲突”更新成真实的 `permission denied` 类启动失败，并且下一轮扫描可自动恢复。
  - 新增 `TestServerScenarioClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn`
    - 从 `empty config` 会话推进到多端口完整配置；
    - 连续三次在最后一个端口注入 `EMFILE`，制造“前几个 listener 已启动、最后一个失败”的高 churn 回滚窗口；
    - 验证每次失败后都不会残留半开 handles、脏 listener 或多余 `config.push`，最终故障解除后可一次性恢复整组 listener。
  - 新增 `TestServerScenarioKeepsSessionAliveWhenUDPRecoveryBindFailsWithPermissionDenied`
    - 对 UDP 恢复路径复用同样的“端口占用 -> 权限失败 -> 自动恢复”流程；
    - 验证 `listen_udp` 失败也只会留下正确 runtime issue，不会误关 session、不会遗留脏 UDP handle。
  - 新增 `TestServerScenarioServesHeartbeatWhileEmptyAndFullRecoveryConfigsArePending`
    - 在 `pending_empty_config` 和 `pending_full_config` 两个窗口里分别发送 `heartbeat.ping`；
    - 验证 heartbeat 仍可正常往返，不会推进错误状态，也不会提前恢复 listener 或插入额外控制帧。

本次确认并收束的边界如下：

- 对于同一快照下的 listener 自动恢复，`runtime_start` 命中权限不足或文件句柄紧张时，当前 active session 会保活，group slot 继续被正确占用，管理态保留最新的资源层 runtime issue。
- 多端口批量恢复在“部分端口已成功、后续端口失败”时，会把本轮已打开的 listener 全部回滚干净；连续多轮失败后也不会积累脏 handle 或半开 listener。
- UDP 恢复路径与 TCP 一样满足“失败只标记 runtime issue，不误关 session；故障消失后允许轮询自动恢复”。
- `pending_empty_config` / `pending_full_config` 期间，heartbeat 不会被配置恢复流程吞掉；直到 `config.ack` 真正接受并完成 listener 恢复前，都不会错误放行 runtime。
- 本轮验证里额外确认：这些依赖本机端口分配的场景在同一台机器上应串行执行验证命令；把多个 `go test` 进程并发跑会引入测试进程之间的端口竞争噪声，不属于实现缺陷。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerScenario(KeepsSessionAliveWhenTCPRecoveryBindFailsWithPermissionDenied|ClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn|KeepsSessionAliveWhenUDPRecoveryBindFailsWithPermissionDenied|ServesHeartbeatWhileEmptyAndFullRecoveryConfigsArePending)" -count=1 -v -timeout 120s`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenario(KeepsSessionAliveWhenTCPRecoveryBindFailsWithPermissionDenied|ClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn|KeepsSessionAliveWhenUDPRecoveryBindFailsWithPermissionDenied|ServesHeartbeatWhileEmptyAndFullRecoveryConfigsArePending)" -count=5 -timeout 120s`（目录：`frps/`，串行执行，确认四个资源层故障场景可稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- `go test ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 6. 竞争窗口前 3 项已补齐，覆盖 `config.push/config.ack` 乱序、session 接管旧帧隔离，以及 blocked bind 与 shutdown/disable/delete 交错

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“竞争窗口补齐”前 3 个子步骤已经完成：已把 `config.push/config.ack` 的重复/旧/乱序 ack，旧 session 被 replacement session 接管后的旧帧隔离，以及 listener 启动与 client disconnect、group disable、group delete 交错的窗口补成正式确定性回归。本步未发现新的实现缺陷，因此没有改业务实现代码。

本次新增测试如下：

- `frps/internal/control/competition_window_scenario_test.go`
  - 新增 `TestServerScenarioLateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession`
    - 用 `ScriptedFrameIO` 把旧 session 的首次 `config.ack` 延迟到连接关闭之后；
    - 再让 replacement session 完成新的 `config.push/config.ack` 和 listener 启动；
    - 验证旧延迟 ack 释放后不会污染新 session，也不会把 listener / group slot 回退到旧快照。
  - 新增 `TestServerScenarioRejectsOutOfOrderRecoveryAckWithoutLeavingPendingOrListeners`
    - 在空配置恢复到完整配置的 pending 窗口里，先注入旧版本 ack；
    - 验证服务端会按 `version mismatch` 拒绝并关闭当前 session，但不会留下脏 pending、脏 listener 或 group slot。
  - 新增 `TestServerScenarioRejectsDuplicateRefreshAckWithoutLeavingListenersOrPending`
    - 覆盖在线热更新 `config.push` 已成功 ack 后再次收到重复 ack；
    - 验证会话被保守关闭，且 listener / pending / group slot 全部回收干净。
  - 新增 `TestServerScenarioDelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession`
    - 用 `ScriptedFrameIO` 延迟旧 session 的 `heartbeat.ping` 和旧连接上的 `error` 返回帧；
    - 在 replacement session 接管并稳定运行后再释放旧帧；
    - 验证这些旧帧不会投递到新 session，也不会影响其 heartbeat 和 listener。
  - 新增 `TestServerUnregisterOldSessionKeepsReplacementSlotAndSession`
    - 直接覆盖旧 session 注销不会清掉新 session 已接管的 `group slot`。
  - 新增 `TestServerScenarioReleasingBlockedStartupBindAfterClientDisconnectLeavesNoListenerLeak`
    - 把首次 `runtime_start` bind 卡住，再让客户端主动断开；
    - 验证放行后不会遗留 listener handle、session、group slot。
  - 新增 `TestServerScenarioReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener`
    - 把首次 `runtime_start` bind 卡住，再并发执行 disable 刷新并下发空配置；
    - 验证旧 bind 放行后不会把已 disable 的 listener 重新 attach，空配置 ack 后状态稳定收敛。
  - 新增 `TestServerScenarioReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak`
    - 把首次 `runtime_start` bind 卡住，再并发删除 group；
    - 验证旧 bind 放行后不会残留半开 listener，旧 session 会完全退出。

本次确认并收束的边界如下：

- 当旧 `config.ack`、旧 heartbeat 或旧 error 已被 transport 层延迟，而旧连接随后被关闭并由新 session 接管时，这些旧帧不会再投递到 replacement session。
- `config.push` pending 期间若刷新目标变化，当前实现仍沿用“关闭旧 session，等待重连”的保守策略；本次已补齐相关重复 ack / 旧 ack / 乱序 ack 回归，确认不会污染 listener 集合或 pending 状态。
- 首次 listener bind 若与 client disconnect、group disable、group delete 交错，当前实现最终都能回收 listener handle、session 和 group slot；本轮验证中未发现脏 map、脏 pending 或 listener 泄漏。
- 本次没有新增业务实现改动；当前竞争窗口的主要剩余空白已收敛到“轮询扫描自身重叠 / 与配置修改并发 / 与网络快照刷新并发”和“服务端关闭过程并发”两组回归。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerScenario(LateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession|RejectsOutOfOrderRecoveryAckWithoutLeavingPendingOrListeners|RejectsDuplicateRefreshAckWithoutLeavingListenersOrPending|DelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession|ReleasingBlockedStartupBindAfterClientDisconnectLeavesNoListenerLeak|ReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|ReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak)|TestServerUnregisterOldSessionKeepsReplacementSlotAndSession" -count=1 -v -timeout 120s`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenario(LateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession|RejectsOutOfOrderRecoveryAckWithoutLeavingPendingOrListeners|RejectsDuplicateRefreshAckWithoutLeavingListenersOrPending|DelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession|ReleasingBlockedStartupBindAfterClientDisconnectLeavesNoListenerLeak|ReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|ReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak)|TestServerUnregisterOldSessionKeepsReplacementSlotAndSession" -count=5 -timeout 120s`（目录：`frps/`，确认竞争窗口回归稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- `go test ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 7. 剩余竞争窗口已全部补齐，并修正“Shutdown 早于首轮扫描完成仍会开放控制端口”的真实缺陷

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“竞争窗口补齐”剩余部分已经完成：已把轮询扫描自身重叠、轮询扫描与本机网络快照刷新并发、以及服务端关闭过程与首轮扫描 / 轮询恢复 / 热更新 listener 启停交错的窗口补成正式确定性回归，并顺手修掉了一处真实实现缺陷。

本次新增的实现与测试如下：

- `frps/internal/control/runtime_scan.go`
  - 新增 runtime scan round 互斥：
    - 当上一轮 `scanNonListeningTunnelRuntimeIssues()` 还未结束时，新的重叠扫描会直接跳过，不再重复进入 `ListGroupRuntimes()`、`probe` 和恢复路径。
  - 新增测试 hook `runtime.scan.skip_overlap`
    - 用于确定性验证“重叠扫描被跳过，而不是排队二次执行”。
  - 新增测试 hook `runtime.scan.before_group_recover`
    - 用于把“network snapshot 在本轮 issue 标记后、恢复动作前发生变化”的中间窗口稳定卡住并复现。
- `frps/internal/control/server.go`
  - 新增 server 级 shutdown 门闩：
    - `Shutdown()` 一旦先发生，`ListenAndServe()` 在首轮扫描结束后不会继续开放控制端口，也不会再启动后续 runtime scan 轮询。
  - 该修复直接收掉了一个真实缺陷：
    - 之前若 `Shutdown()` 发生在 `EnsureInitialRuntimeScan()` 阶段，等首轮扫描放行后仍可能把 frpc 控制端口打开，导致“已关服但端口又短暂可连”。
- `frps/internal/control/remaining_competition_window_scenario_test.go`
  - 新增 `TestServerScenarioSkipsOverlappingRuntimeScanRoundDuringRecoveryPush`
    - 让第一轮扫描卡在仓库 list，第二轮扫描并发进入；
    - 验证第二轮会直接跳过，不会二次 list、二次 `config.push`，也不会制造额外恢复帧。
  - 新增 `TestServerScenarioScanReusesFreshNetworkSnapshotBeforeRecoveryPush`
    - 先让 session 因 `effective_ip` 不在本机进入空配置；
    - 再把网络快照变化卡在“本轮 issue 已写入、恢复动作尚未执行”的窗口；
    - 验证同一轮扫描会基于最新网络快照推进恢复，但旧 `runtime issue` 仍会保留到真实 `ack + listener` 恢复完成后才清掉。
  - 新增 `TestServerScenarioShutdownDuringInitialRuntimeScanDoesNotOpenControlListenerAfterRelease`
    - 验证服务端若在首轮扫描期间关闭，扫描放行后不会再打开控制端口。
  - 新增 `TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak`
    - 把轮询恢复里的 `runtime_probe -> runtime_start` 卡住，再并发 `Shutdown()`；
    - 验证放行后不会残留 listener handle、session 或 group slot。
  - 新增 `TestServerScenarioShutdownWhileRefreshBindBlockedDoesNotAttachStaleListener`
    - 把热更新后的 `runtime_start` bind 卡住，再并发 `Shutdown()`；
    - 验证不会把 stale listener attach 回已关闭会话，也不会遗留旧/新端口 handle。

本次确认并收束的边界如下：

- runtime scan round 现在不会重叠执行；同一时间只允许一轮扫描进入仓库 list / probe / 恢复路径，避免双 probe、双恢复和重复恢复帧。
- 对于 `effective_ip` 恢复路径，若本机网络快照在同一轮扫描中途发生变化，恢复动作会读取最新快照，但管理态仍只会在真实恢复完成后清掉旧 `runtime issue`。
- `Shutdown()` 若早于首轮扫描完成发生，服务端不会再把 frpc 控制端口或后续轮询重新放开；当前窗口下未再发现“关服后重新可登录”的回退。
- `Shutdown()` 若与轮询恢复 bind 或热更新 bind 交错，最终都不会遗留 listener handle、脏 session、脏 group slot 或 stale listener attach。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerScenario(SkipsOverlappingRuntimeScanRoundDuringRecoveryPush|ScanReusesFreshNetworkSnapshotBeforeRecoveryPush|ShutdownDuringInitialRuntimeScanDoesNotOpenControlListenerAfterRelease|ShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|ShutdownWhileRefreshBindBlockedDoesNotAttachStaleListener)" -count=1 -v -timeout 120s`（目录：`frps/`）
- `go test -tags testhooks ./internal/control -run "TestServerScenario(SkipsOverlappingRuntimeScanRoundDuringRecoveryPush|ScanReusesFreshNetworkSnapshotBeforeRecoveryPush|ShutdownDuringInitialRuntimeScanDoesNotOpenControlListenerAfterRelease|ShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|ShutdownWhileRefreshBindBlockedDoesNotAttachStaleListener)" -count=5 -timeout 120s`（目录：`frps/`，确认剩余竞争窗口回归稳定复现）
- `go test -tags testhooks ./internal/control`（目录：`frps/`）
- `go test ./internal/control`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 8. 稳定性补充层已全部落地，并修正一处真实 `-race` 缺陷与一处多调度测试编排缺口

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的“稳定性补充层”已经一次性完成：已把 `-race`、多 `GOMAXPROCS`、高频配置抖动、长稳 soak、双平台基线和资源压力 6 类入口统一收口到同一套可串行执行的稳定性矩阵里，同时补上了稳定性专用场景，并修掉了两处在补充层回归中真实暴露出来的问题。

本次新增的实现与文档如下：

- `frps/internal/control/stability_scenario_test.go`
  - 新增 `TestServerStabilityScenarioConfigChurnConverges`
    - 在同一 active session 上连续推进多轮 `RefreshGroup()` 全量配置变更，验证旧端口 handle 不残留、无双 pending、最终稳定收敛。
  - 新增 `TestServerStabilityScenarioFakeTimeSoakKeepsResourcesBounded`
    - 用 fake time 长周期推进 `effective_ip` 空/满配置反复切换，验证 session、listener handle 和 scheduler job 会回落，不会越跑越脏。
  - 新增 `TestServerStabilityScenarioResourcePressureRefreshAcrossManyTunnelsConverges`
    - 放大 tunnel 数量与连续 refresh 次数，验证大量 listener 批量切换后仍能无泄漏收敛。
- `frps/cmd/stabilitymatrix/main.go`
  - 新增统一稳定性入口工具 `go run ./cmd/stabilitymatrix`
    - 统一定义 `race`、`gomaxprocs`、`config-churn`、`soak`、`platform`、`resource-pressure` 6 个 profile；
    - 每个 profile 都串行调用现有正式测试，不并发启动多个依赖本机端口可见性的 `go test` 进程；
    - 支持 `-list`、`-profile`、`-dry-run`、`-go`，并内置压力规模环境变量。
- `frps/cmd/stabilitymatrix/main_test.go`
  - 补齐 profile 展开顺序、`go test` 参数拼装、命令格式化的自测。
- `docs/frps/testing-stability.md`
  - 新增稳定性补充层执行说明，明确统一入口、profile 语义、环境旋钮和当前覆盖的正式测试集合。

本次在稳定性补充层回归中修掉的问题如下：

- `frps/pkg/transport/scripted_frame_io.go`
  - 修正 `ScriptedConn.Close()` 与 `ScriptedFrameIO.readFrame()` / `deliverToPeer()` 在 endpoint `closed/readClosed/writeClosed` 状态上的数据竞争；
  - 端点关闭语义从“直接关闭 inbox channel”收紧为“原子标记 + 单独 closed 信号”，避免 `-race` 下的读写竞争和 `send on closed channel` 风险。
- `frps/internal/control/recovery_resource_fault_scenario_test.go`
  - 收紧 `TestServerScenarioClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn` 的编排：
    - 初始空配置 `config.ack` 后新增 `waitForIdleConfig()`，确保 `RefreshGroup()` 一定发生在 pending 已清空之后；
    - 这样多 `GOMAXPROCS` 调度档位下不会再因为测试自身踩到“pending 尚未清空就刷新”而误把会话关掉。

本次收束后的边界如下：

- 稳定性补充层不再是零散命令集合，而是统一通过 `cmd/stabilitymatrix` 组织和串行执行。
- `-race` 与多 `GOMAXPROCS` 跑的是同一套正式场景，不再单独维护另一套 stress-only 脚本。
- 高频配置抖动、长稳 soak 和资源压力现在都有对应的正式测试用例，不再只靠重复运行已有单点场景。
- 当前平台基线入口已经在 Windows 上验证通过；同一 `platform` profile 可直接复用于 Linux / WSL 做跨平台比对。

本次执行的验证如下：

- `go test -tags testhooks ./internal/control -run "TestServerStabilityScenario(ConfigChurnConverges|FakeTimeSoakKeepsResourcesBounded|ResourcePressureRefreshAcrossManyTunnelsConverges)" -count=1 -v`（目录：`frps/`）
- `go test ./pkg/transport`（目录：`frps/`）
- `go test -race -tags testhooks ./internal/control -run "^(TestServerScenarioLateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession|TestServerScenarioDelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession|TestServerScenarioClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn)$" -count=1 -parallel=1`（目录：`frps/`）
- `go run ./cmd/stabilitymatrix -list`（目录：`frps/`）
- `go run ./cmd/stabilitymatrix -profile all -dry-run`（目录：`frps/`）
- `go run ./cmd/stabilitymatrix -profile all`（目录：`frps/`，Windows 环境，6 类 profile 全部通过）
- `go test ./...`（目录：`frps/`）
- `go test -tags testhooks ./...`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 9. Windows 全量复验已完成，当前代码树在本机基线下通过 `frps` 稳定性矩阵与双模块总回归

截至 2026-04-23，当前轮“跨平台全量复验”的 Windows 阶段已经完成：已在当前代码树上重新串行执行 `frps` 稳定性矩阵、`frps` 全模块回归、`frps` `testhooks` 全模块回归，以及 `frpc` 全模块回归，当前未发现新增失败项或不稳定项。

本次完成的验证如下：

- `go run ./cmd/stabilitymatrix -profile all`（目录：`frps/`，Windows，依次覆盖 `race`、`gomaxprocs`、`config-churn`、`soak`、`platform`、`resource-pressure` 6 类 profile）
- `go test ./...`（目录：`frps/`）
- `go test -tags testhooks ./...`（目录：`frps/`）
- `go test ./...`（目录：`frpc/`）

本次确认并收束的边界如下：

- 当前 Windows 基线下，端口冲突检查 / 热更新交互主线相关稳定性矩阵仍全部通过，没有因近期补测或文档调整引入回退。
- `frps` 普通构建视角和 `testhooks` 视角的全模块回归都通过，说明测试专用 seam 没有破坏常规行为。
- `frpc` 当前模块总回归通过；本轮服务端稳定性补充没有向客户端模块引入直接回归信号。
- 当前阶段未新增需要补进 `.gitignore` 的测试产物或本地环境文件。

## 10. Linux 全量复验已补跑完毕，确认普通回归通过，但 `race` 档位存在非确定性失败

截至 2026-04-23，当前轮“跨平台全量复验”的 Linux 阶段已经完成：已在 WSL Ubuntu（`linux/amd64`, `go1.22.2`）上复跑 `frps` / `frpc` 的全量测试入口，并额外对 `frps` 稳定性矩阵中的 `race` 档位做了定点复现。结果显示：普通总回归通过、`testhooks` 总回归通过、`gomaxprocs` / `config-churn` / `soak` / `platform` / `resource-pressure` 5 个 profile 通过，但 `race` 档位存在非确定性失败，当前尚不能给出“Linux 全量稳定通过”的结论。

本次完成的验证如下：

- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile all"`（执行 2 次，均在 `race` 档位提前失败）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile race"`（执行 1 次，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=5 -race -tags testhooks -run '^(TestServerScenarioRejectsInitialInvalidEffectiveIPWithoutLeakingSessionState|TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioRecoversNonListeningTunnelAfterPollingSeesOccupancyGone|TestServerScenarioKeepsRuntimeIssueUntilSamePortFlappingReallyRecovers|TestServerScenarioDeduplicatesMatchingRefreshAndScanRecoveryPush|TestServerScenarioIgnoresStaleScanSnapshotAfterRefreshAdvancesVersion|TestServerScenarioUsesFreshSnapshotWhenRepositoryChangesDuringLoginHandshake|TestServerScenarioKeepsSessionAliveWhenPortBecomesOccupiedRightAfterStartupAck|TestServerScenarioShrinksToEmptyConfigWhenEffectiveIPBecomesInvalidRightAfterStartupAck|TestServerScenarioReconnectAfterEmptyConfigClearsOldRuntimeIssueAndListeners|TestServerScenarioReconnectAfterPendingRecoveryPushDropsOldPendingConfig|TestServerScenarioKeepsEffectiveIPRuntimeIssueAcrossHighFrequencyEmptyAndFullRecoveryFlaps|TestServerScenarioKeepsSessionAliveWhenTCPRecoveryBindFailsWithPermissionDenied|TestServerScenarioClosesPartiallyStartedTCPListenersAcrossRepeatedEMFILERecoveryChurn|TestServerScenarioKeepsSessionAliveWhenUDPRecoveryBindFailsWithPermissionDenied|TestServerScenarioServesHeartbeatWhileEmptyAndFullRecoveryConfigsArePending|TestServerScenarioLateDelayedAckFromClosedSessionDoesNotPolluteReplacementSession|TestServerScenarioRejectsOutOfOrderRecoveryAckWithoutLeavingPendingOrListeners|TestServerScenarioRejectsDuplicateRefreshAckWithoutLeavingListenersOrPending|TestServerScenarioDelayedOldHeartbeatAndErrorFramesDoNotAffectReplacementSession|TestServerUnregisterOldSessionKeepsReplacementSlotAndSession|TestServerScenarioReleasingBlockedStartupBindAfterClientDisconnectLeavesNoListenerLeak|TestServerScenarioReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|TestServerScenarioReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak|TestServerScenarioSkipsOverlappingRuntimeScanRoundDuringRecoveryPush|TestServerScenarioScanReusesFreshNetworkSnapshotBeforeRecoveryPush|TestServerScenarioShutdownDuringInitialRuntimeScanDoesNotOpenControlListenerAfterRelease|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|TestServerScenarioShutdownWhileRefreshBindBlockedDoesNotAttachStaleListener)$' -timeout 240s ./internal/control"`（执行 1 次，命中另一处非确定性失败）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=5 -race -tags testhooks -run '^TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak$' -v -timeout 120s ./internal/control"`（执行 1 次，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=5 -tags testhooks -run '^TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak$' -v -timeout 120s ./internal/control"`（执行 1 次，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile gomaxprocs,config-churn,soak,platform,resource-pressure"`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./..."`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -tags testhooks ./..."`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frpc && go test ./..."`（通过）

本次在 Linux / WSL 上实际命中的失败如下：

- `go run ./cmd/stabilitymatrix -profile all`
  - 两次都在 `race` 档位的 `./internal/control` 核心场景集提前失败；
  - 失败用例为 `TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak`；
  - 失败信息为 `scan recovery during shutdown did not complete`。
- `go test -parallel=1 -count=5 -race -tags testhooks ... ./internal/control`
  - 在同一批 `control-core-race` 场景重复执行时，额外命中 `TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind` 的非确定性失败；
  - 失败信息为 `expected runtime issue in empty-config mode, got map[int64]string(nil)`。

本次确认并收束的边界如下：

- 当前 Linux / WSL 基线下，普通构建与 `testhooks` 视角的全模块回归都是绿的，说明常规功能路径没有出现普遍性回退。
- 当前 Linux / WSL 基线下，稳定性矩阵的非 `race` profile 都通过，说明大部分确定性场景、配置抖动、soak、资源压力和平台基线本身没有立即失稳。
- 当前真正阻塞“跨平台生产级结论”的不是功能全挂，而是 `race` 档位里的非确定性并发窗口仍未收干净，而且失败点至少不止一个。
- 在该问题收敛前，不能把“Windows 已通过”外推成“Linux 生产基线也已通过”。

## 11. 跨平台全量复验汇总结论：当前可继续灰度验证，但还不满足“跨平台生产放量”前置条件

截至 2026-04-23，当前轮“端口冲突检查 / 热更新交互”的跨平台全量复验已经完成，汇总结论如下：

- Windows 基线已通过：
  - `frps` 稳定性矩阵 `all` 通过；
  - `frps` 普通全模块回归通过；
  - `frps` `testhooks` 全模块回归通过；
  - `frpc` 全模块回归通过。
- Linux / WSL 基线部分通过、部分失败：
  - `frps` 普通全模块回归通过；
  - `frps` `testhooks` 全模块回归通过；
  - `frpc` 全模块回归通过；
  - `frps` 稳定性矩阵中的 `gomaxprocs`、`config-churn`、`soak`、`platform`、`resource-pressure` 通过；
  - `frps` 稳定性矩阵中的 `race` 档位存在非确定性失败，当前未收敛。

基于本次全量复验，当前发布判断收束如下：

- 如果目标是继续在 Windows 环境做灰度、联调或定向验证，当前代码树可以继续推进。
- 如果目标是给出“Windows / Linux 都达到生产放量标准”的统一结论，当前还不能通过。
- 当前最需要处理的不是补更多随机压力，而是先把 Linux `race` 下暴露出来的两个非确定性窗口定位并收掉：
  - `TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak`
  - `TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind`

本次汇报文档边界如下：

- 本轮只完成测试执行、失败归档和发布判断，不修改业务实现。
- 后续若继续推进，应直接进入 Linux `race` 非确定性失败的定位与修复，而不是重复扩大现有通过范围。

## 12. Linux `race` 非确定性失败已收敛，修正“旧版本 listener 结果覆盖新状态”与“shutdown 后继续恢复 stale listener”的真实缺陷

截至 2026-04-23，前述 Linux / WSL `race` 档位里阻塞跨平台结论的两个非确定性窗口已经完成定位、修复并复验通过：`TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind` 与 `TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak` 当前均已稳定收敛。

本次确认并落地的实现修复如下：

- `frps/internal/control/server.go`
  - 将 `tunnelRuntimeIssues` 从“仅存 reason 字符串”收紧为“`reason + configVersion`”的内部状态；
  - 新增 `recordTunnelRuntimeIssueForConfig()`，使 runtime issue 的写入 / 清理带上快照版本，旧版本 listener 启动晚到时不再清掉新版本已记录的异常。
- `frps/internal/control/listeners.go`
  - `ensureTunnelListeners()` 在写入 / 清理 runtime issue 时统一带上 `snapshot.Version`；
  - 在 listener 恢复路径上新增 `shutdown/session done` 门闩：
    - 若服务端已进入 `Shutdown()`
    - 或当前 session 已结束
    - 则不再继续挂接或放行后续 stale listener。
- `frps/internal/control/runtime_scan.go`
  - `applyScannedTunnelRuntimeIssues()` 改为按 `snapshot.Version` 更新 runtime issue；
  - `recoverScannedActiveSessionTunnels()` 在实际推进恢复前新增 `shutdown/session done` 检查，避免关服过程中仍继续走 scan recovery。
- `frps/internal/control/refresh.go`
  - `effective_ip` 失效并收缩为空配置时，记录 runtime issue 也改为绑定目标 `snapshot.Version`，避免旧版本 listener 恢复结果反向覆盖新版本异常态。
- `frps/internal/control/session.go`
  - 新增 `isDone()`，供 listener 恢复与 scan recovery 在关服 / 会话结束窗口内快速判停。
- `frps/internal/control/observe.go`
  - 适配新的 runtime issue 内部存储结构，外部观测面仍维持原有字符串口径，不扩大测试 / 管理态接口表面。

本次修复后明确收束的边界如下：

- 当旧版本 listener 启动结果晚于新版本 `effective_ip` 收缩 / 恢复状态到达时，旧结果不会再把新版本 runtime issue 清空。
- 当 `Shutdown()` 与 scan recovery bind 交错时，恢复路径会在 attach / 启动前及时止损，不再把 stale listener 挂回已退出会话。
- 本次修复没有引入新的测试 helper、额外 debug 输出或外部接口变更；当前仍保持原有管理态观测口径。

本次执行的定点验证如下：

- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=20 -race -tags testhooks -run '^(TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak)$' -timeout 180s ./internal/control"`（通过）
- `go test -parallel=1 -count=10 -race -tags testhooks -run "^(TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak)$" -timeout 180s ./internal/control`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -parallel=1 -count=20 -race -tags testhooks -run '^(TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|TestServerScenarioReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|TestServerScenarioReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak)$' -timeout 180s ./internal/control"`（通过）
- `go test -parallel=1 -count=10 -race -tags testhooks -run "^(TestServerScenarioRecoversEmptyConfigOnlyAfterAckThenListenerBind|TestServerScenarioShutdownWhileScanRecoveryBindBlockedLeavesNoListenerLeak|TestServerScenarioReleasingBlockedStartupBindAfterDisableRefreshDoesNotAttachStaleListener|TestServerScenarioReleasingBlockedStartupBindAfterGroupDeletionLeavesNoListenerLeak)$" -timeout 180s ./internal/control`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile race"`（通过）
- `go run ./cmd/stabilitymatrix -profile race`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./internal/control"`（通过）
- `go test ./internal/control`（目录：`frps/`，Windows，通过）
- `go test -tags testhooks ./internal/control`（目录：`frps/`，Windows，通过）
- 检查 `.gitignore`，本次无需新增忽略项

## 13. 跨平台最终复验已重新收口，当前代码树通过 `frps` 稳定性矩阵 `all` 与双模块全量回归

截至 2026-04-23，在上述 Linux `race` 修复落地后，已重新串行补跑 Windows 与 Linux / WSL 两侧的稳定性矩阵 `all`、`frps` 全模块回归、`frps` `testhooks` 全模块回归以及 `frpc` 全模块回归。当前所有入口均已通过，前文第 11 节里“尚不满足跨平台生产放量前置条件”的判断已被本节结果更新。

本次完成的验证如下：

- `go run ./cmd/stabilitymatrix -profile all`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go run ./cmd/stabilitymatrix -profile all"`（通过）
- `go test ./...`（目录：`frps/`，Windows，通过）
- `go test -tags testhooks ./...`（目录：`frps/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./..."`（通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test -tags testhooks ./..."`（通过）
- `go test ./...`（目录：`frpc/`，Windows，通过）
- `wsl.exe bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frpc && go test ./..."`（通过）

本次确认并收束的边界如下：

- 当前 Windows 与 Linux / WSL 下，`frps` 稳定性矩阵 `all` 已同时通过，说明 `race`、多 `GOMAXPROCS`、高频配置抖动、长稳 soak、平台基线和资源压力 6 类入口当前都没有新增失败信号。
- 当前 Windows 与 Linux / WSL 下，`frps` 普通构建视角和 `testhooks` 视角的全模块回归均通过，说明本轮并发修复没有破坏常规功能路径，也没有把测试 seam 带坏。
- 当前 Windows 与 Linux / WSL 下，`frpc` 模块总回归通过；本轮服务端并发修复未向客户端引入直接回退信号。
- 以当前代码树为准，端口冲突检查 / 热更新交互主线已经达到“跨平台稳定性矩阵 + 双模块总回归均为绿”的收口状态。

基于本次最终复验，当前发布判断更新如下：

- 如果目标是继续在 Windows / Linux 环境推进灰度、联调或生产前放量验证，当前代码树可以继续推进。
- 若后续再出现回退，应优先按“具体 profile / 具体场景 / 具体平台”归档，而不再沿用本节之前“Linux `race` 未收敛”的旧判断。

## 14. 客户端凭据模型已完成不兼容切换，`frpc` / `frps` / 管理面统一收口到 `client_id + client_secret`

截至 2026-04-23，当前轮“客户端凭据命名与不兼容切换”这个子步骤已经完成：客户端登录、服务端存储、管理 API 和 WebUI 已从旧 `token` 术语整体切到 `client_id + client_secret + client_secret_hash`，并明确按不兼容口径执行，没有保留旧字段、旧路径或双写层。

本次完成的实现如下：

- `frpc`
  - `frpc/cmd/frpc/main.go`
    - CLI 启动参数从 `--token` 改为 `--client-id` 与 `--client-secret`。
  - `frpc/internal/config`
    - 配置模型从单字段 `token` 改为显式 `client_id + client_secret`。
    - 固定长度解析逻辑拆成 `ParseClientID()`、`ParseClientSecret()` 和 `ParseCredentials()`。
  - `frpc/internal/client`
    - 登录握手改为发送 `AuthBegin.ClientID`。
    - challenge 响应改为基于 `sha256(client_secret)` 计算。
    - 客户端测试统一改用新凭据模型。
- `frps`
  - `frps/pkg/protocol`
    - `AuthBegin.TokenID` 改为 `AuthBegin.ClientID`。
    - 认证错误码常量命名切为 `ErrorCodeAuthInvalidClient`。
  - `frps/internal/control`
    - 仓储接口改为 `LoadGroupRuntimeByClientID()`。
    - `GroupRuntime.TokenHash` 改为 `GroupRuntime.ClientSecretHash`。
    - 凭据轮转判定改为比较 `client_secret_hash`，并统一使用“credential rotation”语义。
  - `frps/internal/app/schema.go`
    - `proxy_groups.token_id/token_hash` 改为 `client_id/client_secret_hash`。
    - 当前仍保持“无迁移、无兼容层”的 schema 校验策略。
  - `frps/internal/api/management.go`
    - 分组展示字段改为 `client_id`。
    - 创建分组与轮转凭据统一返回 `credential` 对象。
    - 凭据轮转只更新 `client_secret_hash`，不再更换 `client_id`。
    - 轮转接口路径改为 `POST /api/v1/proxy-groups/{id}/credentials`。
- `frps/webui`
  - 分组列表与详情展示改为 `Client ID`。
  - 创建分组 / 轮转密钥弹窗改为展示 `client_id` 与一次性 `client_secret`。
  - 前端 API 类型与调用路径同步到 `credential` / `/credentials`。

本次收束后的边界如下：

- 当前不保留 `token` 兼容字段、兼容 CLI 参数、兼容数据库列名、兼容管理 API 返回结构，也不承诺新旧版本混跑。
- `client_id` 现在是稳定公开标识；轮转凭据只更新 `client_secret` / `client_secret_hash`。
- 管理登录认证模型本轮未改，仍保持 `auth.json + challenge + session cookie` 的独立机制。

本次执行的验证如下：

- `go test ./...`（目录：`frpc/`）
- `go test ./...`（目录：`frps/`）
- `npm run build`（目录：`frps/webui/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 15. 客户端登录外部 UX 已重新收口到单个 `key`，恢复一次性整串复制

截至 2026-04-23，在第 14 节完成内部凭据命名切换后，当前轮又把客户端登录的外部输入输出形态进一步收口：`frpc` 不再要求分别输入 `client_id` / `client_secret`，而是恢复为单个 `key`；`frps` 管理端创建分组和轮转登录信息时，也恢复为一次性展示整串 `key`，不再拆成两段复制。

本次完成的实现如下：

- `frpc`
  - `frpc/cmd/frpc/main.go`
    - CLI 登录参数改为单个 `-key`。
  - `frpc/internal/config`
    - 配置模型对外改为 `Config.Key`。
    - 新增 `ParseKey()` / `ComposeKey()`，校验固定长度 `client_id + client_secret` 拼接串。
  - `frpc/internal/client`
    - 客户端启动时统一从单个 `key` 解析出登录所需的 `client_id` 与 `client_secret`。
    - 客户端测试夹具同步改成单个 `key`。
- `frps`
  - `frps/internal/api/management.go`
    - 创建分组与轮转登录信息的返回体从拆分 `credential` 改为单个 `key`。
    - 轮转路径从 `POST /api/v1/proxy-groups/{id}/credentials` 改为 `POST /api/v1/proxy-groups/{id}/key`。
    - 服务端内部仍保持 `client_id + client_secret_hash` 的存储与认证边界，不把整串 `key` 落库。
- `frps/webui`
  - 前端 API 类型与调用路径同步到 `key` / `/key`。
  - 创建分组与轮转密钥弹窗恢复为单串展示、单按钮复制。
  - 弹窗文案明确客户端通过 `frpc -key` 使用该值。

本次收束后的边界如下：

- 当前不提供旧 `--client-id` / `--client-secret` CLI 兼容，也不保留旧 `credential` / `/credentials` 管理接口兼容。
- 外部 UX 统一为单个复制的 `key`；内部协议、数据库和认证实现仍保持 `client_id` 与 `client_secret_hash` 分层。
- 分组列表与详情中仍保留 `client_id` 作为稳定可见标识；一次性敏感登录信息仅在创建/轮转后通过整串 `key` 暴露一次。

本次执行的验证如下：

- `go test ./...`（目录：`frpc/`）
- `go test ./...`（目录：`frps/`）
- `npm run build`（目录：`frps/webui/`）
- 检查 `.gitignore`，本次无需新增忽略项

## 16. `sessionState` 第一刀重构已完成，配置态 / 运行态边界已拆开并保住当前 Windows 基线

截至 2026-04-23，当前轮 `frps/internal/control` 第一阶段重构的第一刀已经完成：本轮把 `sessionState` 里原先混放的配置态与运行态正式拆开，并把 `refresh`、`runtime_scan`、`listeners`、`observe` 等主要调用面收口到新的最小访问入口；当前未引入新功能，但已经为后续 `runtime issue store`、`runtime registry`、管理面连接视图和连接治理挂点预留了更稳定的内部结构。

本次完成的实现如下：

- `frps/internal/control/session.go`
  - 新增 `sessionConfigState`，配置态只保留：
    - `current`
    - `pending`
    - `acked`
    - `recoveryMode`
  - 新增 `sessionRuntimeState`，运行态只保留：
    - `done`
    - `frozen`
    - `generation`
    - `listeners`
    - `streams`
    - `udp`
  - 新增统一构造入口 `newSessionState()`，把 session 初始化、map 创建和 `nextServerRequestID` 初始值收口到一个地方。
  - 新增并收紧一组最小访问方法，包括：
    - `currentGroupID()`
    - `currentGroupAndSnapshot()`
    - `currentSnapshot()`
    - `observeState()`
    - `canStartTunnelRuntime()`
    - `resetRuntimeGenerationIfIdle()`
  - TCP stream、UDP session、listener attach/freeze/IO 门闩逻辑全部切到新的运行态子结构上，但保持原有并发语义不变。
- `frps/internal/control/auth.go`
  - 登录成功后的 session 创建改为统一走 `newSessionState()`。
- `frps/internal/control/config.go`
  - `config.ack` 路径不再直接摸 session 内部字段，改为通过配置态访问方法取当前/已接受配置。
  - 原有 `testhooks` 点位名称保持不变。
- `frps/internal/control/refresh.go`
  - active session 注册/注销改为通过 `currentGroupID()` 取分组归属，不再直接依赖混放字段。
- `frps/internal/control/listeners.go`
  - listener 恢复入口不再直接读写 `runtimeFrozen` / `listenersStarted` / `runtimeGeneration`，改为走新的运行态访问方法。
- `frps/internal/control/observe.go`
  - 观测面改为通过 `observeState()` 一次性抓取配置态 / 运行态快照，避免继续横跨 session 内部多组字段。
- `frps/internal/control/server.go`
  - 登录成功日志和 frame context 改为通过当前配置态读取 group / snapshot 信息。
- `frps/internal/control/udp.go`
  - UDP session 绑定、复用、回收全部切到新的 `runtime.udp` 子结构。
- `frps/internal/control/runtime_status_test.go`
- `frps/internal/control/server_test.go`
  - 测试同步适配新的嵌套状态结构，保持原有断言语义不变。

本次确认并收束的边界如下：

- 当前只做 `sessionState` 第一刀结构拆分与访问入口收口，不拆 `control` 子包，不引入事件总线、插件系统、状态机 DSL 或 DI 容器。
- 当前没有直接引入 `runtime issue store` / `runtime registry` 实现，但命名和状态边界已经按后续可接入方向收口。
- 当前保留了既有恢复判定、竞争窗口处理、listener attach/freeze、UDP cleanup 和关闭/接管语义；这一步没有改外部协议、管理登录模型或单个 `key` UX。
- 当前保留既有 `testhooks` 点位名称，避免把 scenario 测试依赖的屏障命名一起打散。
- 本次未重新补跑 WSL/Linux 的全量矩阵；新增验证重点放在当前 Windows 环境下的全量回归和 `frps/internal/control` 定向 `-race`。

本次执行的验证如下：

- `go test ./internal/control`（目录：`frps/`）
- `go test -race ./internal/control`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frpc/`，Windows）
- `npm run build`（目录：`frps/webui/`，Windows）
- 检查 `.gitignore`，本次无需新增忽略项

## 17. `runtime issue store` / `runtime registry` 第二刀已完成，运行态问题存储与 active session 读面已集中收口

截至 2026-04-23，当前轮 `frps/internal/control` 第一阶段重构的第二刀已经完成：本轮把原先挂在 `Server` 上的 runtime issue 裸 map 与 active session / group slot 裸 map 正式收成 `runtime issue store` 与 `runtime registry` 两个内部对象，并把 `observe`、`runtime_scan`、`refresh`、`listeners` 等主要入口切到新的集中边界上；当前未引入新功能，但已经进一步为后续管理面、连接观察、连接治理和运行态扩展挂点压实基础。

本次完成的实现如下：

- `frps/internal/control/runtime_issue_store.go`
  - 新增 `runtimeIssueStore`：
    - 集中维护 tunnel runtime issue 的读写；
    - 保留并内聚 `configVersion` 保护语义；
    - 收口 `clearTunnels()`、`clearUnknown()`、`applyScanResult()` 等批量操作。
  - `Server.TunnelRuntimeIssues()` 与现有 `record/clear` 包装方法改为统一委托给该对象。
- `frps/internal/control/runtime_registry.go`
  - 新增 `runtimeRegistry`：
    - 集中维护 active session 注册表；
    - 集中维护 `group slot` 占用；
    - 提供 `snapshot()` 与 `activeRuntimeGroups()` 两类统一读模型。
  - `reserveGroupSlot()`、`releaseGroupSlot()`、`registerActiveSession()`、`unregisterActiveSession()`、`activeSession()`、`lockCurrentActiveSession()` 全部改为统一委托给该对象。
  - 新增 `runtimeSessionSnapshot` / `runtimeRegistrySnapshot` 作为当前统一的 active runtime 读模型雏形。
- `frps/internal/control/server.go`
  - `Server` 不再直接持有 `sessions`、`groupSlots`、`tunnelRuntimeIssues` 三个裸 map；
  - 改为持有 `runtimeIssues` 与 `runtimeRegistry` 两个内部对象。
- `frps/internal/control/observe.go`
  - 观测面改为统一从 `runtimeRegistry.snapshot()` 与 `runtimeIssueStore` 快照组装；
  - `observeSessionState()` 改为消费 `runtimeSessionSnapshot`，不再自己从 live session / activeSession 临时拼读。
- `frps/internal/control/listeners.go`
  - runtime 端口冲突检查改为统一依赖 `activeRuntimeGroups()` 提供的 registry 读模型；
  - 删除原先散落在 listener 启动路径里的 active runtime group 组装逻辑。
- `frps/internal/control/runtime_scan.go`
  - 扫描结果回写改为统一经由 `runtime issue store`；
  - 删除原先散落在扫描文件里的 issue map 清理 / 应用实现。
- `frps/internal/control/refresh.go`
  - 删除原先直接挂在 `Server` 上的 active session / group slot map 管理实现；
  - 刷新路径继续沿用原有行为，但底层注册表边界已切换。
- `frps/internal/control/auth.go`
  - 登录占槽逻辑改为走新的 registry 包装入口，不再直接操作 `Server` 内部 map。
- `frps/internal/control/competition_window_scenario_test.go`
  - 适配新的 group slot 管理入口，不再直接写 `Server` 内部 slot map。
- `frps/internal/control/runtime_boundary_test.go`
  - 新增针对第二刀边界的定向测试：
    - `TestRuntimeIssueStoreKeepsNewerConfigVersion`
    - `TestRuntimeRegistryBuildsSnapshotAndActiveRuntimeGroups`

本次确认并收束的边界如下：

- 当前只做第二刀“状态集中收口”，不拆 `control` 子包，不引入事件总线、插件系统、状态机 DSL 或 DI 容器。
- 当前不改变现有恢复判定、竞争窗口处理、`testhooks` 语义、管理登录模型或单个 `key` 外部 UX。
- `runtime issue store` 现在成为 tunnel runtime issue 的唯一内部规则承载点；后续若增加更多 runtime issue 类型或管理面展示口径，应继续沿这个对象扩展，而不是重新散回 `listeners` / `runtime_scan` / `refresh`。
- `runtime registry` 现在成为 active session / group slot / runtime 快照的统一读写边界；后续若增加“查看所有连接”“连接级操作”“连接统计”或其他运行态读模型，应继续沿这个 registry 扩展。
- 本次实现里还顺手修掉了一处真实死锁窗口：
  - `scan recovery` 持有当前 `activeSession.mu` 时，新的 registry snapshot 不能再反向锁同一个 `activeSession`；
  - 当前 snapshot 路径已改成只复制稳定引用并读取 session 快照，避免恢复路径把自己锁死。
- 本次未重新补跑 WSL/Linux 的稳定性矩阵；新增验证重点仍放在当前 Windows 环境下的 `control` 定向回归、`-race` 与双模块全量基线。

本次执行的验证如下：

- `go test ./internal/control -run TestServerEnsureTunnelListenersDetectsRuntimeConflictWithActiveGroup -count=1`（目录：`frps/`，Windows）
- `go test ./internal/control -run TestServerScanNonListeningTunnelRuntimeIssuesScansActivePartialGroupAndRecoversTunnel -count=1 -v -timeout 60s`（目录：`frps/`，Windows）
- `go test ./internal/control`（目录：`frps/`，Windows）
- `go test -race ./internal/control`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frpc/`，Windows）
- `npm run build`（目录：`frps/webui/`，Windows）
- 检查 `.gitignore`，本次无需新增忽略项

## 18. `runtime coordinator` 第三刀已完成，`refresh` / `runtime_scan` 的恢复动作编排已收口到统一 action plan

截至 2026-04-23，当前轮 `frps/internal/control` 第一阶段重构的第三刀已经完成：本轮把 `RefreshGroup()` 与 scan recovery 里原先各自拼接的恢复动作抽成统一的 `runtime coordinator` / `runtime recovery plan`，把“读状态决定做什么”和“真正执行恢复动作”继续分离开，同时保住了当前 Windows 基线与既有恢复语义。

本次完成的实现如下：

- `frps/internal/control/runtime_coordinator.go`
  - 新增 `runtimeRecoveryAction`，统一表达当前恢复动作模型：
    - `keep_runtime`
    - `ensure_listeners`
    - `rebind_runtime`
    - `push_empty_config`
    - `push_full_config`
    - `close_session`
  - 新增 `runtimeRecoveryPlan`，把恢复路径先收成“单个动作计划”，再统一执行。
  - 新增 `runtimeRecoveryTarget`，收口执行动作时需要的当前 session / conn / logger / 当前配置快照。
  - 新增 `runtimeCoordinator.planRefresh()` 与 `planScannedRecovery()`，把 `refresh` 与 `runtime_scan` 的动作判定统一落到同一处。
  - 新增 `runtimeCoordinator.execute()`，统一承接 `freeze`、listener 补建、rebind、full-config push、empty-config push 和保守关会话动作。
- `frps/internal/control/refresh.go`
  - `RefreshGroup()` 现在只保留：
    - active session 装载
    - group runtime 读取 / 删除 / credential rotation 判定
    - 构建 recovery target
    - 交给 coordinator 产出 plan 并执行
  - 原先散落在 `RefreshGroup()` 里的 pending 去重、empty/full push、rebind 分支已收口到 coordinator。
- `frps/internal/control/runtime_scan.go`
  - `recoverScannedActiveSessionTunnels()` 现在改为先生成 scanned recovery plan，再统一执行。
  - 同一恢复语义不再继续由 `refresh` 与 `runtime_scan` 各自维护一份动作拼装逻辑。
- `frps/internal/control/runtime_coordinator_test.go`
  - 新增 coordinator 定向边界测试，锁住第三刀新增的动作判定，包括：
    - matching pending config 去重
    - 不同 pending config 保守关会话
    - 仅 `effective_ip` 变化走 rebind
    - `effective_ip` 失效优先 shrink 到 empty config
    - scan recovery 补 listener
    - scan recovery 从 empty config 回推 full config

本次实现过程中还确认并修正了一处第三刀引入过程中的真实回归风险：

- 当 `effective_ip` 失效且 runtime snapshot 本身未变化时，动作判定必须优先命中 `push_empty_config`，不能先落到“同配置但 IP 变化”的 `rebind_runtime`。
- 当前该优先级已经在 coordinator 里固定住，并由新增单测与现有 server 用例共同覆盖。

本次确认并收束的边界如下：

- 当前第三刀只收口 `refresh` / `runtime_scan` 的恢复编排，不改 `handleConfigAck()`、`ensureTunnelListeners()` 内部 listener 启动细节，也不拆 `control` 子包。
- 当前不改协议、不改管理登录模型、不改外部单个 `key` UX，也不改变既有 `testhooks` 点位与语义。
- `runtime coordinator` / `action` / `plan` 的命名已经为后续管理面、查看所有连接、连接级操作、抓包、令牌桶限速和连接治理预留统一挂点；但这些能力本轮不直接实现。
- 当前 `refresh`、`runtime_scan` 只负责给出恢复计划；具体恢复动作仍复用现有 `freezeGroupRuntime()`、`rebindGroupRuntime()`、`ensureTunnelListeners()`、`pushReloadConfig()`。

本次执行的验证如下：

- `go test ./internal/control`（目录：`frps/`，Windows）
- `go test -race ./internal/control`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frpc/`，Windows）
- `npm run build`（目录：`frps/webui/`，Windows）
- 检查 `.gitignore`，本次无需新增忽略项

## 19. `config apply / runtime operation` 第四刀已完成，`config.push/config.ack`、listener startup/probe 与会话观测读面已继续收口

截至 2026-04-23，当前轮 `frps/internal/control` 第一阶段重构的第四刀已经完成：本轮把 `config.push` / `config.ack` 的配置生效边界继续收口到 session 内部 apply 操作，把 `ensureTunnelListeners()` 继续拆成“运行态启动计划 + 运行态执行”，并把会话观测从直接暴露 listener map 收成更稳定的 attached listener / active tunnel 读模型；当前未改外部行为，但已经进一步为后续管理面、查看所有连接、抓包、限速和连接治理预留了更清晰的内部接缝。

本次完成的实现如下：

- `frps/internal/control/session.go`
  - 新增 `prepareConfigPush()`，把 pending config 写入与 pending recovery mode 更新收口到 session 内部。
  - `acceptConfigAck()` 改为直接返回 `sessionConfigApplyResult`，并在 session 内统一推进 applied recovery mode。
  - 新增 `pendingRecoveryModeForSnapshot()` / `appliedRecoveryModeForSnapshot()`，避免 `config.go`、`runtime_coordinator.go` 再各自手写 recovery mode 迁移。
  - `observeState()` 改为输出：
    - `activeTunnelIDs`
    - `attachedListeners`
    - `generation`
    - `listenersStarted`
    - `frozen`
  - 不再把 runtime 观测读面建立在 `tcpListeners` / `udpListeners` 裸 map clone 上。
- `frps/internal/control/config.go`
  - `pushReloadConfig()` 现在改为复用 `session.prepareConfigPush()`，不再由 `config.go` 自己决定 pending recovery mode。
  - `handleConfigAck()` 现在改为复用 `session.acceptConfigAck()` 返回的 apply 结果，再统一触发 runtime start；`config.go` 不再自己拼 current snapshot / recovery mode 迁移。
  - 新增 `applyAcceptedConfig()` 作为 `config.ack -> runtime start` 的独立接缝。
- `frps/internal/control/runtime_operation.go`
  - 新增 `sessionRuntimeStartTarget` / `sessionRuntimeStartPlan`。
  - 新增 `tunnelListenerOperationContext`，统一封装 runtime start / runtime probe 所需的 `group_id`、`config_version`、`tunnel`、`bind_ip` 与 `BindKind`。
  - 新增 `tunnelRuntimeServeContext`，统一封装 listener 启动后进入 TCP/UDP 运行态处理所需的上下文。
- `frps/internal/control/listeners.go`
  - `ensureTunnelListeners()` 现在只负责：
    - 生成 `sessionRuntimeStartPlan`
    - 执行该 plan
  - 原先散在一个函数里的“目标 tunnel 选择、issue 清理、effective_ip 判定、冲突探测、listener 启动、attach、serve 启动”已被继续拆开。
  - `startTunnelListeners()` 改为接收 `tunnelListenerOperationContext`，不再直接吃一串裸参数。
  - `serveTunnelListener()` / `serveUDPTunnelListener()` 改为接收 `tunnelRuntimeServeContext`，为后续连接级 hook、抓包和限速预留稳定挂点。
- `frps/internal/control/tcp_bridge.go`
  - `handlePublicConnection()` 改为接收 `tunnelRuntimeServeContext`，减少控制连接、logger、session、tunnel 元信息的散落传参。
- `frps/internal/control/udp.go`
  - `handlePublicUDPDatagram()` 改为接收 `tunnelRuntimeServeContext`，与 TCP 路径保持一致的 runtime serve 上下文。
- `frps/internal/control/runtime_scan.go`
  - `probeTunnelRuntimeIssue()` 现在改为复用 `tunnelListenerOperationContext`，不再单独手拼 probe bind 上下文。
- `frps/internal/control/observe.go`
  - 观测面改为消费 `attachedListeners` / `activeTunnelIDs` 读模型，不再直接依赖 raw listener map。
- `frps/internal/control/runtime_registry.go`
  - active runtime group 的判定改为复用 `activeTunnelIDs` 读模型，不再自己再次遍历 listener map。
- `frps/internal/control/runtime_coordinator.go`
  - `push_empty_config` / `push_full_config` 动作不再重复手写 pending recovery mode，统一交回 `pushReloadConfig()` 的 apply 边界。

本次新增与补强的测试如下：

- `frps/internal/control/runtime_boundary_test.go`
  - 新增 `TestSessionConfigApplyTracksPendingAndAppliedRecoveryModes`
    - 锁住 pending full / pending empty 与 ack 后 applied recovery mode 的迁移边界。
  - 新增 `TestServerPlanSessionRuntimeStartSeparatesActiveAndPendingTunnels`
    - 锁住第四刀新增的 runtime start plan：active tunnel、disabled tunnel 与真正待启动 tunnel 的分流边界。
  - 扩展 `TestRuntimeRegistryBuildsSnapshotAndActiveRuntimeGroups`
    - 进一步锁住新的 `activeTunnelIDs` / `attachedListeners` 读模型。
- `frps/internal/control/listener_factory_test.go`
  - 适配新的 `tunnelListenerOperationContext` 启动入口。
- `frps/internal/control/server_test.go`
  - 适配新的 `tunnelRuntimeServeContext` UDP 入口。

本次确认并收束的边界如下：

- 当前第四刀只继续收口 `config apply`、listener startup/probe 和 session 观测读面，不拆 `control` 子包，不引入事件总线、插件系统、状态机 DSL 或 DI 容器。
- 当前不改变既有恢复判定、竞争窗口处理、`testhooks` 语义、管理登录模型或外部单个 `key` UX。
- 当前新增的 `tunnelRuntimeServeContext` 只是收口内部上下文，不直接实现抓包、令牌桶限速、连接级操作或管理 API；这些能力留到后续刀继续接。
- 当前 `observe` / `runtime registry` 已经不再依赖原始 listener map 作为稳定读面；后续如果补“查看所有连接”“连接治理”“连接统计”，应继续沿 attached listener / active tunnel / connection 读模型扩展，而不是重新回退到 live map 拼读。
- 本次验证重点仍放在当前 Windows 环境下的 `control` 定向回归、`-race`、`frps` / `frpc` 全量与 WebUI 构建；本轮未重新补跑 WSL/Linux 稳定性矩阵。

本次执行的验证如下：

- `go test ./internal/control`（目录：`frps/`，Windows）
- `go test -race ./internal/control`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frpc/`，Windows）
- `npm run build`（目录：`frps/webui/`，Windows）
- 检查 `.gitignore`，本次无需新增忽略项

## 20. `connection / session io` 第五刀已完成，public stream / UDP session / runtime I/O 与连接读面已继续收口

截至 2026-04-23，当前轮 `frps/internal/control` 第一阶段重构的第五刀已经完成：本轮把 `handlePublicConnection()`、`handlePublicUDPDatagram()`、`copyPublicToClient()` 与 session stream / UDP 状态之间原先仍然交叉的编排继续收成统一的 `connection / session io` 边界，并把会话级连接读面补成稳定模型；当前未改外部协议和用户行为，但已经把后续管理面“查看所有连接”“连接级操作”、抓包、限速和连接治理所需的 runtime I/O 接缝再往前压实了一步。

本次完成的实现如下：

- `frps/internal/control/session_io.go`
  - 新增 `sessionRuntimeIOWriter`，把 runtime 数据面 frame 写入门闩从 `tcp_bridge.go` / `udp.go` 的裸 `conn + session + configVersion` 组合收成统一 writer。
  - 新增 `preparePublicStreamOpen()`，把 public TCP 连接进入 session、生成 `stream.open` 帧和 admission 判定集中到 session 内。
  - 新增 `preparePublicUDPDatagramForward()`，把 public UDP datagram 对应的 session 复用 / 新建、`udp.open + udp.data` 计划生成和 admission 判定集中到 session 内。
  - 新增 `observeRuntimeConnections()`，统一生成 stream / udp session 的稳定连接读模型。
- `frps/internal/control/runtime_operation.go`
  - `tunnelRuntimeServeContext` 继续收口为真正的数据面 serve 上下文，直接挂上 `runtimeIO` writer，不再把 control 连接、config version 裸散给各条路径。
- `frps/internal/control/listeners.go`
  - TCP / UDP listener 启动后进入运行态时，统一注入 `sessionRuntimeIOWriter`，为后续连接级 hook、抓包和限速保留单一写面。
- `frps/internal/control/tcp_bridge.go`
  - `handlePublicConnection()` 改为先向 session 申请 `sessionStreamOpenOperation`，再由 `runtimeIO` writer 执行 `stream.open`。
  - `copyPublicToClient()` 改为依赖 `sessionRuntimeIOWriter`，不再自己拼 `conn + session + configVersion`。
  - `publicStream` 新增 `remotePort`、`clientAddr`、`openedAtMs`、`lastActiveUnixMs`，为连接读面与后续策略挂点补齐元数据。
  - `handleStreamData()` 在写回 public 连接成功后更新 stream 活跃时间。
- `frps/internal/control/udp.go`
  - `handlePublicUDPDatagram()` 改为先向 session 申请 `sessionUDPDatagramForwardOperation`，再统一经 `runtimeIO` writer 下发 `udp.open / udp.data`。
  - `publicUDPSession` 新增 `openedAtMs`，并补齐统一的连接观测元数据输出。
- `frps/internal/control/session.go`
  - `observeState()` 继续扩展为输出：
    - `activeStreamCount`
    - `activeUDPSessionCount`
    - `connections`
  - 连接读面不再需要外部代码自己拼 `runtime.streams` / `runtime.udp.sessions`。
- `frps/internal/control/observe.go`
  - `ObserveState()` 继续消费 session 内部连接读模型，对外输出：
    - session 级 `ActiveStreams` / `ActiveUDPSessions`
    - server 级 `Connections`
- `frps/internal/control/sockaddr.go`
  - 新增 `sockAddrString()`，统一把 runtime 里的 `SockAddr` 元数据投影成稳定文本地址。
- `frps/pkg/testsupport/types.go`
  - 新增 `ConnectionObservedState`。
  - `SessionObservedState` 补充 `ActiveStreams` / `ActiveUDPSessions`。
  - `ServerObservedState` 补充 `Connections`。
- `frps/pkg/testsupport/clone.go`
  - 适配新的连接观测读面 clone 逻辑。

本次新增与补强的测试如下：

- `frps/internal/control/runtime_boundary_test.go`
  - 新增 `TestSessionPreparePublicStreamOpenBuildsFrameAndTracksConnectionMetadata`
    - 锁住 TCP public stream admission、`stream.open` 帧生成和连接读面元数据边界。
  - 新增 `TestSessionPreparePublicUDPDatagramForwardReusesSessionAndTracksConnectionMetadata`
    - 锁住 UDP session 首包打开、后续复用与连接读面元数据边界。
- `frps/internal/control/server_test.go`
  - 适配新的 `sessionRuntimeIOWriter` / `tunnelRuntimeServeContext` 入口。

本次确认并收束的边界如下：

- 当前第五刀只继续收口 public stream / UDP session / runtime I/O 与连接观测读面，不直接实现管理 API、WebUI 连接页、抓包、令牌桶限速或连接治理策略本身。
- 当前不改变既有恢复判定、竞争窗口处理、`testhooks` 语义、管理登录模型或外部单个 `key` UX。
- 当前新增的 `sessionRuntimeIOWriter` 和 `connections` 读模型属于内部稳定接缝；后续如需补“查看所有连接”“连接级关闭/限速/抓包”“连接统计”，应继续沿这两个边界扩展，而不是重新回退到 `tcp_bridge` / `udp` / `session` 裸状态拼装。
- 当前 session 连接读面已经能稳定区分 `tcp_stream` 与 `udp_session`，并携带 `tunnel_id`、`remote_port`、`client_addr`、`opened_at`、`last_active` 等基础元数据；当前仍未补连接级策略状态、抓包状态和限速状态。
- 本次验证重点仍放在当前 Windows 环境下的 `control` 定向回归、`-race`、`frps` / `frpc` 全量与 WebUI 构建；本轮未重新补跑 WSL/Linux 稳定性矩阵。

本次执行的验证如下：

- `go test ./internal/control`（目录：`frps/`，Windows）
- `go test -race ./internal/control`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frps/`，Windows）
- `go test ./...`（目录：`frpc/`，Windows）
- `npm run build`（目录：`frps/webui/`，Windows）
- 检查 `.gitignore`，本次无需新增忽略项


