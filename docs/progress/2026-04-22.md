# 2026-04-22

## 1. 端口冲突判定矩阵与共享冲突模型已收口

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 1 个子步骤已经完成：端口冲突检测的共享判定模型、比较矩阵、触发入口和预计代码落点已经收口，后续实现统一按同一套监听占位语义推进。

本次已完成的规则收束如下：

- 端口冲突检测的核心对象不是原始 `tunnel` 文本，而是“会声明一个公网 listener 占位”的监听声明；后续管理面和控制面都按同一模型比较，不各写一套规则。
- 监听声明的最小比较维度已经固定为：`protocol`、`effective_ip`、`remote_start`、`remote_end`，以及用于报错定位的 `group_id` / `tunnel_id`；`single` 只是 `remote_start == remote_end` 的特例，`range` 不再展开成逐端口数组后再比较。
- 管理面口径已经固定为“配置占位”而不是“当前在线会话占位”：只要分组和隧道都处于启用态，就视为该声明会占用对应公网监听空间，不要求 `frpc` 已在线。
- 因此，禁用分组或禁用隧道当前不参与占位；但凡 `group.enabled`、`group.effective_ip`、`tunnel.enabled`、`tunnel.protocol`、`tunnel.remote_start`、`tunnel.remote_end` 发生变化，都必须重新校验受影响的监听声明集合。
- 协议维度已经收口为完全隔离：`tcp` 只与 `tcp` 比较，`udp` 只与 `udp` 比较，二者不共享同一冲突空间。
- 端口区间比较规则已经固定：
  `single-single` 看端口是否相同；
  `single-range` 看单点是否落入区间；
  `range-range` 看两个区间是否相交。
- IP 比较规则已经固定为“按监听空间是否重叠”：
  具体 IPv4 只与相同 IPv4 冲突；
  具体 IPv6 只与相同 IPv6 冲突；
  `0.0.0.0` 与全部 IPv4 具体地址冲突；
  `::` 与全部 IPv6 具体地址冲突。
- `0.0.0.0` 与 `::` 之间额外固定为冲突关系：虽然它们在配置语义上分属 IPv4 / IPv6 wildcard，但当前 Go/Windows 环境下两者会落到同一类 wildcard socket 资源，第二次绑定会直接失败；为了避免管理面放过、控制面再报底层 bind 错误，本轮统一按冲突处理。
- 具体 IPv4 与具体 IPv6、具体 IPv4 与 `::`、具体 IPv6 与 `0.0.0.0` 当前不按静态冲突处理；这部分继续按地址族语义隔离，不因为当前平台可能出现 dual-stack 行为就扩大到全部跨族冲突。
- 为保持配置行为可预期，`0.0.0.0` 与具体 IPv4、`::` 与具体 IPv6 当前仍按冲突处理；即使某些平台/调用顺序下底层 socket 允许 wildcard 与 specific 同端口并存，管理面也不接受这类重叠配置。
- 共享 helper 的预计落点已经固定为新的 `frps/internal/ports` 包：管理面负责把数据库里的分组/隧道投影成监听声明，控制面负责把 `GroupRuntime` / `ConfigSnapshot` 投影成监听声明，底层冲突比较函数本身不依赖 `api` 或 `control` 包。
- 共享 helper 的输出形态已经收口为“返回冲突双方及原因”，而不是只返 `bool`；后续管理面需要把它转成稳定的 API 错误文案，控制面则需要把它转成结构化日志和运行态 bind 失败语义。
- 管理面第一批必须接入的入口已经确认：`createTunnel()`、`updateTunnel()`、`updateProxyGroup()`；其中 `updateTunnel()` 必须忽略自身旧记录，`updateProxyGroup()` 在 `effective_ip` 或 `enabled` 变化时必须按整组隧道重算。
- 控制面第一批必须接入的入口已经确认：`ensureTunnelListeners()` 和 `effective_ip` 本地重绑路径；即使管理面静态校验已经兜住，控制面仍要把“外部进程占用端口”或“极端竞态下的运行态冲突”收敛成明确的端口冲突错误，而不是直接透传底层 `listen` 文案。

本次固定下来的边界如下：

- 当前轮只处理 `frps` 公网 listener 端口冲突，不处理 `frpc` 本地目标端口冲突。
- 当前轮优先保证“配置层冲突”与“运行态 bind 冲突”的口径一致，不在第一步引入前端侧预测逻辑或额外的持久化字段。
- 当前轮不把“操作系统是否偶然允许 overlap bind”当作配置合法性的标准；配置层一律按监听空间语义和已确认的 wildcard 特判来收口。
- 当前轮仍然不会让“分组是否在线”影响管理面冲突判定；否则同一份配置在离线时可写、上线时失败，行为不可预测。

本次执行的验证如下：

- 本地 Go 绑定实验：对 `tcp` / `udp` 分别验证 `0.0.0.0`、`[::]`、`127.0.0.1`、`[::1]` 的同端口绑定关系，确认当前 Go/Windows 环境下 `0.0.0.0` 与 `[::]` 互相冲突，而 wildcard 与 specific 在部分场景下可共存。
- `git diff --check -- docs/progress/2026-04-22.md docs/progress/README.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 2. `specific-specific` 冲突检测与隧道四态已落地

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 2 个子步骤已经完成：管理面第一阶段 `specific-specific` 静态冲突校验、隧道四态派生，以及 listener 启动失败到“异常”状态的运行态回传已经进入真实代码和测试链路。

本次已完成的实现如下：

- 新增 `frps/internal/ports` 共享 helper，当前只处理 `specific-specific` 冲突：只比较同协议、相同具体 `effective_ip`、启用分组下启用隧道的远端端口区间重叠。
- `createTunnel()`、`updateTunnel()`、`updateProxyGroup()` 现在都会在写库前重算受影响隧道集合；命中当前可判定冲突时直接返回 `409 Conflict`，不再把这类配置写入数据库。
- 当前阶段明确不处理 wildcard 规则：`0.0.0.0`、`::` 以及 wildcard-specific / wildcard-wildcard / IPv4-IPv6 的扩展口径仍然延后，不在这一步实现里混入。
- 管理 API 的隧道返回模型已补充 `status` 和可选 `status_reason`，当前状态固定为四态：`启用 / 禁用 / 冲突 / 异常`。
- 当前隧道状态优先级已经收口为：
  - 分组禁用或隧道禁用时显示 `禁用`
  - 否则，命中 `specific-specific` 静态冲突时显示 `冲突`
  - 否则，`frpc` 登录后 listener 启动失败时显示 `异常`
  - 其余情况显示 `启用`
- 控制面 `ensureTunnelListeners()` 现在会把 listener bind/start 失败包装成结构化运行态错误，并记录到服务端内存态；同一 tunnel 后续成功启动 listener 时会清除对应异常。
- WebUI 隧道表格已切换为消费后端返回的 `status` / `status_reason`，对 `冲突`、`异常` 展示危险态标签与原因提示。
- 正式文档 `docs/frps/functional-spec.md`、`docs/frps/technical-design.md` 已同步改写为当前已实现口径，不再继续保留“当前完全不做端口冲突检测”的旧描述。

本次收束后的边界如下：

- 当前只实现 `specific-specific`，不把 wildcard 规则、双栈特殊行为或外部进程预扫描混进本步骤。
- `异常` 当前只承接 listener 启动失败这一类运行态问题，不泛化为所有 tunnel 运行态故障。
- 隧道状态仍然是运行态派生值，不新增持久化状态字段，也不回写数据库。

本次执行的验证如下：

- `go test ./...`（目录：`frps/`）
- `git diff --check`
- 检查 `.gitignore`，本次无需新增忽略项

## 3. Linux/Windows 监听差异与 Linux 网卡扫描已补充 Python 验证

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 3 个子步骤已完成一次平台验证补充：新增 Python 探针来复现实机 wildcard 绑定差异，并在 WSL Linux 对本机网卡/IP 快照规则做了实测校验。

本次已完成的执行如下：

- 新增 `test/wildcard_bind_matrix.py`：对 `tcp` / `udp` 统一输出 `wildcard-wildcard`、`wildcard-specific`、IPv4/IPv6 组合在同端口二次绑定时的结果矩阵。
- 新增 `test/linux_network_snapshot_probe.py`：按 `frps/internal/system/network_collect.go` 的排序和去重语义输出 Linux 地址快照，并支持 `--expect-ip` 断言关键地址存在。
- `docs/frps/testing-debugging.md` 已新增“Python 平台探针（免编译）”入口，固定 Windows 与 WSL 的复现实验命令。

本次关键实验结果如下：

- Linux（WSL）网卡扫描探针识别到了已创建的多张虚拟网卡和多 IP：`veth1..veth5` 的 `192.168.8.2..6` 与 `192.168.8.130..134` 均命中；`missing_expected_ips` 为空。
- TCP 绑定上，Linux 对更多 wildcard/specific 组合返回 `address already in use`，而 Windows 在部分 IPv4/IPv6 交叉组合下允许并存，平台差异明显。
- UDP 绑定上，当前实验环境中 Linux 与 Windows 对同端口重叠绑定的接受度也不一致，说明 wildcard 规则不能只凭单平台行为收口。

本次收束后的边界如下：

- 当前新增探针定位为平台行为验证工具，不替代 `frps` 管理面冲突判定本身。
- 下一步仍需把这些平台差异转换成统一、可预期的静态冲突规则，避免配置层与运行态口径分裂。

本次执行的验证如下：

- `python test/wildcard_bind_matrix.py --output test/tmp/wildcard-bind/windows.json`
- `wsl -d ubuntu -u root bash -lc "python3 /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/wildcard_bind_matrix.py --output /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/tmp/wildcard-bind/linux.json"`
- `wsl -d ubuntu -u root bash -lc "python3 /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/linux_network_snapshot_probe.py --output /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/tmp/wildcard-bind/linux-network-scan.json --expect-ip 192.168.8.2 --expect-ip 192.168.8.130 --expect-ip 192.168.8.3 --expect-ip 192.168.8.131 --expect-ip 192.168.8.4 --expect-ip 192.168.8.132 --expect-ip 192.168.8.5 --expect-ip 192.168.8.133 --expect-ip 192.168.8.6 --expect-ip 192.168.8.134"`
- `git diff --check -- test/wildcard_bind_matrix.py test/linux_network_snapshot_probe.py docs/frps/testing-debugging.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 4. Lin/Win 冲突差异与统一保守规则已同步到技术文档

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 4 个子步骤已完成：已把 Linux / Windows 的监听冲突差异、wildcard 静态判定规则和“规范依据 + 本机实验”一起写入技术文档，作为下一阶段 wildcard 实现的固定输入。

本次已完成的更新如下：

- `docs/frps/technical-design.md` 的隧道校验章节已补充“下一阶段 wildcard 静态冲突规则（已收口、待实现）”。
- 已明确 Lin/Win 差异与统一策略：
  - Linux `IPV6_V6ONLY`、Windows dual-stack 默认行为不同；
  - Windows `SO_REUSEADDR` 与 Go `net` 的 listener 默认选项口径不同于 Linux；
  - 管理面静态判定继续按跨平台可预期优先的保守策略执行，不以单机偶发 bind 成功判定配置合法。
- 技术文档已加入官方参考链接（Linux man pages / RFC / Microsoft Learn）并保留本机 Python 探针实验作为共参考。

本次收束后的边界如下：

- 当前仅完成规则和差异文档收口，不在本步骤引入 wildcard 代码实现。
- 下一步最小动作切到实现层：把已收口的 wildcard 规则接入 `frps/internal/ports` 与管理面/控制面入口。

本次执行的验证如下：

- `git diff --check -- docs/frps/technical-design.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 5. 跨平台唯一方案已定稿并写入技术文档

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 5 个子步骤已完成：基于 Lin/Win 差异，已在技术文档中固定一套可跨平台落地的唯一方案（V1），并明确后续实现必须严格按该方案执行，不做平台分支。

本次已完成的更新如下：

- `docs/frps/technical-design.md` 已把 wildcard 规则从“原则描述”升级为“唯一方案（V1）”：
  - 判定主公式固定为：`同协议 + 端口区间相交 + 监听空间重叠 => 冲突`
  - `effective_ip` 分类固定为 `Any4(0.0.0.0)` / `Any6(::)` / `Specific4` / `Specific6`
  - 文档内新增固定冲突矩阵（跨平台统一，不区分 Linux / Windows）
- 已明确管理面与控制面的执行边界：
  - 管理面按固定矩阵拒绝冲突配置，不因单机偶发 `bind` 成功而放宽。
  - 控制面继续保留运行态 `listen` 失败到 `异常` 的兜底回传，不反向改写静态规则。
- 已补充“为什么采用该唯一方案”的依据链：
  - Linux / Windows `IPV6_V6ONLY` 默认行为差异
  - Go `net` 在 Win/Lin listener 默认 socket 选项差异
  - 本机 `wildcard_bind_matrix.py` 的差异实验结果

本次收束后的边界如下：

- 当前只完成唯一方案文档定稿，不在本步骤引入代码实现。
- 后续 wildcard 冲突实现必须逐项对齐该矩阵，不再回到“按平台分别设计规则”的路线。

本次执行的验证如下：

- `git diff --check -- docs/frps/technical-design.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 6. wildcard 冲突检测已实现并完成 Win/WSL 调试验证

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的第 6 个子步骤已完成：`frps/internal/ports` 已实现 wildcard + specific 的统一冲突比较，管理面写库前冲突校验与隧道状态派生已切换到同一套比较逻辑，并补了 Windows/WSL 调试验证。

本次已完成的实现如下：

- 新增 `frps/internal/ports/conflict.go`，提供通用 `Claim/Conflict` 模型和 `DetectConflicts()`：
  - 固定比较条件：同协议 + 端口区间相交 + 监听空间重叠
  - 监听空间按 `Any4 / Any6 / Specific4 / Specific6` 矩阵比较
- 管理面冲突校验已切换为通用冲突比较：
  - `createTunnel()`、`updateTunnel()`、`updateProxyGroup()` 统一使用 wildcard-aware 校验
  - 冲突错误与隧道状态 `status=冲突` 统一来自同一套冲突结果，不再只限 `specific-specific`
- 隧道状态派生已支持 wildcard 冲突展示：
  - wildcard 组合导致的监听空间重叠现在会在 `GET /api/v1/tunnels` 中稳定显示 `冲突` 与 `status_reason`
- 新增/扩展测试：
  - `frps/internal/ports/conflict_test.go`：矩阵判定与 range 覆盖
  - `frps/internal/api/server_test.go`：wildcard 冲突拒绝、允许组合、状态派生回归

本次文档同步如下：

- `docs/frps/technical-design.md`：更新为“wildcard 已实现”的当前口径
- `docs/frps/functional-spec.md`：更新管理面冲突规则和隧道状态定义，移除“wildcard 未实现”的旧描述

本次执行的验证如下：

- `gofmt -w frps/internal/ports/conflict.go frps/internal/ports/conflict_test.go frps/internal/api/management.go frps/internal/api/server_test.go`
- `go test ./internal/ports ./internal/api`（目录：`frps/`，Windows 本机通过）
- `python test/wildcard_bind_matrix.py --output test/tmp/wildcard-bind/windows-after-impl.json`
- `wsl -d ubuntu -u root bash -lc "python3 /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/wildcard_bind_matrix.py --output /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/tmp/wildcard-bind/linux-after-impl.json"`
- `wsl -d ubuntu -u root bash -lc "python3 /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/linux_network_snapshot_probe.py --output /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/test/tmp/wildcard-bind/linux-network-scan-after-impl.json --expect-ip 192.168.8.2 --expect-ip 192.168.8.130 --expect-ip 192.168.8.3 --expect-ip 192.168.8.131 --expect-ip 192.168.8.4 --expect-ip 192.168.8.132 --expect-ip 192.168.8.5 --expect-ip 192.168.8.133 --expect-ip 192.168.8.6 --expect-ip 192.168.8.134"`
- `wsl -d ubuntu -u root bash -lc "cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./internal/ports ./internal/api"`（受限于当前网络，`go1.25.0` Linux toolchain 下载超时，未完成）
- `git diff --check -- frps/internal/ports/conflict.go frps/internal/ports/conflict_test.go frps/internal/api/management.go frps/internal/api/server_test.go docs/frps/technical-design.md docs/frps/functional-spec.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 7. 观测图单元颜色分级与悬停信息设计已收口

截至 2026-04-22，已按当前跨平台唯一冲突规则补充“端口占用观测图”单元格颜色设计，形成四档可用度方案和悬停详情字段规范。

本次已完成的更新如下：

- `docs/webui/port-usage-map.md` 新增“单元格可用度颜色规范（以本节为准）”：
  - 绿色：可用
  - 黄色：大部分可用
  - 橙色：小部分可用
  - 红色：完全不可用
- 颜色分档不再是二元占用状态，而是基于统一冲突矩阵计算 `availability_ratio`。
- 悬停浮层字段固定为：基础信息、候选统计、冲突明细（协议、候选监听类型、冲突目标、冲突原因）。
- 追加前后端一致性要求：颜色判定优先消费后端返回的可用度统计与明细，不允许前端另算一套规则。

本次收束后的边界如下：

- 本次只完成 UI 语义和字段设计，不引入新的后端 API 实现。
- 当前轮主线 `todo` 的唯一下一步保持不变，继续推进控制面运行态冲突语义收口。

本次执行的验证如下：

- `git diff --check -- docs/webui/port-usage-map.md docs/progress/2026-04-22.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 8. WSL UDP 回归不稳定已修复

截至 2026-04-22，针对 Linux/WSL 下 `internal/control` 的 UDP 回归偶发 `connection refused` 问题已完成修复并验证通过。

本次问题与修复如下：

- 现象：`go test ./...` 在 WSL 下偶发失败，典型报错为：
  - `TestServerForwardsPublicUDPDatagramsAndReusesSession`
  - `TestServerStartsListenersForEnabledUDPRangeTunnelAndUsesMatchedRemotePort`
  - `write udp ...: write: connection refused`
- 原因：测试辅助函数在 `config.ack` 后立即向 UDP listener 发首包，Linux 下在 listener 刚起阶段可能返回瞬时 `ECONNREFUSED/ECONNRESET`，原逻辑直接 `Fatal` 导致误判失败。
- 修复：`frps/internal/control/server_test.go` 的 `writeUDPAndReadOpenFrame` 和 `writeUDPToAndReadOpenFrame` 改为在截止时间内重试可恢复的 UDP 写错误（`connection refused/reset`），仅对非可恢复错误立即失败。
- 边界：本次仅修测试稳定性，不改线上控制面逻辑和协议行为。

本次执行的验证如下：

- `go test ./internal/control -run "TestServerForwardsPublicUDPDatagramsAndReusesSession|TestServerStartsListenersForEnabledUDPRangeTunnelAndUsesMatchedRemotePort" -count=1 -v`（Windows 通过）
- `wsl -d ubuntu -u root bash -lc 'cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./internal/control -run "TestServerForwardsPublicUDPDatagramsAndReusesSession|TestServerStartsListenersForEnabledUDPRangeTunnelAndUsesMatchedRemotePort" -count=1 -v'`（WSL 通过）
- `wsl -d ubuntu -u root bash -lc 'cd /mnt/c/Users/Zightch/Desktop/Aicksaim/frp/frps && go test ./...'`（WSL 全量通过）
- `git diff --check -- frps/internal/control/server_test.go docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 9. 控制面运行态端口冲突收口已完成

截至 2026-04-22，当前轮“`frps` 端口冲突检测最小闭环”的最后一个子步骤已完成：共享冲突矩阵已经接入 `ensureTunnelListeners()` 与 `effective_ip` 本地重绑路径，控制面会在启动前比较当前快照与其他在线分组的监听声明，并把实际 `bind` 占用统一收口为明确的端口冲突原因。

本次已完成的实现如下：

- `frps/internal/control/listeners.go` 已新增运行态 claim 投影和 `ports.DetectConflicts()` 预检查：当前待启动分组会按 `GroupRuntime + ConfigSnapshot` 生成 `ports.Claim`，并与其他在线且 listener 已启动的分组运行态统一比较。
- `ensureTunnelListeners()` 现在会在真实 `net.Listen` / `net.ListenUDP` 前先做矩阵比较；命中运行态冲突时不再尝试部分启动，也不会等到底层 socket 错误才发现冲突。
- `effective_ip` 本地重绑路径继续复用 `rebindGroupRuntime() -> ensureTunnelListeners()`，因此只改绑定 IP 的在线热刷新也会命中同一套运行态冲突收口；失败时保留明确 runtime issue 并关闭当前控制连接。
- `tunnelListenerStartError` 的错误文案已统一规范：对 `EADDRINUSE` / WinError 10048 等端口占用返回明确的“端口冲突，无法启动”，其他错误仍保留原始启动失败原因。
- 每次新的 listener 启动尝试前都会先清理当前快照下旧 runtime issue，再写入本轮新的冲突/失败结果，避免旧异常残留。

本次新增/扩展测试如下：

- `frps/internal/control/runtime_status_test.go`：覆盖外部端口占用的错误归一化，以及与其他在线分组的运行态 claim 冲突预检查。
- `frps/internal/control/server_test.go`：覆盖 `effective_ip` 本地重绑命中运行态冲突后，会关闭当前会话并留下 runtime issue。

本次收束后的边界如下：

- 当前仍不做外部进程端口矩阵预扫描；外部占用继续靠真实 `listen` 失败兜底，但错误语义已经统一为明确端口冲突。
- 控制面当前只给“当前准备启动/重绑的分组”记录 runtime issue，不去反向改写已正常运行的其他在线分组状态。
- 隧道四态与数据库配置保持不变：控制面运行态冲突仍通过现有 `冲突 / 异常` 派生路径展示，不新增持久化字段。
- 至此，当前轮“`frps` 端口冲突检测最小闭环”总目标已完成。

本次执行的验证如下：

- `gofmt -w frps/internal/control/listeners.go frps/internal/control/runtime_status_test.go frps/internal/control/server_test.go`
- `go test ./internal/control -run "TestServerEnsureTunnelListenersRecordsAndClearsRuntimeIssueOnBindFailure|TestServerEnsureTunnelListenersDetectsRuntimeConflictWithActiveGroup|TestServerRefreshGroupRebindsListenersWhenOnlyEffectiveIPChanges|TestServerRefreshGroupClosesSessionWhenEffectiveIPRebindConflictsWithActiveGroup" -count=1`（目录：`frps/`）
- `go test ./...`（目录：`frps/`）
- `git diff --check -- frps/internal/control/listeners.go frps/internal/control/runtime_status_test.go frps/internal/control/server_test.go docs/frps/technical-design.md docs/frps/functional-spec.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 10. 启动期全量扫描与未监听隧道轮询已补齐

截至 2026-04-22，上一轮控制面运行态端口冲突收口后补查出的两个缺口已经完成修补：`frps` 现在会在开放 `frpc` 控制端口前先完成一次“当前没有 listener 的启用隧道”全量运行态扫描，启动完成后继续固定轮询同一批未监听隧道，用于写入和清理隧道 `异常` 状态。

本次已完成的实现如下：

- `frps/internal/control/server.go`：
  - 新增启动期扫描门闩；`ListenAndServe()` 会先完成首轮未监听隧道扫描，再真正打开控制监听端口，因此 `frpc` 登录不会早于首轮隧道状态标记。
  - 新增后台轮询生命周期管理；控制面启动后会固定轮询“当前没有 listener”的启用隧道，关闭时同步停止轮询并等待退出。
- `frps/internal/control/runtime_scan.go`：
  - 新增未监听隧道扫描实现，统一负责：
    - 读取全部 `GroupRuntime`
    - 清理已删除 tunnel 的旧 runtime issue
    - 跳过当前已有真实 listener 的在线分组
    - 对未监听启用 tunnel 先按静态冲突矩阵过滤，再比较与当前在线 listener 的运行态冲突
    - 对剩余 tunnel 做临时 `bind` 探测，记录外部端口占用或其他启动失败
    - 在端口恢复可用或 `effective_ip` 恢复后自动清理旧异常
- `frps/internal/control/listeners.go`：
  - 修补 `effective_ip` 失效路径的隧道状态标记缺口；现在即使在真正启动 listener 前就因为本机地址失效而失败，也会把对应启用 tunnel 统一标为 `异常`，不再只返回错误而不落状态。
- `frps/internal/control/repository.go`：
  - Repository 新增 `ListGroupRuntimes()`，供启动期扫描与后续轮询复用当前完整运行态投影视图。

本次新增/扩展测试如下：

- `frps/internal/control/runtime_status_test.go`：
  - 覆盖未监听 tunnel 命中外部端口占用后会被标异常，端口恢复后再次扫描会自动清掉异常。
  - 覆盖 `effective_ip` 当前不在本机时，所有启用 tunnel 都会被写入明确异常原因。
- `frps/internal/control/server_test.go`：
  - 覆盖 `ListenAndServe()` 会先完成首轮运行态扫描，再开放控制端口。
  - 顺手收紧原有 `effective_ip` 缺失回归，验证会同时落下隧道 runtime issue。

本次收束后的边界如下：

- 后台轮询当前只扫描“当前没有 listener”的启用 tunnel；已经真实监听中的 tunnel 不参与这条扫描路径。
- 轮询只负责隧道状态标记与清理，不会替离线分组或先前失败的在线分组自动补开 listener；真实 listener 启停仍走现有登录 / `config.ack` / 热重载路径。
- 静态冲突 `status=冲突` 仍优先于 runtime issue；未监听扫描不会把静态冲突 tunnel 再额外标成 `异常`。

本次执行的验证如下：

- `gofmt -w frps/internal/control/repository.go frps/internal/control/server.go frps/internal/control/listeners.go frps/internal/control/runtime_scan.go frps/internal/control/runtime_status_test.go frps/internal/control/server_test.go`
- `go test ./internal/control -count=1`（目录：`frps/`）
- `go test ./...`（目录：`frps/`）
- `git diff --check -- frps/internal/control/repository.go frps/internal/control/server.go frps/internal/control/listeners.go frps/internal/control/runtime_scan.go frps/internal/control/runtime_status_test.go frps/internal/control/server_test.go docs/frps/technical-design.md docs/frps/functional-spec.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 11. 管理 API 首次可见状态已收紧到首轮扫描之后

截至 2026-04-22，启动顺序又向前收紧了一层：管理 API 现在也会等待控制面首轮未监听隧道扫描完成后才开始监听，因此管理端首次看到的隧道状态已经包含首轮扫描结果，不再存在“API 先开放、状态后补齐”的启动窗口。

本次已完成的实现如下：

- `frps/internal/control/server.go`：
  - 新增 `EnsureInitialRuntimeScan(ctx)`，把控制面首轮扫描抽成显式幂等入口。
  - `ListenAndServe()` 继续在自身启动前调用该入口，因此直接使用 control server 的场景仍保留原有门闩语义。
- `frps/internal/app/app.go`：
  - `App.Run()` 现在会在创建 control server 后、启动管理 API 和控制监听前，先执行 `EnsureInitialRuntimeScan(ctx)`。
  - 只有首轮扫描成功后，才会继续启动管理 API 和控制 listener。
- `frps/internal/app/startup_test.go`：
  - 新增 app 级启动顺序回归，固定“首轮扫描卡住时，管理 API 端口仍不可连接；扫描放行后管理 API 才开始监听”。
  - 为了稳定复现，测试在 app 层注入带阻塞 `ListGroupRuntimes()` 的 control server 构造路径。

本次收束后的边界如下：

- 当前启动门闩已经同时覆盖管理 API 和 `frpc` 控制端口；两者都会等待同一轮首轮扫描完成后再开始对外可见。
- 首轮扫描仍只负责写入/清理无 listener 启用 tunnel 的状态，不负责替离线分组自动补开 listener；这条边界保持不变。

本次执行的验证如下：

- `gofmt -w frps/internal/app/app.go frps/internal/app/startup_test.go frps/internal/control/server.go`
- `go test ./internal/app ./internal/control -count=1`（目录：`frps/`）
- `go test ./...`（目录：`frps/`）
- `git diff --check -- frps/internal/app/app.go frps/internal/app/startup_test.go frps/internal/control/server.go docs/frps/technical-design.md docs/frps/functional-spec.md docs/progress/2026-04-22.md docs/tmp/todo.md`
- 检查 `.gitignore`，本次无需新增忽略项

## 12. 首轮 `effective_ip` 无效登录已改为明确拒绝客户端启动

截至 2026-04-22，当前轮“`frps` 冲突检查与热更新运行时交互策略”的第 1 个子步骤已完成：首次登录进入首个 `config.ack` 启动监听路径时，如果分组 `effective_ip` 无效或当前不在本机，服务端现在会直接拒绝该次客户端上线，并向客户端返回明确的“请联系管理员解决”类错误。

本次已完成的实现如下：

- `frps/internal/control/listeners.go` 为分组 `effective_ip` 启动失败补充了类型化错误，区分“IP 字面量无效”和“当前不在本机”两类场景，不再只依赖字符串判断。
- `frps/internal/control/config.go` 在首个 `config.ack` 成功后、首次尝试启动监听失败时，会识别上述 `effective_ip` 错误并返回 `config apply failed` 错误帧，错误文案明确提示客户端“请联系管理员解决”。
- `frps/internal/control/listeners.go` 仍然会先按既有逻辑写入 tunnel runtime issue，再终止这次首轮上线，保证管理面状态标记和客户端可见报错同时成立。
- 非首轮启动路径未被放宽或扩大：后续热更新 / 重载仍维持现状，本子步骤只收敛首次登录启动这一条链路。

本次新增/扩展测试如下：

- `frps/internal/control/server_test.go` 新增首次登录 `effective_ip` 不在本机时的回归：验证客户端在 `config.ack` 后收到 `ErrorCodeConfigApplyFailed`，错误文案包含“当前不存在于本机”和“请联系管理员解决”，随后连接被关闭。
- 原有 `effective_ip` 运行态扫描与启动测试继续通过，确认 runtime issue 标记行为没有被这次客户端报错改坏。

本次收束后的边界如下：

- 当前只对“首次 `config.ack` 后首次启动监听”这一刻的 `effective_ip` 无效/缺失本机场景做直接拒绝。
- 数据库解码阶段或后续热更新阶段的 `effective_ip` 处理策略不在本子步骤内改动，留给后续子步骤继续收敛。
- 普通端口冲突、外部占用或其他监听启动失败暂不改成同类客户端提示，避免在本步骤内扩大行为面。

本次执行的验证如下：

- `gofmt -w frps/internal/control/listeners.go frps/internal/control/runtime_scan.go frps/internal/control/session.go frps/internal/control/config.go frps/internal/control/server_test.go`
- `go test ./internal/control -run "TestServerRejectsInitialConfigAckWhenEffectiveIPIsNotCurrentLocalIP|TestServerStartsTunnelListenerOnlyAfterConfigAckAndStopsOnShutdown|TestServerScanNonListeningTunnelRuntimeIssuesRecordsEffectiveIPFailureForEnabledTunnels" -count=1`（目录：`frps/`）
- `go test ./internal/control -count=1`（目录：`frps/`）
- 检查 `.gitignore`，本次无需新增忽略项
