# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 等待下一轮目标写入；证书 / CA 资产层当前只剩 WebUI 是否恢复推进待确认，但按最新口径本轮先不做。

子步骤：

- 如果后续恢复证书资产管理页，再把已完成的后端接口接到 WebUI。
- 如果不恢复 WebUI，直接按下一轮新目标重写本文件。

当前轮边界：

- 证书 / CA 资产层后端闭环已完成：启动准备、运行时管理 API、结构化错误、删除影响扫描和级联删除都已落地。
- WebUI 页面按本轮最新要求暂不推进，不把未做的前端工作写成已完成。
- 当前仍不展开端口 / SNI / 反代 / WebUI HTTPS / listen 绑定，也不实现 tunnel CA 池消费与 `frps -> frpc` CA 下发。

当前唯一下一步：

- 等待你确认下一轮目标，或明确是否恢复证书资产 WebUI。
