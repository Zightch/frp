# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

## 当前总目标

在不改动后端 API、认证语义、现有路由路径和已实现管理能力的前提下，重构 `frps/webui` 为更现代化、更清晰的管理面板布局。新界面需要统一视觉系统、梳理导航层级、提升管理页的信息密度与可读性，并同时保证桌面端与移动端可用。

## 剩余子步骤

4. 登录与初始化页重构
   - 统一认证页面视觉语言，使初始化、登录、异常提示和会话状态表达保持一致。
5. 收束与验收
   - 完成前端构建、响应式检查、临时文件清理、正式文档同步和进度归档。

## 当前轮边界

- 只做前端信息架构、视觉和交互层重构，不新增后端 API、数据库字段或控制面协议。
- 不改动现有认证流程、路由语义、分组管理能力、隧道管理能力和 token 使用模型。
- 继续沿用 Vue + Element Plus，不引入重型状态管理、低价值封装层或新的前端框架迁移。
- 页面表达必须严格基于当前真实能力，不把未来功能写成已实现入口。

## 当前唯一下一步

进入第 4 步，先只重构 `InitSecretView.vue` 和 `LoginView.vue`：在保持当前初始化流程、challenge 登录流程和路由语义不变的前提下，统一认证页的版式、状态提示、表单层次和异常反馈表达，但暂不进入最终收束与验收步骤。
