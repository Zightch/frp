# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 收敛 `frps` 冲突检查与热更新的运行时交互策略，并继续把 listener 启停粒度下沉到 tunnel 级。

子步骤：

- 运行时监听收敛粒度下沉到 tunnel 级：单个 tunnel 的冲突、端口占用或 bind 失败只阻塞该 tunnel，不拖垮同组其他健康 tunnel，也不默认关闭整个 session。
- 后续轮询扫描所有“启用但当前没有监听”的 tunnel：包括未登录分组和已登录但部分 tunnel 未拉起的分组；轮询除更新状态外，还要在存在活动 session 时补启动已恢复的 tunnel。
- 补齐 tunnel 级收敛、在线补启动恢复、静态冲突优先级不变等测试，再归档进度并提交。

当前轮边界：

- 只处理冲突检查与热更新之间的运行时交互，不改动静态配置冲突校验规则。
- 管理 API 首次可见状态收紧到首轮扫描之后这一项已完成，本轮不重复展开。
- 首轮客户端登录时 `effective_ip` 无效/不在本机直接拒绝上线这一项已完成，本轮不重复展开。
- 在线分组 `effective_ip` 热更新为无效/不在本机时，下发空配置并保留 control 会话这一项已完成，本轮不重复展开。
- “下发空配置”语义指服务端把该组运行时监听收缩为 0 个 tunnel，但保留已认证 control 会话，等待后续恢复。
- 静态配置冲突仍然优先显示为“冲突”；运行时问题继续显示为“异常”。

当前唯一下一步：

- 先把运行时 listener 启停/冲突收敛粒度下沉到 tunnel 级，避免单个 tunnel 启动失败拖垮整组和整个 session。
