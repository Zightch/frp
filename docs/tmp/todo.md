# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 继续为端口冲突检查 / 热更新交互补齐正式测试矩阵；当前确定性场景、异常输入矩阵、恢复故障场景和竞争窗口已全部补齐，后续转入稳定性补充层入口建设。

子步骤：

- 稳定性补充层：
  - 在确定性场景补齐后，再补 `-race`、多 `GOMAXPROCS`、高频配置抖动、长稳 soak、双平台回归和资源压力回归入口。
  - 这些入口必须复用现有观测和失败判定，不允许退回“跑一晚看日志”。
- 可测性验收门槛：
  - 任一准备进入正式测试矩阵的场景，先要求能在单机连续复现、在快慢机器上保持同一状态结论、在失败时输出明确状态断言差异；做不到这一点的场景，先回到前置基建补能力。

当前轮边界：

- 启动门闩、管理 API 首次可见内容、未监听轮询补启动、首次握手快照变更、首次 `config.ack` 后立即故障边界、异常输入矩阵、“空配置保活后的客户端断线重连”、“外部占用高频抖动同一端口”、“`effective_ip` 在本机地址集合内外高频抖动”、“恢复路径资源层故障系统性覆盖”，以及全部竞争窗口（`config.push/config.ack` 乱序、session 接管旧帧隔离、blocked bind 交错、轮询扫描自身重叠、扫描与网络快照刷新并发、服务端关闭并发）都已完成并归档；当前 `todo` 只保留剩余稳定性任务，不回填已完成项。
- 当前轮继续优先复用 `testhooks`、`ObservedState`、`ScriptedListenerFactory`、`ScriptedFrameIO` 和现有不变量断言；旧式 `sleep/poll` helper 仅在现有夹具不可替代处少量复用。
- 当前轮的资源层故障场景已经确认需要串行验证；不要并发启动多个依赖本机端口分配的 `go test` 进程，避免测试进程之间互相抢端口造成假阳性。
- 当前轮仍聚焦端口冲突检查与热更新交互，不扩展到其他独立功能优化。
- 测试工作不局限于现有 `*_test.go`，允许包含脚本化双进程场景、异常注入、双平台验证与长稳回归。

当前唯一下一步：

- 开始稳定性补充层的第一步：把 `-race`、多 `GOMAXPROCS` 和高频配置抖动这 3 类入口整理成可复用、可判定成败的统一执行入口。
