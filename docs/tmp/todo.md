# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 在线整组配置热重载最小闭环落地。

子步骤：

- 待完成：补单端口 / 端口范围在线热重载回归。

当前轮边界：

- 在线热重载继续复用现有 `config.push / config.ack`，不新增单 tunnel 增量协议事件。
- 分组离线时只写数据库，不做运行态动作。
- 首版按“整组冻结 + 整组全量重建”收口，不承诺保留未受影响 tunnel 的现有连接。
- 分组名和隧道名修改不触发运行态热重载。
- 端口范围只要边界或映射基准变化，就按整 tunnel 替换处理，不做重叠区间复用优化。

当前唯一下一步：

- 先修正现有 TCP/UDP e2e seed 数据里的 `effective_ip` 缺口，为单端口在线热重载回归建立可运行基线。
