# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 完成“端口冲突检查 / 热更新交互”当前代码树的跨平台全量复验，并把结果归档成正式汇报文档。

子步骤：

- 在 Linux 环境复跑同一套全量回归，确认平台基线与关键稳定性场景结论一致。
- 汇总 Windows / Linux 的测试范围、命令、结果、失败项（如有）与仍存风险，归档到 `docs/progress/2026-04-23.md`。

当前轮边界：

- 本轮只做测试执行、结果确认和文档归档，不改业务实现逻辑，除非测试暴露阻塞性缺陷且用户另行确认修复。
- 依赖本机端口可见性的测试命令必须串行执行，不并发启动多个 `go test` 进程。

当前唯一下一步：

- 在 WSL Ubuntu 上串行复跑 `frps` 与 `frpc` 的全量测试入口，完成 Linux 基线确认。
