# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 收敛 `frps` 冲突检查与热更新的运行时交互策略，补齐“未监听 tunnel 的轮询恢复”和对应回归。

子步骤：

- 后续轮询扫描所有“启用但当前没有监听”的 tunnel：包括未登录分组，以及已登录但只有部分 tunnel 已拉起的分组；轮询除更新状态外，还要在存在活动 session 时补启动已恢复的 tunnel。
- 补齐“在线补启动恢复”“静态冲突优先级不变”等回归测试，再归档进度并提交。

当前轮边界：

- 只处理冲突检查与热更新之间的运行时交互，不改动静态配置冲突校验规则。
- 管理 API 首次可见状态收紧到首轮扫描之后这一项已完成，本轮不重复展开。
- 首轮客户端登录时 `effective_ip` 无效/不在本机直接拒绝上线这一项已完成，本轮不重复展开。
- 在线分组 `effective_ip` 热更新为无效/不在本机时，下发空配置并保留 control 会话这一项已完成，本轮不重复展开。
- 运行时 listener 启停/冲突收敛粒度下沉到 tunnel 级这一项已完成，本轮不重复展开。
- “下发空配置”语义指服务端把该组运行时监听收缩为 0 个 tunnel，但保留已认证 control 会话，等待后续恢复。
- 静态配置冲突仍然优先显示为“冲突”；运行时问题继续显示为“异常”。

当前唯一下一步：

- 让后续轮询真正扫描所有“启用但当前没有监听”的 tunnel，并在存在活动 session 时补启动已经恢复的 tunnel。
