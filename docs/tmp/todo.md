# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 为端口冲突检查与热更新交互整理一份全量测试方案，覆盖 `frps` / `frpc` 的功能正确性、非法输入、竞态与稳定性验证。

子步骤：

- 审查当前端口冲突检查、首次登录、热更新与轮询恢复链路，确认是否仍有明显代码缺陷或状态机缺口。
- 基于现有实现与测试，整理覆盖矩阵，覆盖首次启动、首轮扫描、首次登录、热更新、轮询恢复、静态冲突、运行态冲突、外部占用、无效 `effective_ip`、`pending config`、断线重连与恢复路径。
- 设计全量测试方案，区分单元测试、集成测试、端到端脚本、故障注入、竞态回归与稳定性/压力测试，并明确环境、注入手段、观测点和验收标准。

当前轮边界：

- 当前轮先做实现审查和测试方案设计，不修改业务实现代码。
- 测试方案不局限于现有 `*_test.go`，允许包含脚本化双进程场景、异常注入、双平台验证与长稳回归。
- 讨论范围只聚焦端口冲突检查与热更新交互，不扩展到其他独立功能优化。

当前唯一下一步：

- 审查当前 `frps` / `frpc` 代码链路，先汇报端口冲突检查与热更新之间是否存在明显缺陷。
