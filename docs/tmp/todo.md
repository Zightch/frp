# 当前轮 Todo

- 进度归档索引：`docs/progress/README.md`

当前总目标：

- 收口代码巡检中剩余的明显缺陷，优先处理会直接影响默认启动或外部调用契约的问题。

子步骤：

- 修复 `frps` 在 `webui/dist` 缺失时默认启动直接失败的问题。
- 统一 `PATCH /api/v1/proxy-groups/:id` 的实际语义与调用契约。

当前轮边界：

- 当前只继续处理 `frps` 侧巡检缺陷，不回到已完成的 `effective_ip` 功能链路。
- 每次只完成一个独立子步骤；完成后立即归档、提交并停下，等待用户确认。
- 修复启动失败问题时，优先保证服务可启动和管理 API 可用，不顺带扩展 WebUI 功能范围。

当前唯一下一步：

- 修复 `frps` 在未构建 `webui/dist` 时默认启动失败的问题，并完成归档与提交。
